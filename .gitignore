.DS_Store
__pycache__/
*.py[cod]

# These local resource databases exceed GitHub's normal 100 MB file limit.
# Use Git LFS or an external artifact store if they need to be published.
resources/*.sqlite

# Uncompressed corpus path inventories can exceed GitHub's 100 MB file limit.
# Keep the generated .jsonl.gz archives as the portable copies.
network/v3/experiments/corpus_neo_min5/*/paths/semantic_path_candidates.jsonl
network/v3/experiments/corpus_neo_min5/.corpus_candidates.lock
network/v3/experiments/corpus_neo_min5/corpus_run_state.json
network/v3/experiments/corpus_neo_min5/*/stage_failure.json
