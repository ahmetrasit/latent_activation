# Mature Formation Field: S112 / V7.5.9

This is the second turn of the same formation session. The primary scaffold is
still absent. Every local assertion remains `SOURCE_ACTUALITY`; every invariant
and parameter change below remains `LATENT_MAPPING`; every functional whole
whose roles come from independent source tuples remains `LATENT_ASSEMBLY`.
Token identity, proposition continuity, referent continuity, role recurrence,
and fresh performance are never substituted for one another.

The field was matured in stable surface order. Each named formation below has
been run through participants, controller, medium, direction, state change,
result, aftermath, vacancy, return, and backward rereading. The names identify
completed operations or structures, not ranks or candidate interpretations.

## 1. The Commanded Content Relay

**Mature form:** operational configuration plus distributed mapping.

**Exact entrance.** The positioned surface tuple is indivisible:

`implicit 2MS grammatical controller -> قُلْ [imperative, active, root ق و ل]
-> quotation boundary -> هُوَ ٱللَّهُ أَحَدٌ [whole content]`.

Supplied syntax leaves the speech verb outside the quotation and binds the
entire three-token nominal clause as its complement. The exact four-word
surface line, after removal of vocalization, also occurs as the complete source
utterance `قل هو الله أحد` in AHd-B001. That creates `LATENT_MAPPING`
`LM-CONTENT-EXACT`: proposition and word order recur; passage performance and
lexical-source quotation remain separate occurrences. Nothing identifies the
imperative's implicit controller beyond `2MS`, and no audience is named.

### Carrier succession at maximum resolution

QWL-B012 supplies a content-bearing psyche before expression: `المتصور في
النفس قبل الإبراز باللفظ قول` and `في نفسي قول لم أظهره`. QWL-B001 supplies
the next typed interface: composed letters are externalized through
pronunciation, whether the unit is `مفردا` or `جملة` or `قصيدة` or `خطبة`.
QWL-B002 supplies `المقول اللسان`, a bodily instrument. The maximal latent
carrier chain is therefore:

`conceived proposition [QWL-B012] -> psyche as current container -> expression
boundary -> tongue [QWL-B002] -> pronunciation [QWL-B001] -> new audible/spoken
token -> open receiver -> possible later carrier`.

The quote fills proposition schema; it does not fill psyche, tongue, performer,
or receiver by identity. If the commanded performance occurs, its token is a
fresh occurrence carrying the same proposition. If QWL-B012's `لم أظهره`
variant is enacted, proposition exists but the crossing is absent: the command
opens an output role, internal content reaches the boundary, and no external
token results. That is a completed blocked-performance variant, not absence of
content.

### Every supplied controller and medium replay

The content interface was replayed through every QWL controller/outcome tuple.
Each replay is a new carrier path with its own complete vector:

- **Fluent/frequent controller, QWL-B003.** A `قولة/قوال/قوالة/تقوالة/قؤول/
  مقوال/مقول` speaker repeatedly externalizes content. The exact AHd-B001
  reiteration `أحد أحد` can fill repeated-token structure. Result is more
  performances of one form; it does not create new content. Backward effect:
  the single surface imperative is now perceived as capable of generating a
  succession rather than only one token, while its actuality remains one
  command.
- **Effective title-holder, QWL-B004.** `القيل` possesses a saying whose
  outcome `ينفذ قوله`; the source also fixes rank below `الملك الأعظم`, plural
  titles, and feminine `قيلة`. Projected quote-content becomes an instrument
  of an effective word. Result is execution/effect, but the title-holder is not
  identified with the surface controller or referent. Backward effect: QWL-B001
  output is no longer only transmitted sound; under this changed controller it
  can alter another relation.
- **Nonexistent or misattributed content, QWL-B005.** One controller `قال ما لم
  يكن`; another assigns `ما لم أقل` to a claimed speaker; another `تقول عليه`
  and lies against him. The exact surface contact `لم يكن` in line 4 enters
  this source assertion, but the completed replay keeps truth status local:
  false speech produces a false-attribution aftermath; the passage's source
  content remains actual and is not relabelled false. Backward effect: the
  command's exact content and performer must be tracked separately, because a
  proposition can be carried under an alien controller.
- **Appropriating controller, QWL-B006.** `اقتال قولا` draws a saying toward
  self, branching good and evil content. Direction reverses the expression
  chain: outside content becomes inwardly acquired content. Result is
  possession/association at self; it is not the same occurrence as the
  internal conception that began there.
- **Social carrier succession, QWL-B007.** A good or bad `قالة` spreads `في
  الناس`; `القيل والقال` multiplies and circulates. Each listener/carrier can
  instantiate a fresh token. One return to an already circulating schema
  records the cycle and stops. Result is distributed recurrence; valuation
  remains two variants. Backward effect: the unnamed surface audience is a
  typed receiver opening, not an asserted crowd.
- **Reciprocal controllers, QWL-B009.** `قاولته في أمره وتقاولنا` makes one
  party's output the other's input around an affair. Enacted to fixed point,
  the exchange yields response and one return cycle; it does not endlessly
  rewrap the same negotiation state. The quote may fill one proposal only
  projectively. Result is negotiated relation, with addressee no longer a
  passive receiver.
- **Controlling controller, QWL-B010.** `اقتال عليه تحكم` fills direction
  `controller -> over another -> control`. If content occupies instrument, the
  completed assembly changes the other's agency rather than merely informing
  them. Consent remains unknown. This controller map stays distinct from the
  effective title-holder: one source supplies rank and an executing word; the
  other supplies control over an affected participant.
- **Supposition-performing construction, QWL-B011.** In interrogation,
  `تقول` operates like `تظن`; the named community extends inflected `قلت`
  outside interrogation. Here the speech form changes grammatical/evidentiary
  operation. Result is a supposition frame with a new controller, not a simple
  vocal copy. The exact surface `قل` inside `قلت` reactivates this whole tuple
  while retaining final `ت` and its different grammar.
- **Belief carrier, QWL-B013.** A person `يقول بقول` another: proposition
  continues as adopted belief/doctrine without requiring a new spoken token.
  Result is a state of adherence. Backward effect: proposition continuity
  alone cannot prove performance continuity.
- **Object-state as carrier, QWL-B014.** The filled basin `قال قطني` by
  indication. Controller changes from human speaker to an object's state;
  tongue and pronunciation are vacant without preventing successful
  signification. Result is perceived sufficiency. This completes the medium
  counterfield `bodily speech / state-sign`.
- **Sincere-attention carrier, QWL-B015.** `فلان يقول بكذا` can mean genuine
  care directed toward a thing. Content becomes an object of continued
  attention, not necessarily a transmitted proposition. Result is maintained
  orientation.
- **Definition carrier, QWL-B016.** Logicians use `قول الجوهر` and `قول العرض`
  as `حدهما`. Content becomes a boundary around a defined object. Result is a
  conceptual limit. The quote can fill proposed definition content only as a
  projected variant; no source identifies it as such.
- **Wooden-instrument branch, QWL-B008.** `القال الخشبة التي تضرب بها القلة`
  cannot carry proposition content merely by root membership. Provisional
  insertion changes the functional spine from communication to force:
  controller -> wooden implement -> strike -> affected `القلة` -> impact. The
  completed mismatch reroutes to Formation 11 rather than being discarded.

### Fixed-point result and open remainder

The mature operation is not "speech" as a topic. It is a typed succession in
which one exact proposition can be internal, commanded, externally performed,
repeated, enforced, falsely attributed, appropriated, circulated, negotiated,
used to control, made suppositional, adopted, indicated by an object, treated
with care, or used as a definition. Each phase changes controller, medium,
truth/evidentiary status, recipient, and aftermath. The only identity-connected
surface operation is command plus quoted content. All later realizations are
`LATENT_MAPPING` by proposition/role schema or `LATENT_ASSEMBLY` by projected
division of labor. The exact receiver of `قُلْ`, whether performance occurs,
and any response remain typed open ports.

## 2. The Referent Through Rotating Grammatical Stations

**Mature form:** actuality continuation and structural constellation.

The supplied surface supports one exact referential path:

`هُوَ [112:1:2 independent 3MS pronoun; apposition head]
-> ٱللَّهُ [112:1:3 named nominal; predication head]
-> ٱللَّهُ [112:2:1 same named referent; new line and predication head]
-> Ø [112:3:2 implicit active 3MS subject of يَلِدْ]
-> Ø [112:3:4 implicit affected/passive 3MS subject of يُولَدْ]
-> هُ [112:4:3 suffixed 3MS endpoint governed by ل in the complement of كفوا]`.

Every arrow is `SOURCE_ACTUALITY` continuation of the discourse occupant; the
local grammatical work changes at every station. The implicit `2MS`
controller of `قُلْ` is excluded from this path. The `3MS` agreement of
`يَكُن` is also excluded: supplied syntax binds that verb to delayed subject
`أَحَدٌ`, while `هُ` is the comparison endpoint. Shared person, number, and
gender do not create coreference.

The operation performed by the path is role rotation without occupant loss.
The referent begins as the content of a deictic pronoun/name relation; becomes
head of two predications; becomes active source-position in a negated birth
schema; becomes affected-position in the passive replay of the same negated
schema; and finally becomes relational endpoint rather than grammatical
controller. The two WLD stations do not assert state changes because `لَمْ`
denies both events. What changes is exposed role and voice.

Backward from `هُ`, the endpoint recovers the two implicit verbal stations,
the repeated name, and `هُوَ`. This changes how the opening pronoun functions:
it is not merely a static substitute but the first explicit carrier in a path
that later survives name repetition, null agreement, voice rotation, and
suffixation. Forward from `هُوَ`, the path makes line 4's pronoun exact
coreferential return while keeping `أَحَدٌ` a different grammatical occupant.

The mature constellation has no open identity slot for the central referent.
It retains one open semantic role at the command: who performs the imperative
is not fixed by this coreference. It also retains the passive initiator as
unknown and the negated counterpart candidate as uninstantiated.

## 3. The `أَحَدٌ` Enclosure With Polarity and Role Reversal

**Mature form:** structural constellation plus distributed mapping.

The complete carrier set is the two exact forms and the entire passage they
enclose:

`112:1:4 أَحَدٌ [indefinite, masculine, nominative predicate; affirmative]
-> lines 2--3 and first four words of line 4
-> 112:4:5 أَحَدٌ [same form/lemma/root/case; delayed nominative subject under
لم; passage-final]`.

AHd-B001 directly licenses the first position through exact full-line source
utterance `قل هو الله أحد`, `أحد بمعنى الواحد`, absolute descriptive use, and
`أحد أحد`. AHd-B002 directly licenses the final negative position:
`لا أحد في الدار`, `ما في الدار أحد`, and `أحد في النفي لاستغراق جنس
الناطقين ولا واحد ولا اثنان فصاعدا`. The mapping invariant is exact form,
lemma, root, indefiniteness, gender, nominative case, and boundary function.
The parameter transformation is
`predicate -> delayed subject`, `affirmative -> negated`, `quote close ->
passage close`, `description -> exhaustive candidate class`.

The completed passage-level recognition is an enclosure whose closing term
does not merely repeat its opening term: it reverses grammatical direction.
At the first boundary, the named referent receives `أحد` as predicate. At the
last, `أحد` supplies the candidate subject that would bear `كفوا له`, but the
copular relation is denied. Thus exact recurrence carries changed agency and
scope.

### Count and distribution re-entries

AHd-B003 supplies unit-plus-tens composition and causative `صيرهن أحد عشر`.
AHd-B004 supplies firstness in either side of an إضافة and named day
`الأحد`. AHd-B005 supplies a controller acting alone and a plural group arriving
`آحاد أحاد`. These create three distinct structural replays around the surface
counts:

- **Composition replay:** one can enter a larger counted expression, but the
  complete surface vectors `[4,2,4,5]`, `[2,1,2,1]`, and `[4,3,5,7]` are not
  arithmetically converted into eleven. The result is a live unit/group
  interface with no licensed surface calculation.
- **Ordinal replay:** the first `أحد` is earlier and the last is later, while
  AHd-B004's firstness can occupy an ordinal role. Its day-name variant joins
  the temporal formation, not the direct enclosure mapping.
- **Distribution replay:** exact endpoints are two positioned tokens, whereas
  `آحاد أحاد` distributes plural arrivals as individuals. Projecting that
  operation over the line sequence yields individually retained lines rather
  than one merged block; no group of passage occupants is asserted.

AHd-B006's `أحد جبل بالمدينة` cannot fill the predicate or negative candidate
by actuality. It matures in the place/elevation assembly (Formation 15), where
mountain is a concrete place filler.

Backward rereading from final exhaustive `أحد` sends the no-candidate mechanism
through `كفوا` and back to initial affirmative `أحد`; forward rereading sends
the first exact form through the intervening predicate, birth-voice pair, and
counterpart relation before it returns. The enclosure is complete. Its only
open remainder is how its formal whole will meet the withheld primary field.

## 4. The Name, Its Supplied Formation, and the Returned Endpoint

**Mature form:** structural constellation with an actuality lineage and a
formal-contact mapping.

ALH-B002 asserts a complete form transformation:

`إله [base] -> حذف الهمزة + إدخال الألف واللام -> الله [specialized name]`.

It also preserves oath, address, and invocation carriers: `الله ما فعلت`
meaning the oath form with `والله ما فعلته`; `اللهم` as `يا ألله`; `لاه أنت`;
`لا هم اغفر لنا`; `لاه أبوك`; `لهنك`; `لهنا`. These are distinct fresh
performances of the name. The passage then supplies exact `ٱللَّهُ` at 112:1:3
and 112:2:1, producing a same-name, same-referent actuality continuation with
ordinal shift `third word -> first word`.

Under the stipulated unvocalized comparison, complete surface `لَّهُۥ/له` is
also the terminal ordered sequence of written `إله` and `الله`; later it is
independently decomposed as preposition `ل` plus pronoun `ه`. This gives a
formal circuit:

`إله -> الله [asserted name formation]
-> terminal له [exact contained sequence only]
-> 112:4:3 له [separate preposition+coreferential suffix]
-> ه [returned referential endpoint]
-> earlier هو/الله [actuality continuation]`.

The circuit's payload changes cleanly at its joins. `إله -> الله` carries an
asserted lexical formation. `الله -> contained له` carries form and position
only. Surface `له -> ه -> earlier referent` carries grammar and coreference.
No worship or name semantics are imported into the two-letter prepositional
token.

ALH-B001 supplies another complete operation: a person/group worships; an
object becomes `معبود`; `لا يكون إلاها حتى يكون معبودا` makes reception of a
prior act a prerequisite for the role. This matures as a vacancy mechanism:
before worship is supplied, the role position is unavailable; after it, a
source-local worshipped object can bear the name-class. The sun, `الأصنام`,
and `الأوثان` remain the lexical examples' occupants. The passage's named
referent is never merged with them. In interaction with line 4, this
prerequisite vacancy is compared with denied counterpart occurrence: one role
is conditionally unavailable until a prior relation; the other relation is
terminally denied under `لم`. Their consequences remain different.

Backward effect: final `له` makes the earlier written name perceptible as a
container of the same two-letter sequence while simultaneously recovering the
same referent through suffixation. Forward effect: the repeated name supplies
the endpoint later referred to by `ه`, but it never controls `يكن`. The mature
constellation retains no unresolved formal contact; oath/request speakers and
requested acts remain independent source-local ports.

## 5. The Directed Need and the Reoriented Route

**Mature form:** distributed mapping plus projective latent assembly.

The exact surface entrance is 112:2:2 `ٱلصَّمَدُ`, definite nominative
predicate of the continuing named referent. SMD-B001 supplies a complete
directional topology repeatedly and explicitly:

`seeker/controller -> قصد واعتماد -> affair or حاجة -> السيد/endpoint sought
in needs -> arrival at a relied-upon relation`.

Its local carriers remain distinct: `صمدته`, `صمد هذا الأمر`, a lord sought in
needs, and `بيت مصمد` as a sought house. The surface predicate shares exact
form and lexical branch; it does not make any seeker, need, lord, or house a
surface occupant. This distinction permits a precise `LATENT_MAPPING` of
direction and a separate `LATENT_ASSEMBLY` of complementary roles.

### Maximal direction/controller map

Five complete tuples replay the same route slots under different operations:

| source tuple | controller | carried material/affected role | direction | endpoint/result |
|---|---|---|---|---|
| SMD-B001 | intending seeker | affair/need | toward another relied-upon target | intended reliance/approach |
| QWL-B006 | appropriator | saying, good or evil | toward self | acquired content |
| KFW-B002 | diverter | group | away from its prior direction | redirected group |
| SMD-B005 | first-person overseer | affair | positioned over/toward it | attentive oversight |
| QWL-B015 | caring person | thing/affair | sustained attention toward it | sincere care |

The invariant is controller + oriented relation + content/affected participant
+ endpoint. The parameter transformation is
`approach -> inward drawing -> diversion -> oversight -> care`. Physical travel
is not required in the last two; self replaces other in QWL-B006; KFW-B002
changes the outgoing endpoint instead of completing the incoming one.

### Completed assembly variants

- **Carried saying as need.** Formation 1 supplies externalized proposition as
  carried material; SMD-B001 supplies seeker, route, affair, and relied-upon
  receiver. The saying travels toward an endpoint and becomes a presented
  need. The endpoint's response remains open because no source tuple supplies
  one. Backward effect: `قُلْ` becomes capable of launching a directed request
  path, but the passage does not assert that the quote is a request.
- **Sought house and covered boundary.** SMD-B001 already asserts `بيت مصمد`
  as a sought house. KFW-B004 projects one or two sewn pieces onto its rear
  boundary. Operation: seeker approaches house; maker joins covering material;
  rear becomes covered; entrant/recipient remains open. Seeking is
  `SOURCE_ACTUALITY` only in its own house tuple; covering is
  `LATENT_ASSEMBLY` from another source event.
- **Approach prevented by diversion.** Intention and need begin along SMD-B001's
  route; KFW-B002 inserts a controller who turns the group `إلى غيره`. Result
  is nonarrival at the original endpoint and arrival-direction toward an
  unspecified other. The unfilled alternate endpoint remains typed rather than
  being silently supplied.
- **Stationary oversight.** SMD-B005 positions the controller `على صمادة من
  أمر`, overlooking and attending to the affair. KWN-B002 supplies place or
  relative station; QWL-B015 supplies genuine concern. No approach movement is
  needed. Result is a maintained supervisory relation.
- **Responsibility rather than location.** KWN-B003 supplies guarantor `على
  فلان`, beneficiary, and continuing obligation. SMD-B005 supplies attention.
  The relation `over/for` changes from spatial station to responsibility;
  outcome is care/liability rather than arrival.
- **Inward reversal.** QWL-B006 draws the already external saying back to self.
  The path `internal content -> external token -> self-acquisition` returns to
  an internal holder but not to the same typed state: the final content is
  acquired, not merely conceived. One cycle is recorded and stops.

The mature formation is a route whose controller, carried material, and
endpoint can change without loss of directionality. Its exact open remainder
is response at the relied-upon endpoint; no generated variant supplies it.

## 6. The Interior, the Applied Boundary, and the Failed Closure

**Mature form:** projective latent assembly with an exact material
counterfield.

The entrance contact is surface `ٱلصَّمَدُ` and released base `صمد`. The exact
source operations are not one event:

- SMD-B002 supplies a hard, compact, elevated/thick material or place,
  `المصمت الذي ليس بأجوف`, `لا جوف له`, rock fixed in place, ground without
  grooves.
- SMD-B003 supplies bottle, opening, `الصماد` stopper, applying agent, and a
  changed closed state: `صمدتها أصمدها`.
- SMD-B004 supplies head, wrapping agent, alternative media `خرقة/منديل/ثوب`,
  wrapped result, and explicit exclusion `ما خلا العمامة`.

Written `الصماد` retains its medial alif and is not flattened into exact
`الصمد`; the verbs `صمدتها/أصمدها` do contain the released base. The decisive
relations are therefore exact base contacts where present and functional
assembly elsewhere.

### Canonical roles

`region/container -> interior state -> exposed boundary/opening -> controller
-> applied medium -> resistance/closure -> outgoing inside/outside relation`.

Every supplied filler was enacted:

- **Cavityless solid.** SMD-B002 fills region, resistance, and explicit
  interior vacancy. Because no cavity/opening exists, projected SMD-B003
  stopper reaches no applicable boundary. The result is not a closed empty
  vessel; it is a failed closure attempt whose instrument has no typed port.
  This completed mismatch sharpens `no interior` as different from `interior
  retained`.
- **Stoppered bottle.** SMD-B003 fills vessel, opening, agent, and stopper.
  Incoming state is open/unstopped; operation applies `صماد`; outgoing state
  retains whatever interior position the bottle has behind a closed boundary.
  Contents and later opener remain open roles.
- **Stoppered then tilted bottle.** KFW-B002 supplies an independent controller
  who tips or overturns `الإناء`. With stopper still applied, outward route is
  resisted; without it, contents become an open spill position. These are two
  variants because stopper state differs. No source asserts the actual bottle
  was tipped.
- **Wrapped head.** SMD-B004 fills body region and closure medium. KWN-B002's
  `موضع العمامة` can project a head location, but the turban cannot fill medium
  because SMD-B004 excludes it. Cloth, kerchief, and garment each make a
  separate completed wrapper variant. Result is covered head, not sealed
  cavity.
- **Covered rear of shelter.** KFW-B004 supplies one or two pieces, reciprocal
  sewing when there are two, house/tent rear, and covered result. With one
  piece, partner role remains optional; with two, each is attached to the
  other. The applied boundary is planar and architectural rather than a mouth
  or body circumference.
- **Internal saying at an expressive boundary.** QWL-B012 fills psyche and
  existent content; QWL-B001 fills vocal crossing; QWL-B002 fills tongue. With
  `لم أظهره`, boundary remains closed and content stays inside. With
  pronunciation, a fresh token crosses out. This is transmission control, not
  physical stoppage.
- **Definition as boundary.** QWL-B016 supplies substance/accident as defined
  object and `قول` as `حد`. Result is conceptual delimitation. There is neither
  cavity nor cover material, yet the same functional slots boundary/open
  interpretation/closed determination are occupied.
- **Quotation boundary.** Surface syntax itself supplies command outside and
  content inside the quote. A performance can carry content outward while
  preserving the speech verb's exclusion from the quoted clause. This formal
  boundary transforms the material assembly backward: an opening may control
  what crosses without enclosing the controller.

The mature formation differentiates four vacancy consequences: no interior
(closure inapplicable), open container (closure possible), present internal
content but no performance (transmission blocked), and an undefined object
(conceptual boundary available). Its open remainders are bottle contents,
future opener, and receiver of expressed content.

## 7. Endurance That Continues to Send a Product

**Mature form:** source-local operation enlarged into a projective production
assembly.

SMD-B007 contains two actuality tuples. One asserts `الصمد الدائم` and
remaining after `فناء خلقه`. The other asserts a `ناقة مصماد` remaining through
`القر والجدب` while `الدائمة الرسل`. The camel tuple already differentiates
carrier, adverse conditions, duration, survival, and continuing outward
product. It is not merely a static durability image.

KFW-B005 supplies a one-year scale, camel or palm source, requested yield,
giver/receiver relation, fruit or `لبنها ووبرها وأولادها`, two alternating
herd cohorts, and an explicitly incomplete yield. WLD-B001 supplies offspring
class; WLD-B003 supplies mother, birth, impending/completed time, and attendants.
Together they mature the following `LATENT_ASSEMBLY`:

`camel/herd carrier [SMD-B007/KFW-B005]
-> cold and barrenness [SMD-B007]
-> persistence/resistance [SMD-B007]
-> one-year production phase [KFW-B005]
-> milk/wool/offspring [SMD-B007/KFW-B005/WLD-B001]
-> requester/recipient [KFW-B005]
-> completion, incompletion, or next alternating cohort [KFW-B005]`.

### Completed variants

- **Persistent and productive.** One carrier survives adverse conditions and
  continues milk; annual scale and recipient are projected from KFW-B005.
  Result is continued delivered product. Persistence revises the production
  tuple by making hardship a condition rather than a terminal interruption.
- **Persistent but incomplete.** KFW-B005's `ليست تامة كفأة` supplies
  aftermath. Carrier survives, but annual bundle is incomplete. This prevents
  survival and productivity from being merged.
- **Alternating renewal.** `أكفأت إبلي كفأتين` divides the herd into two
  production phases. First cohort yields; second succeeds it; one return to the
  first phase records a cycle and stops. SMD-B007 supplies adversity resistance
  across the succession. Result is renewed rather than simultaneous output.
- **Attended birth inside the year.** WLD-B003 supplies mother and attendant
  roles; KFW-B005 supplies yearly measure; WLD-B001 supplies product number/
  gender branches. Result is offspring production supported by attendants.
  The source animals/events differ, so the whole remains projective.
- **Palm-scale branch.** KFW-B005 replaces camel source with palm and result
  with fruit. SMD-B007's camel hardship/milk facets do not transfer. The shared
  annual-yield topology survives with changed material.

Backward to line 3, the completed assembly meets `لَمْ يَلِدْ وَلَمْ يُولَدْ`.
The lexical livestock births and products remain actual only in their own
tuples; the surface blocks both active and passive birth roles for its
continuous referent. Thus the assembly sharpens the exact scope of the surface
negations rather than filling them. Recipient of SMD-B007's milk and duration
beyond the named adversity remain typed open.

## 8. The Active/Passive Birth Rotation Under a Double Block

**Mature form:** operational counterfield plus distributed mapping.

The exact surface operation is a coordinated pair:

`لَمْ -> يَلِدْ [IMPF.JUS.ACT.3MS]
-> وَ + لَمْ -> يُولَدْ [IMPF.JUS.PASS.3MS]`.

Both verbs have lemma `وَلَدَ` and root `و ل د`; both refer to the continuing
surface occupant; both events are denied. Voice changes which role is exposed.
In the active clause, the referent is grammatical controller/semantic source
and a child/product position is prevented. In the passive clause, the referent
is affected/passive subject; initiating parent is suppressed by voice and the
event itself is prevented by negation.

### Whole-topology replay through supplied WLD tuples

- WLD-B003 `ولدت المرأة` supplies a positive active realization: mother
  controls, carried child is placed/born, and result occurs. `أولدت` supplies a
  delayed impending boundary; `شاة والد` supplies pregnant incoming state;
  `ولدناها` supplies attendants who oversee but do not become mother; `يوم
  ولدت/يوم ولد` supplies time.
- WLD-B002 supplies father and mother as distinct source roles and `والدان` as
  their dual. Neither can be inserted into the surface vacancies as an actual
  parent.
- WLD-B001 supplies possible product class across one/many, male/female, and
  young/older uses. Under the active surface negation, none is instantiated.
- WLD-B004 changes the product label's parameters: near-birth boy/girl versus
  enslaved-status boy/girl, with an old woman still able to bear `وليدة` in the
  status use. The surface opens no such age or status classification.
- WLD-B005 generalizes the topology: `source thing + cause -> resulting thing`,
  with new speech, non-pure person/language, fabricated book, or generated but
  unverified evidence as products. This creates a scale mapping from biological
  lineage to causal production, not occupant continuity.
- WLD-B006 supplies same-age peer and the formal lineage `ولدة -> لدة` by loss
  of initial `و`. Peerhood matures with the counterpart relation in Formation
  12; it is not a birth participant.

### Vacancy propagation and formal asymmetry

The active product vacancy propagates only through relations that accept a
product from that same denied position: number/gender, parent-child lineage,
birth day, and later age all remain prevented for this event. It does not erase
the lexical source examples. The passive source vacancy remains unknown by
voice; negation additionally denies any actual parent/event. Suppressed is not
the same as nonexistent.

Formal closure adds a second asymmetry. Surface `يُولَدْ/يولد` contains the
explicit base `ولد` after initial `ي` and exactly meets WLD-B001/B004 `حين
يولد`. Surface `يَلِدْ/يلد` contains no exact `ولد`; its relation to the base is
morphological actuality. Thus the clause that suppresses the initiator retains
the base visibly, while the active clause that exposes the controller changes
its internal letter sequence. This is a structural result, not a semantic
identity claim.

Backward effect: passive affected position makes the preceding active source
position perceptible as a role reversal; forward effect: the pair supplies two
different typed vacancies to the later `يكن ... كفوا أحد` vacancy. The mature
counterfield is closed: no child, parent, birth event, or lineage occupant is
created. Its openings are exactly the suppressed passive initiator and the
counterfactual product roles denied by `لم`.

## 9. The Three Renewed Negators and Their Different Vacancies

**Mature form:** structural constellation and maximal vacancy mapping.

The complete positioned negator sequence is:

`112:3:1 لَمْ [bare line opening; governs active يلد]
-> 112:3:3 وَ+لَمْ [internal conjunction; governs passive يولد]
-> 112:4:1 وَ+لَمْ [exact word return at next line opening; governs يكن]`.

The invariant is `لَمْ + jussive imperfect government`; the complement changes
`active production -> passive reception/production -> copular occurrence`.
The first negator begins a line without conjunction. The second renews negation
inside the line and coordinates a voice replay. The third repeats complete
`وَلَمْ` but moves from word 3 to word 1 and carries the negative sequence over
a line boundary. This is an ordered three-carrier operation, not three isolated
particles.

### Exact `لم يكن` collision

Removing only the preceding surface conjunction yields exact surface sequence
`لم يكن`. QWL-B005 contains `تقول باطلا أي قال ما لم يكن`. The complete contact
is:

`surface ولم يكن -> exact embedded لم يكن -> QWL-B005 nonexistent-content /
false-speaking tuple -> surface copular event under negation`.

Its initial payload is exact form and order. The lexical tuple has a speaker
who says what did not occur and a false result. The surface tuple has delayed
`أحد`, predicate `كفوا`, endpoint `ه`, and a denied counterpart relation. No
speaker or false result belongs to the surface event. Maturation makes the
contact protective: it forces explicit separation of non-occurring content
from a false report about that content. Backward to `قُلْ`, the passage
contains both an imperative to say and a later exact phrase that can occur
inside a lexical definition of false saying, yet their source actualities
remain disjoint.

### Complete vacancy mechanism coalition

Every comparable supplied absence was enacted through its own consequence:

| vacancy tuple | what remains present | what is absent/open | consequence |
|---|---|---|---|
| surface `لم يلد` | grammatical controller, WLD schema | event and child/output | no generated product occupant |
| surface `لم يولد` | affected role encoded by verb | birth event; passive initiator also unexpressed | no source/lineage event, unknown actor stays unknown |
| surface `لم يكن له كفوا أحد` | endpoint, predicate schema, exhaustive candidate class | occurrence of any counterpart | terminal no-candidate result under negation |
| AHd-B002 | domain and eligible class | any one or more occupant | exhaustive empty domain |
| QWL-B005 `ما لم أقل` | attribution and claimed content | claimed speaker's performance | false attribution can still occur |
| QWL-B005 `ما لم يكن` | report/content | represented occurrence | false saying can still occur |
| QWL-B012 `لم أظهره` | internal proposition | external performance | content remains available behind boundary |
| SMD-B002 `لا جوف له` | resistant solid | cavity/opening | stopper operation becomes inapplicable |
| SMD-B002 `ليس فيه خدد` | hard ground/material | grooves | uninterrupted surface/resistance |
| KFW-B005 `ليست تامة كفأة` | a yield | completion | partial product, future renewal still possible |
| WLD-B005 `ليست بمحققة` | generated evidence | verification | produced result lacks confirmation |
| ALH-B001 `لا يكون إلاها حتى يكون معبودا` | candidate role and prerequisite schema | role before prior worship | role is conditionally unavailable, not terminally denied |
| KWN-B001 `لا يكون زيدا` | exception construction | ordinary predication reading | grammatical outcome becomes exception in its source |

The mapping exposes six distinct vacancy types: terminal denial; suppressed
unknown actor; absent performance with surviving content; structural absence;
incomplete but renewable result; and conditional role awaiting prerequisite.
No vacancy fills another. The later surface counterpart vacancy sends this
typology backward: the two earlier WLD clauses are not generic repetitions but
different blocked interfaces. The earlier `لم أظهره` variant sends a different
effect forward: a missing output can preserve content, unlike denied birth.

The mature three-negator constellation has no unprocessed carrier. The passive
initiator remains an exactly typed unknown; the command's performance and
receiver remain open elsewhere.

## 10. Occurrence, Station, Obligation, and Remembered State

**Mature form:** structural constellation of asserted derivations plus
projective state succession.

The surface entrance `يَكُن` is a jussive imperfect form of lemma `كَانَ`, root
`ك و ن`, governed by the third `لم`. KWN-B001 supplies occurrence/presence:
`كان الشيء يكون كونا إذا وقع وحضر`, event among people, past or current time,
and caused occurrence `كونه فتكون أحدثه فحدث`. It also supplies grammatical
uses as report, emphasis, and exception. Surface actuality selects only the
negated copular construction fixed by syntax; the other uses are mapping
counterfields.

### Supplied transformations around occurrence

- **Occurrence to place/station.** KWN-B002 explicitly derives `المكان` from
  `كان يكون` and equates `المكانة` with `المنزلة/الموضع`. An event's presence
  supplies a location interface; an occupant can then be established, located
  relative to another (`فلان مني مكان هذا`), or ranked. `موضع العمامة` keeps a
  physical body-site distinct from social station.
- **Occurrence to obligation.** KWN-B003 asserts `الكيانة الكفالة` and
  `كنت على فلان ... تكفلت به`. Controller stands for/over a beneficiary;
  incoming relation is optional/unbound, operation assumes responsibility,
  outgoing state is continuing guarantee. This is a fresh event, not a later
  phase of the surface copula.
- **Occurrence to submission.** KWN-B004's exact equation `الاستكانة الخضوع`
  supplies a participant in a lowered/submissive relation. Authority endpoint
  is open. It can project beneath KWN-B003's guarantor or QWL-B010's controller,
  but no source asserts either pairing.
- **Past state carried by fresh speech.** KWN-B005 labels an old man from his
  repeated `كُنْتُ في شبابي كذا وكذا`. Same referent continues from youth to
  old age; state and time change; QWL-B001 can externalize the recollection,
  QWL-B012 can hold it internally, and QWL-B007 can circulate the label. The
  content is past-state report, not current occurrence.
- **Night-long bad state.** KWN-B006 derives `كينة` from `الكون` and places a
  person through one night `بحال سوء`. Occupant, duration, and adverse state
  are fixed; condition after the night remains open.
- **Caused occurrence.** KWN-B001 `كونه فتكون` fills causer, caused item/event,
  transition, and resulting presence. WLD-B005 can project source material and
  product; QWL-B001 can project a new spoken token. The resulting assembly is
  completed in Formation 16.

The structural recognition is that the supplied root field does not give one
generic "being." It supplies exact transformations among occurrence in time,
place/station, responsibility, submission, remembered former state, and a
condition endured through a night. Their controllers differ: event causer,
located occupant, guarantor, submissive participant, aged reporter, and person
in bad condition.

Backward from surface `يكن`, the positive occurrence and causation tuples make
the effect of `لم` exact: what is denied is the counterpart relation's
occurrence, not place, guarantee, submission, memory, or bad condition. Forward
to delayed `أحد`, grammatical subject remains the candidate; the central
referent stays endpoint in `له`. The surface occurrence vacancy is terminal in
scope, while the aged man's later state, night aftermath, and submission
authority remain typed open source-local ports.

## 11. The Counterpart Search That Returns No Candidate

**Mature form:** operational configuration, complete topology mapping, and
controller/polarity counterfield.

The surface tuple is exact and ordered:

`وَلَمْ -> يَكُن [copular governor, agrees with delayed 3MS subject]
-> لَّهُ [comparison endpoint, pronoun corefers with the named referent]
-> كُفُوًا [indefinite accusative predicate]
-> أَحَدٌ [indefinite nominative delayed subject; exhaustive under AHd-B002]`.

Its semantic roles, without rearranging word positions, are:

`candidate class أحد -> counterpart relation كفوا -> endpoint ه -> occurrence
denied`.

KFW-B001 supplies the positive relation in complete source assertions:
`الكفء المثل`, `التكافؤ التساوي`, `هذا كفء له`, `الكفئ النظير`, and `كل شيء
ساوى شيئا حتى يكون مثله فهو مكافئ له`. This yields a maximal
`LATENT_MAPPING`:

| parameter | KFW-B001 positive tuples | surface tuple |
|---|---|---|
| first comparand | `هذا/شيء/فلان/رجل/فارس` in local examples | delayed exhaustive `أحد` candidate class |
| relation | `كفء/مثل/نظير/مكافئ` | accusative `كفوا` |
| endpoint | `له/لفلان/الرجل/فارس` | coreferential `له` |
| measure | lineage, wealth, war, marriage, equal action, pairing | no domain specified beyond counterpart relation |
| polarity | equality/peer relation asserted locally | occurrence denied by `لم` |
| order/grammar | nominal or verbal positive constructions | endpoint and predicate advanced; subject delayed |
| result | equality, pairing, reciprocal return, or warding | no actual counterpart occupant |

The source domains never enter the passage as actuality. They fully inhabit
the positive counterfield:

- Lineage, wealth, marriage, and war each provide a measure under which two
  local persons can be peers. Changing the measure can change result without
  changing occupants.
- `كافأت الرجل أي فعلت به مثل ما فعل بي` supplies first act and like-for-like
  return. One return to the prior relational state records a reciprocity cycle
  and stops.
- `كافأ الرجل بين فارسين برمحه` supplies two horsemen and spear as mediating
  instrument. It establishes pairing/position; strike is not asserted.
- `نكافىء بهما عنا عين الشمس` supplies an unspecified dual medium, incoming
  relation from `عين الشمس`, protected endpoint, and warded result.

### Every competing candidate relation

- **Exhaustive negative candidate.** AHd-B002 fills candidate schema from one
  upward. Search traverses the eligible class and terminates with no occupant.
  This is the direct mature surface operation.
- **Same-age peer.** WLD-B006 supplies two people and age-at-birth as measure:
  `لدة الرجل تربه`. Positive peer result is actual only in that lexical tuple.
  When projected beside the surface, it proves that a peer under one supplied
  measure would still be a fresh candidate, not an exception to the negation.
- **Equality removed by inversion.** KFW-B002 explicitly defines overturning
  as `إزالة المساواة`. Begin at a KFW-B001 equal state; apply inclination/
  inversion; outgoing state is unequal orientation. A restoring operation is
  not supplied, so return remains open.
- **Poetic ending comparison.** KFW-B003 changes comparands from persons/things
  to positioned rhymes and measure to letter/movement/inflection. It matures
  independently in Formation 13.
- **Sewn pieces.** KFW-B004 supplies one or two pieces attached to cover a
  house rear. Two pieces are complementary partners, not asserted equals. The
  completed role mismatch prevents mere pairing from becoming `كفء`.
- **Alternating cohorts.** KFW-B005 supplies two herd groups related by
  succession across years. They correspond by phase, not simultaneous
  equality. Result is renewal, not counterpart identity.
- **One joined to tens.** AHd-B003 creates a larger number by composition.
  Unit and tens are operands, not peers; completed result is eleven or
  unit-plus-tens expression.

The operation's force is vacancy, not a hidden candidate. No projection is
allowed to fill the surface subject position because `لم` denies occurrence
and AHd-B002 exhausts the class. Backward, final `أحد` reopens initial
affirmative `أحد`; `له` returns to the central referent; positive KFW tuples
make the denied relation maximally specified. Forward, the endpoint's previous
active/passive role rotation clarifies that being source or product is
different from being counterpart. The mature formation has no open candidate;
only the measure by which `كفوا` could have been tested is unsupplied.

## 12. Force Through Stick, Word, Spear, Tilt, and Resistance

**Mature form:** exact topology mapping plus projective controller counterfield.

The smallest exact force mapping joins two complete lexical operations:

`SMD-B006 صمده بالعصا صمدا إذا ضربه بها`

and

`QWL-B008 القال الخشبة التي تضرب بها القلة`.

Both instantiate:

`unknown/controller -> stick or wooden instrument -> transmitted strike force
-> affected target -> impact`.

The SMD target is a pronominal other and instrument `العصا`; the QWL target is
`القلة` and instrument `القال/الخشبة`. Controllers remain unknown in both.
This is `LATENT_MAPPING` by complete force topology; no occupant crosses between
events. It also explains why QWL-B008 could not remain inside Formation 1's
content relay: its root-relative form performs as material instrument.

### Force replay under every supplied controller and result

- **Spear mediation.** KFW-B001 supplies a man placing a spear between two
  horsemen. Without added force, result is pairing/position. Project SMD-B006's
  strike through the spear and direction branches toward either horseman;
  impact becomes latent, never source actual in KFW-B001. The alternative
  target assignments remain separate.
- **Inclination and inversion.** KFW-B002 replaces impulse-to-impact with
  force-to-orientation. Controller inclines a thing or bow/plate, tips a bowl or
  vessel, or diverts a group. Result is tilted, overturned, or redirected
  state. When a bottle from SMD-B003 fills affected container, stopper state
  decides whether contents remain retained or acquire an outward spill port.
- **Self-swaying/ambient-force variant.** KFW-B002's woman in her gait or
  ship-like swaying supplies no explicit external controller. Orientation
  alternates; one return records a cycle. Force source remains unknown rather
  than being borrowed from the striker.
- **Face-state aftermath.** `مكفأ الوجه كاسف اللون` changes affected region and
  outcome from orientation to broken/dim face-color state. No instrument is
  supplied. This is a completed compromised-state branch.
- **Effective word as nonmaterial instrument.** QWL-B004 supplies title-holder,
  word, and `ينفذ قوله`; QWL-B010 supplies controller over another. Projected
  content travels through a receiver and changes agency/relation rather than
  producing bodily impact. Consent stays unknown. This variant sends force
  analysis backward into `قُلْ`: a word may have consequence, but no such
  controller/outcome is surface actual.
- **Warding counterforce.** KFW-B001 `نكافىء بهما عنا عين الشمس` supplies an
  unspecified dual medium and force/relation directed away from a protected
  endpoint. Result is deflection/warding, the opposite of transmitting a
  strike into a target.
- **Material resistance.** SMD-B002 supplies hard, compact material, anchored
  rock, and severe ground; SMD-B007 supplies endurance through cold and
  barrenness. When either fills affected role, incoming force/condition meets
  resistance and outgoing state can remain. This is projective unless the
  source tuple itself already asserts persistence.
- **Boundary compromise.** KFW-B004's sewn covering can receive KFW-B002
  inversion force. Result is changed orientation of shelter/cover; tearing or
  restoration is not supplied and remains open.

The mature formation differentiates force source, medium, direction, consent,
affected participant, resistance, and result. It contains one exact
stick-strike mapping and several projective variants. Backward effect on line 2
is to keep SMD-B006's force affordance distinct from SMD-B001 reliance and
SMD-B002 solidity. Backward effect on line 1 is to separate QWL-B008 instrument
from QWL-B001 content. Unknown actors remain unknown; no restoration event is
invented.

## 13. The Four `د` Boundaries With Changed Grammatical Work

**Mature form:** structural constellation and formal-pattern mapping.

The complete line-ending vector is:

`[112:1 أَحَدٌ, 112:2 ٱلصَّمَدُ, 112:3 يُولَدْ, 112:4 أَحَدٌ]`.

Every term ends in written consonant `د`; the root-bearing end sequence is
`[ء ح د, ص م د, و ل د, ء ح د]`. First and fourth complete words recur exactly.
The full grammatical parameter vector is:

| ordinal | carrier | final marking | definiteness | voice/category | local work |
|---:|---|---|---|---|---|
| 1 | `أَحَدٌ` | nominative tanwin | indefinite | noun | affirmative predicate |
| 2 | `ٱلصَّمَدُ` | nominative damma | definite | noun | affirmative predicate |
| 3 | `يُولَدْ` | jussive sukun | verbal | passive verb | second denied event |
| 4 | `أَحَدٌ` | nominative tanwin | indefinite | noun | delayed subject under negation |

KFW-B003 explicitly defines comparison across complete poetic ending
distributions: one rhyme may be raised and another lowered; letters may differ
`ميم/نون`; movements or inflection may differ. Its interface makes every
surface parameter visible. The supplied passage does **not** instantiate the
source's consonant-discord example: all four closing consonants are `د`.
Instead it instantiates consonantal recurrence with changes of definiteness,
case/mood, category, voice, role, and polarity.

Three complete pattern operations coexist:

1. **Consonantal invariant:** `[د,د,د,د]` persists while carriers change.
2. **Grammatical differentiation:** noun predicate, noun predicate, passive
   verb, delayed noun subject prevent acoustic recurrence from averaging work.
3. **Exact enclosure:** first/fourth `أحد` make a word-level ring; middle
   boundaries `الصمد/يولد` change root and category.

Backward from final `د`, the passive verb's jussive boundary makes the two
nominal damma/tanwin boundaries newly audible as grammar; final `أحد` returns
the first complete form with different role. Forward from first `د`, the
sequence passes predicate -> predicate -> event -> candidate. The mature
constellation is passage-wide and has no omitted boundary. KFW-B003's possible
letter discord remains a source counterfield, not an unrealized need in the
surface.

## 14. The Whole-Unit Count and Grouping Machine

**Mature form:** formal structural constellation.

Only complete supplied units enter. The passage has four lines and these
positioned vectors:

- word tokens: `[4,2,4,5]`;
- clause/event carriers: `[2,1,2,1]`;
- morphological carriers: `[4,3,5,7]`;
- syntax edges: `[3,1,3,4]`;
- root-bearing stems: `[3,2,2,3]`;
- normalized letter totals: `[11,9,12,15]`.

### Every distinct full-vector operation

- **Word-count return with changed operation.** Lines 1 and 3 each have four
  word tokens. Line 1 partitions `1 command + 3 quoted nominal carriers`; line
  3 partitions `2 + 2` coordinated negative clauses. Equal count therefore
  carries a grouping transformation, not symmetry of syntax.
- **Clause alternation.** `[2,1,2,1]` alternates paired/single units. The first
  pair nests command and quote; the second pair coordinates active/passive
  denials. The single units differ: direct nominal predication in line 2,
  copular negative clause with delayed subject in line 4.
- **Edge-vector broken return.** Syntax edges `[3,1,3,4]` echo the first three
  clause densities but line 4 expands to four attachments: government,
  prepositional complement, copular predicate, delayed subject. The final
  single clause is structurally densest.
- **Morphological expansion.** `[4,3,5,7]` first contracts, then grows by two
  and two. Line 2 splits determiner/stem; line 3 splits conjunction/negator;
  line 4 retains that split and adds preposition/pronoun. Word count and
  morpheme count diverge because formal relations are packed into fewer words.
- **Root-bearing enclosure.** `[3,2,2,3]` returns to three. Line 1 has QWL,
  ALH, AHd roots; line 4 has KWN, KFW, AHd. The shared final AHd root occurs in
  different syntax; the other two root roles transform command/name into
  occurrence/counterpart.
- **Letter-scale expansion.** `[11,9,12,15]` contracts then increases by three
  and three. This sequence remains distinct from word/morpheme operations; no
  arbitrary subset is used to force equivalence.
- **Opening-category progression.** Complete openings are
  `imperative -> proper name -> negator -> conjunction+negator`. The last
  carries prior negative syntax across a boundary.
- **Closing-category progression.** Complete closes are
  `noun -> noun -> verb -> noun`, all on `د`, with first/final exact word
  enclosure.
- **Recurrence distribution.** `الله` moves from line-1 word 3 to line-2 word
  1; `أحد` remains at line ends 1 and 4 but changes ordinal `4 -> 5`; `ولم`
  moves from line-3 word 3 to line-4 word 1; `لم` distributes bare/internal/
  cross-line as Formation 9 specifies.

AHd-B001/B003/B004/B005 supplies unit, firstness, composition, and individual
distribution interfaces. KFW-B004 supplies one/two complementary pieces;
KFW-B005 supplies two alternating cohorts; WLD-B001/B002/B006 supplies
singular/plural/dual and peer counts. Every interface was tried. Their results
remain respective composition, covering, production alternation, offspring
number, parent dual, and age-peer relation; none authorizes a numerical
interpretation of the passage vectors.

Backward effect: final morphological density exposes how earlier compact words
carry fewer explicit relation morphemes; final five-word delay makes the first
four-word quote's predicate-at-end order perceptible. Forward effect: the
initial command/quote grouping becomes the first phase of an alternating
clause architecture. The complete vector operations are mature and remain
parallel rather than consuming one another.

## 15. Terrain, Shelter, Rank, and Responsibility as Different Positions

**Mature form:** projective place/station assembly with controller distinctions.

The independent resurrection sweep began with AHd-B006 `أحد جبل بالمدينة`,
which had no direct role in the Ahad enclosure beyond exact form. It opened a
concrete place/elevation role. The whole field then supplied:

- SMD-B002: hard place, elevated/thick place, anchored rock, severe ground,
  compact material without cavity or grooves;
- KWN-B002: `المكان/المكانة/المنزلة/الموضع`, relative position, establishment,
  and `موضع العمامة`;
- SMD-B005: controller overlooking and attending to an affair;
- SMD-B001: sought house/endpoint;
- KFW-B004: house/tent rear covered by one/two sewn pieces;
- QWL-B004: ruler below the greatest king, with a word that takes effect;
- KWN-B003: guarantor standing for/over another;
- KWN-B004: submissive participant under an open authority.

These sources do not share occupants or one invariant actuality. They mature
as differentiated `LATENT_ASSEMBLY` variants around canonical roles
`occupant -> site/station -> vertical or social measure -> boundary ->
controller relation -> outgoing located/protected/ranked/obligated state`.

### Completed place/station variants

- **Material terrain.** Mountain fills large located occupant; SMD hard/elevated
  place or anchored rock fills material/vertical measure; KWN-B002 fills
  location. Result is an elevated resistant positioned mass. Mountain and rock
  remain alternative fillers; neither is the line-2 predicate's occupant.
- **Severe ground without grooves.** SMD-B002 fills ground, resistance, and
  uninterrupted surface. KWN-B002 supplies a located traveler only
  projectively. Route across the ground is possible but destination remains
  open. This variant differs from mountain by scale and mobility.
- **Sought shelter.** SMD-B001 supplies a house as intended endpoint; KFW-B004
  supplies maker, one/two pieces, joining, rear boundary, and cover; KWN-B002
  supplies establishment in place. Result is a located covered shelter reached
  by a seeker. Interior occupant remains open.
- **Body-site.** KWN-B002 supplies `موضع العمامة`; SMD-B004 supplies head and
  wrapping operation while excluding turban as medium. Result is a wrapped
  head at the turban's site using cloth, kerchief, or garment. Site identity
  does not defeat medium exclusion.
- **Overlooking station.** SMD-B005 supplies controller above/an affair and
  serious attention. KWN-B002 supplies relative position. Result is maintained
  oversight, not terrain or rank.
- **Social rank.** KWN-B002 changes measure from vertical location to
  `مكانة/منزلة`; QWL-B004 supplies a ruler below the greatest king. Result is
  ordered rank plus effective word. No physical height is asserted.
- **Obligational station.** KWN-B003 supplies guarantor over/for beneficiary;
  position becomes responsibility. Result is continuing liability/care.
  SMD-B005 can project attentive performance, but the guarantor and overseer
  remain different source controllers.
- **Submissive relation.** KWN-B004 fills participant and lowered relation;
  QWL-B010 or QWL-B004 may project authority endpoint. Result is submission
  under a controller. Consent and identity of authority stay open.

The mature assembly prevents a broad vertical metaphor from erasing exact
operations. Elevation can be material, oversight can be spatial-attentional,
rank can be social, `على` can mark obligation, and submission can orient a
participant toward an authority. Backward to line 2, SMD's exact predicate
retains all these affordances without asserting the mountain, house, ruler, or
guarantor. Forward to line 4, KFW counterpart relation tests peerhood, not
height, station, or obligation.

## 16. One Source-to-Result Topology Across Birth, Speech, Event, and Yield

**Mature form:** maximal distributed mapping plus projected cross-scale
assemblies.

The common interface is precise:

`source/controller + incoming material/content/state -> operation through a
medium/cause and boundary -> new result occurrence -> aftermath/verification/
recipient`.

It is instantiated by complete local tuples whose materials do not merge:

| tuple | source/controller | operation/medium | result | aftermath |
|---|---|---|---|---|
| QWL-B012 -> B001/B002 | psyche/speaker + composed content | tongue/pronunciation crossing | external spoken token | receiver or later carrier open |
| WLD-B003 | mother + carried child | birth/placement at a time | born child | day, attendant, age/number roles |
| WLD-B005 | source thing + cause | causal generation | resulting thing, new speech, mixed person/language, fabricated book, generated evidence | novelty, non-purity, fabrication, or absent verification |
| KWN-B001 | causer | `كونه/أحدثه` | event occurs and is present | reported past/current state |
| KFW-B005 | palm/camel owner and annual source | production/giving during year | fruit, milk, wool, offspring | recipient; full, incomplete, or alternating yield |
| SMD-B007 | enduring camel | persistence through cold/barrenness | continued milk sending | ongoing survival/product |

### Every controller, medium, and outcome transformation

- **Expression.** Incoming content already exists; operation changes carrier,
  not proposition. With QWL-B012 `لم أظهره`, result port stays empty while
  source content remains. With QWL-B001, a fresh token occurs.
- **Birth.** Incoming carried state becomes separately born participant;
  mother is semantic initiator; attendants support; day marks boundary. Surface
  `لم يلد/لم يولد` supplies denied active and passive counterfields rather than
  positive phases.
- **General generation.** WLD-B005 removes the biological participant
  requirement and retains cause-to-result. Product aftermath branches:
  `المولد من الكلام` is newly coined; `عربية مولدة/رجل مولد` is non-pure or
  environment-formed; `كتاب مولد` is fabricated; `بينة مولدة` exists but is
  `ليست بمحققة`. Novelty, falsehood, purity, and verification are orthogonal.
- **Caused event.** KWN-B001 produces occurrence rather than a material product.
  Result is presence. Surface `لم يكن` blocks precisely the counterpart event,
  not all possible generation.
- **Seasonal production.** Annual source yields a bundle to a beneficiary.
  Completion can fail while some product exists; alternate cohorts can renew
  output. This differs from one irreversible birth boundary.
- **Persistence-mediated output.** SMD-B007 maintains carrier and output under
  adversity. Source does not transform into product; one carrier continues to
  send it. This changes the topology from source-consumed/source-separated to
  durable producer.
- **False report as result about no result.** QWL-B005 can generate a spoken
  token whose represented event did not occur. The speech result exists while
  its content's referent event does not. This orthogonal branching is why exact
  `لم يكن` contact cannot merge occurrence and report.

### Cross-scale assemblies

- Internal proposition [QWL-B012] + vocal medium [QWL-B001/B002] + new-word
  classification [WLD-B005] yields a newly coined spoken token. QWL-B005 can
  independently add false-attribution outcome; novelty does not entail it.
- Mother/birth [WLD-B003] + year [KFW-B005] + persistent herd [SMD-B007]
  yields the livestock production cycle already matured in Formation 7.
- Causer/event [KWN-B001] + generated evidence [WLD-B005] yields an occurring
  evidentiary artifact whose verification remains absent. No verifier is
  supplied.
- Exact quote-content [surface/AHd-B001] + fresh QWL performance + social
  circulation [QWL-B007] yields token succession while proposition remains
  stable. Each carrier is new.

Backward effect on the passage: line 3 blocks both source and product positions
in the biological schema; line 4 blocks occurrence of the counterpart schema;
line 1 opens but does not assert fresh performance. The formation therefore
turns the three surface areas into three different controls on result creation:
commanded output open, birth outputs denied, counterpart occurrence denied.
Receivers, passive initiator, verifier, and later artifact fate remain typed
open.

## 17. Day, Night, Year, Age, and Open-Ended Continuance

**Mature form:** distributed temporal mapping and projective state succession.

The independent resurrection sweep brings together only complete supplied time
measures:

- AHd-B004 `يوم الأحد أي يوم الأول`: named day and firstness;
- WLD-B003 `يوم ولدت/يوم ولد` and impending `حان ولادها`: birth boundary and
  delayed event;
- WLD-B006 same-age peer: two persons measured from birth;
- KFW-B005 one-year yield and alternating herd phases;
- KWN-B001 past or current occurrence;
- KWN-B005 old man's current label from `كنت في شبابي` past self-report;
- KWN-B006 one night in a bad state;
- SMD-B007 continuance through cold/barrenness and after others' annihilation.

The invariant is `occupant/state/event -> explicit temporal index -> transition
or endurance -> outgoing state`. The scales and consequences differ.

### Completed trajectories

- **First named day.** AHd-B004 gives a calendar/day-name actuality and first
  ordinal. It can fill a sequence beginning but supplies no following day.
- **Impending-to-completed birth day.** WLD-B003 begins with pregnant/near-time
  state; `حان` delays product until boundary; active or passive birth completes
  transition on a day. In the surface WLD pair, negation prevents this path.
- **Coeval comparison.** WLD-B006 uses birth time as measure between two
  people. Result is peer relation, not a shared event.
- **Annual recurrence.** KFW-B005 produces through one year; two cohorts
  alternate; one return closes a cycle. An incomplete yield leaves renewal
  possible rather than ending time.
- **Youth-to-age with content return.** KWN-B005 preserves one referent across
  youthful past and aged present; repeated `كنت` report carries former state
  into new speech. QWL-B012/B001 branches memory held versus expressed.
- **Bad night.** KWN-B006 holds one occupant in an adverse condition across a
  night. State after the boundary is not supplied and remains open.
- **Open continuance.** SMD-B007 has no fixed terminal measure: carrier remains
  through adversity or after another population's extinction. In the camel
  tuple, continued milk gives endurance an outward consequence.

The temporal formation does not turn these scales into one chronology. Its
recognition is the controller/state transformation at each scale: day begins
or bounds an event; year renews product; age reclassifies one referent; night
contains a condition; endurance crosses a threatened endpoint. Backward from
surface negated imperfects, positive source times specify exactly which
transitions are not asserted. Forward from AHd firstness, final `أحد` is not
made a calendar return. Open remainders are post-night state, terminal point of
endurance, and the next member after the first day.

## 18. The Released-Base and Affix Return Network

**Mature form:** structural constellation at exact-form fixed point.

Every supplied/recovered form is kept with its exact written differences.
The network has several independent transformation lineages:

1. **QWL imperative lineage.** Morphology binds surface `قُلْ/قل` to lemma
   `قَالَ/قال` and root `ق و ل`; literal containment does not join `قل` to
   `قال` because of medial alif. Exact `قل` occurs in QWL-B005 `أقل` after
   initial `أ`, QWL-B011 `قلت` before final `ت`, QWL-B008 `القلة` internally,
   and the AHd-B001 whole quote. Each contact reactivates its full unsaid,
   supposition, strike-instrument, or exact-content tuple without transferring
   semantics.
2. **AHd base relations.** AHd-B001 asserts `أحد` as branch with origin `وحد`,
   and asserts `أحد بمعنى الواحد`. Under base-letter detection `احد` occurs in
   `واحد` after `و`; written `أ/ا` difference remains recorded. Exact written
   `أحد` returns at both passage ends and in its longer exact source forms
   (`أحدهن`, `أحدكما`, `الأحد`, `استأحد`).
3. **Name formation.** ALH-B002 explicitly performs
   `إله -> hamza deletion + alif-lam insertion -> الله`; the passage repeats
   result twice. Terminal `له` then returns as independent surface
   preposition+pronoun, as Formation 4 specifies.
4. **SMD base and medial-alif differentiation.** Released `صمد` occurs in the
   surface stem and in `صمدته/يصمده/المصمد/صمد رأسه/صمده`. `صماد/صمادة/مصماد`
   retain medial alif and do not become exact `صمد`; their functional relations
   join through source tuples, not fabricated containment.
5. **WLD voice and deletion lineages.** Morphology binds lemma `ولد` to `يلد`
   and `يولد`; only passive contains base exactly. WLD-B006 separately asserts
   `ولدة -> loss of initial و -> لدة`. Restoring `ولدة` re-enters the contact
   field and meets other `ولد` forms without making the age-peer event a birth
   event.
6. **KWN inflection and derivation.** Morphology binds `كان` to jussive `يكن`;
   source `يكون` retains medial `و` and does not exactly contain `يكن`.
   QWL-B005 supplies exact `لم يكن`. KWN-B006 separately derives `كينة` from
   `الكون`; KWN-B002 derives `المكان` from `كان يكون`.
7. **KFW surface/lexical spellings.** Morphology releases lemma `كفو` from
   accusative `كفوا`; final inflection differentiates them. Root `ك ف ء`
   connects lexical `كفء/كفئ/كفاءة/كفاء/كفأة/تكفؤ`, but waw and hamza-bearing
   forms remain visibly different and no exact substring is invented.

### Short-form closure

The complete supplied morphemes `و`, `ل`, `ه`, and `ال` were compared against
every containing surface and lexical form. Canonical family results are:

- `و` at the two conjunction positions and in the displayed QWL/KWN/WLD/KFW/
  AHd forms carries exact letter/offset only; an internal waw is never made a
  conjunction.
- `ل` at surface preposition meets `لم`, `الله`, `الصمد`, WLD forms, and the
  lexical records; prepositional government belongs only to 112:4:3.
- `ه` at surface suffix meets `هو`, `الله`, `إله`, `أحدهن`, `أحدكما`,
  `فعلته`, `أظهره`, `ولدها`, and other recorded forms; only the suffix carries
  coreference.
- `ال` at the SMD determiner meets surface `الله/الصمد` and every displayed
  definite lexical form; only ALH-B002 separately asserts insertion of
  `الألف واللام` as a name-forming act.

Each family is canonicalized by short form, containing form, offset, written
delta, surface position, and source tuple. A second comparison pass over
`وحد/إله/صمد/ولد/كفو` and the morphology lemmas yields only the contacts above.
Interface-output re-entry adds no new form, and the least-complete seeds
produce no new typed contact. Exact-contact fixed point is reached.

The mature recognition is not that deletion/addition has one meaning. It is
that surface relations remain linked to explicit base transformations while
written loss, gain, inflection, and containment are individually recoverable.
Backward reading from final `أحد` and `له` activates base/name circuits near
the opening; from `يولد` it activates the retained WLD base; from `يكن` it
activates an exact source phrase outside the KWN root field. No open formal
contact remains.

## 19. Interaction After Precision

The mature formations now interact through explicit relations. None is
dissolved into a shared topic.

### Commanded Content Relay -> Directed Need

Formation 1 produces an external-token interface; Formation 5 accepts carried
material on a route toward an endpoint. Their join is `LATENT_ASSEMBLY`:

`exact quote proposition -> fresh spoken carrier -> affair/need role ->
directed reliance route -> relied-upon receiver -> response open`.

The relay gains direction and an endpoint; the route gains concrete carried
content. Backward, `قُلْ` becomes capable of launching the assembly. Forward,
`الصمد` becomes capable of receiving directed approach. No identity or surface
syntax asserts that this particular quote is a need, so the projective status
remains explicit.

### Referent Rotation -> Counterpart Search

Formation 2 delivers the same referent into surface suffix `ه`; Formation 11
accepts that suffix as comparison endpoint. This join is `SOURCE_ACTUALITY`
surface binding, not projection. It produces:

`continuous referent -> endpoint له` beside
`different delayed subject أحد -> predicate كفوا -> denied occurrence`.

The interaction changes both formations. The referent path now terminates in a
relation rather than only another pronominal form. The counterpart search now
has an exact endpoint while candidate remains exhaustively absent. Backward,
line 4 clarifies why `يكن` agreement belongs to `أحد`, not the coreferential
referent.

### Ahad Enclosure -> Counterpart Search

The final `أحد` is simultaneously the closing carrier of Formation 3 and the
delayed candidate of Formation 11. This is one `SOURCE_ACTUALITY` token with
two exact structural functions. AHd-B002 supplies exhaustive negative range;
KFW-B001 supplies counterpart topology. Their maximal `LATENT_MAPPING` yields
the no-candidate operation. Backward, that operation transforms the opening
`أحد` into the positive pole of an exact form/polarity ring. Forward, the ring
acquires a relational close rather than a bare echo.

### Boundary Assembly -> Vacancy Coalition

Formation 6 emits four vacancy types: no cavity, unstopped opening, internal
content without expression, and undefined conceptual boundary. Formation 9
compares them with denied event, suppressed initiator, no counterpart,
incomplete yield, and unverified result. The join is `LATENT_MAPPING` by typed
vacancy consequence. It revises the boundary assembly: a closed boundary is
not automatically an absence, and absence of interior is not a blocked event.
It revises the negator formation: `لم` does not behave like material solidity
or incomplete production.

### Enduring Production -> Active/Passive Block

Formation 7 produces a concrete mother/herd/product/year architecture;
Formation 8 exposes the same broad source/product ports under active/passive
surface denial. Their topology map preserves every delta:

`positive livestock source -> birth/yield -> products -> recipient`

versus

`surface referent as active source, event denied` and
`surface referent as passive product, source suppressed and event denied`.

The mature herd assembly makes the negated roles concrete without populating
them. The surface counterfield makes clear that SMD-B007 persistence and milk
remain lexical/projective, not surface lineage.

### Force -> Applied Boundary

Formation 12 emits a force/path interface; Formation 6 accepts force at vessel,
wrap, shelter, or solid boundary. Variants are fully differentiated:

- tipped stoppered bottle: orientation changes, retained-content route resists;
- tipped open bottle: outward spill port appears;
- force against cavityless solid: resistance without interior consequence;
- force against sewn shelter boundary: orientation/cover compromised, repair
  open;
- effective word against a conceptual boundary: definition may be challenged,
  but no revision outcome is supplied.

The boundary formation gains dynamic compromise; the force formation gains
resistance and medium-dependent aftermath. No tearing, spilling content, or
restoration occupant is invented.

### Ending Constellation -> Whole-Unit Machine

Formation 13 supplies the exact closing carrier at each line; Formation 14
supplies the complete line/clause/word/morpheme/edge positions. This is a formal
`LATENT_MAPPING`: each `د` boundary is indexed inside all full vectors. The
result is not a detached rhyme but a sequence in which boundary role transforms
with unit density: line-1 quote predicate, compressed line-2 predicate,
line-3 passive denial, dense line-4 delayed subject. Backward, the count vectors
gain acoustic/grammatical closure; the ending constellation gains complete
internal line structure.

### Released Forms -> Name and Referent Paths

Formation 18 supplies exact formal contacts; Formations 2 and 4 supply
actuality/coreference. Their interaction preserves three statuses:

- `إله -> الله` is asserted lexical transformation;
- `الله` containing terminal `له` is formal contact;
- surface `له -> ه -> earlier referent` is grammatical/coreferential actuality.

The completed chain prevents the attractive form circuit from impersonating
semantic identity. It also sends final `له` backward to the two name positions
and lets their actual referent continuity deepen a previously form-only
contact.

### Place/Rank -> Counterpart Measure

Formation 15 supplies material height, social station, ruler rank,
responsibility, and submission. Formation 11 supplies peer/equality domains.
Each station can provisionally fill a comparison measure, producing variants
`same place`, `same rank`, `same obligation`, or `same vertical level`.
KFW-B001 actually licenses lineage/wealth/war/marriage measures, not these new
ones, so the variants are `LATENT_ASSEMBLY`, not topology mappings. Their
completed result shows that sharing position would not by itself establish
the surface's denied `كفوا` relation.

### Result Production -> Three Surface Controls

Formation 16 sends result interfaces into the exact passage operations:

- line 1: content is present and performance is commanded, but performer,
  occurrence, receiver, and aftermath remain open;
- line 3 active: source/controller is exposed but product event is denied;
- line 3 passive: affected/product role is exposed but source is suppressed and
  event denied;
- line 4: candidate, relation, and endpoint schemas are exposed but occurrence
  is denied.

This distributed interaction is a controller/vacancy map, not a claim that the
passage tells one production story. It changes Formation 16 by showing four
ways an output relation can be open or blocked; it changes the surface sequence
by making the different role controls explicit.

### Time -> Surface Order

Formation 17 supplies day, birth boundary, year, age, night, and endurance;
Formation 14 supplies four ordered lines. Projecting temporal measures onto
line order yields several complete variants, but no source interface identifies
a line as a calendar day/year/life phase. The enacted mismatch is positive:
passage ordinality is exact positional order, while lexical time carriers
remain independent. Only KWN morphology contributes tense/aspect/state directly
to surface grammar.

## 20. Independent Resurrection and Triple-Frontier Fixed Point

After the first mature operations formed, their organizing names were set
aside and the field was restarted from the least-connected source facets.
This produced, rather than merely catalogued, the following mature returns:

- AHd-B006 mountain reopened place and became Formation 15's material-location
  variant; it did not disappear under numerical `أحد`.
- AHd-B004 day/firstness, KWN-B005 aged self-report, and KWN-B006 bad night
  reopened time and became Formation 17.
- SMD-B004's excluded turban forced a site/medium distinction in Formations 6
  and 15.
- SMD-B006 stick and QWL-B008 wooden implement formed the exact force topology
  in Formation 12.
- QWL-B011's supposition grammar, B014's basin indication, B015's care, and
  B016's definition prevented Formation 1 from collapsing every `قول` carrier
  into vocal speech.
- KFW-B003 rhyme discord reopened the complete four-line boundary sequence and
  became Formation 13.
- KFW-B004 one/two shelter pieces and B005 incomplete/alternating yield became
  material-boundary and periodic-production operations rather than generic
  pairing.
- WLD-B004's old `وليدة` status use preserved age/status separation; WLD-B005's
  generated but unverified evidence preserved production/verification
  orthogonality in Formation 16.
- KWN-B003 guarantee and B004 submission reopened the place field as obligation
  and authority relation, producing Formation 15's nonspatial stations.
- The short explicit forms `و/ل/ه/ال`, initially slight, reopened the exact
  contact field and completed Formation 18 without semantic leakage.

### Triple-frontier closure

**Actuality re-entry.** Every surface token, morpheme, syntax edge, coreferential
station, lexical source operation, explicit form equation, derivation, result,
negation, exclusion, and vacancy has a recoverable source node inside a mature
formation. Recomparison adds no new occupant identity. In particular, `يكن`
does not join the coreferential path, lexical example occupants do not enter
surface events, and projected livestock/terrain/shelter participants remain
projected.

**Mapping re-entry.** Every mature output was compared against all typed input
interfaces. New joins yielded content relay, directed route, source/product
mapping, force topology, peer/counterpart mapping, vacancy coalition, time
scale, formal transformations, ending distribution, and whole-unit vectors.
A further pass produces only existing canonical states or a first return cycle;
it adds no longer wrapper.

**Assembly re-entry.** Least-complete roles were replayed through every supplied
filler. Distinct results remain branched: cavityless/stoppered/wrapped/covered;
positive peer/no candidate/equality removed/complementary pieces/alternating
cohorts; spoken/adopted/indicated/defined content; persistent/full/incomplete
production; material/social/obligational/submissive station; impact/diversion/
control/warding/resistance. A further role pass exposes no new role type.

The reciprocal speech exchange, like-for-like action, swaying orientation,
annual cohort alternation, and social circulation each record one cycle and
terminate. Repeated resemblance creates no new state. Fixed point is reached
for the supplied field while the following typed openings remain honestly
open.

## 21. Typed Open Remainders

These are completed movements with an acquired unfilled role. They are not
additional mature-form categories.

| formation | completed movement | exact open port |
|---|---|---|
| Commanded Content Relay | command binds exact whole quote and opens a possible fresh performance | identity beyond grammatical `2MS`, whether performance occurs, receiver, response |
| Referent Stations | one referent reaches suffix endpoint through name and verbal roles | passive initiator remains suppressed; command controller is outside path |
| Directed Need | seeker/need travels or orients toward relied-upon target | target response/aftermath |
| Boundary Assembly | closure, wrapping, covering, expression, and definition variants complete | bottle content, later opener, external receiver |
| Enduring Production | carrier survives and sends/renews product | SMD camel's exact recipient and terminal duration |
| WLD Voice Block | active product and passive event are denied | counterfactual product class; passive initiator unknown, not replaceable |
| Three Negators | each governed complement reaches its own vacancy consequence | only the passive unknown actor; other denials are terminal in scope |
| KWN States | occurrence/place/guarantee/submission/memory/night trajectories complete | submission authority, post-night state, aged report's audience |
| Counterpart Search | exhaustive candidate class traversed; no counterpart occurs | comparison measure not specified; candidate slot is not open but denied |
| Force | every supplied medium/direction/result variant enacted | unknown source actors; restoration after compromise |
| Terrain and Station | material, shelter, rank, responsibility, submission variants complete | severe-ground destination, shelter occupant, authority identity |
| Result Production | source-to-result variants reach products and aftermaths | verifier of evidence, later artifact fate, some recipients |
| Time | day/night/year/age/endurance trajectories complete | post-night state, endpoint of open continuance, successor after first day |
| Formal Network | all explicit containments/base lineages reach fixed point | none |
| Boundary and Count Constellations | complete carrier vectors enacted | none within supplied field |

## 22. Mature-Form Register Without Hierarchy

| named formation | mature status | decisive passage carriers | decisive cross-record bridge |
|---|---|---|---|
| Commanded Content Relay | operational configuration + distributed mapping | `قل` and whole quote | exact AHd-B001 quote; QWL-B001..B016 carrier replays |
| Referent Through Stations | actuality continuation + constellation | `هو -> الله -> الله -> Ø/Ø -> ه` | grammar/coreference only |
| Ahad Enclosure | structural constellation + mapping | first/final `أحد` | AHd-B001 affirmative / B002 exhaustive negative |
| Name and Endpoint Return | structural constellation | repeated `الله`, later `له/ه` | ALH-B002 formation; exact contained form; coreference |
| Directed Need | distributed mapping + assembly | `الصمد` trigger | SMD-B001/B005 + QWL/KFW/KWN role variants |
| Interior and Boundary | projective assembly | `الصمد` trigger, quote boundary | SMD-B002..B004 + KFW-B002/B004 + QWL-B012/B016 |
| Enduring Product | projective assembly | `الصمد` trigger; line-3 counterfield | SMD-B007 + KFW-B005 + WLD |
| WLD Voice Block | operational counterfield + mapping | `لم يلد ولم يولد` | WLD-B001..B006 complete roles |
| Three Negators | structural constellation + vacancy mapping | all three `لم`; two `ولم` | QWL/SMD/KFW/WLD/ALH/KWN vacancy tuples |
| Occurrence and State | constellation + state assembly | `يكن` | KWN-B001..B006 asserted transformations |
| Counterpart Search | operation + topology mapping | `لم يكن له كفوا أحد` | KFW-B001 positive schema + AHd-B002 + WLD-B006 |
| Force Transmission | topology mapping + assembly | QWL/SMD surface-root triggers | SMD-B006 + QWL-B008 + KFW/QWL controller variants |
| Four `د` Boundaries | structural constellation | all four line ends | KFW-B003 ending interface |
| Whole-Unit Machine | structural constellation | every line/word/clause/morpheme/edge carrier | AHd/KFW/WLD count interfaces kept distinct |
| Terrain and Station | projective assembly | AHd/SMD/KWN triggers | mountain, hard place, shelter, rank, guarantee, submission |
| Source-to-Result | distributed mapping + assembly | command output, WLD block, KWN block | QWL/WLD/KWN/KFW/SMD full tuples |
| Temporal Scales | distributed mapping + assembly | ordered surface and verbal grammar | AHd day, WLD birth/age, KFW year, KWN states, SMD endurance |
| Released Forms | structural constellation | every positioned surface/morpheme form | explicit bases, deletions, additions, inflections, containments |

These mature forms coexist. They interact through the exact statuses stated,
but no one has been promoted to a governing interpretation and none has been
discarded for being concrete, formal, remote, or partly open. The resulting
field is ready for reciprocal primary integration when the withheld scaffold
enters.
