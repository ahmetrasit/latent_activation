# Mature Formation Field: S103, V7.5.8

## Fixed-point ground

The field remains closed to the supplied passage, morphology, syntax, lexical assertions, and the Task 01 reservoir. The primary scaffold is still absent. Every formation below preserves two noninterchangeable frontiers:

- `O` carries an identical surface or source-local occupant, state, material, result, or event phase through an exact identity, coreference, asserted lineage, or surface binding.
- `R` carries a supplied content or role schema into a new occurrence. The accepting occurrence supplies its own controller, carrier, medium, participants, and result.
- `K` carries a complete relational topology, parameter transformation, vacancy consequence, or ordered pattern without transporting any local occupant.
- `F` carries exact form, containment, recurrence, affix, or position only.

The exact surface lattice remains the first carrier: three lines, word vector `[1,4,9]`, morpheme totals `[3,6,21]`, syntax-edge vector `[1,3,7]`, opening sequence `وَـ | إِنَّ | إِلَّا`, and ending sequence `عَصْرِ | خُسْرٍ | صَبْرِ`. The only passage-level literal participant continuities are the S2 predication and the S3 controller relay. Every broader movement is typed separately below.

## Mature Operational Configurations

### The Four-Act Controller Relay

The surface supplies one relative-group occurrence and four distinct perfect events. Its exact occupancy path is:

```text
ٱلَّذِينَ@S3.w2
  -> O[exception binding + 3MP agreement + coordination]
  -> controller of ءَامَنُوا@S3.w3
  -> O[same discourse group, new event]
  -> controller of عَمِلُوا@S3.w4
  -> O[same discourse group, new event]
  -> controller of تَوَاصَوْا@S3.w6
  -> O[same discourse group, new event]
  -> controller of تَوَاصَوْا@S3.w8
```

The group is the grammatical controller throughout; that continuity does not make the predicates one event. The first event has `OPEN[explicit surface object/content]`. The second receives the exact direct object `ٱلصَّٰلِحَٰتِ@S3.w5`. The third receives `بِٱلْحَقِّ@S3.w7`; the fourth receives `بِٱلصَّبْرِ@S3.w9`. The four-port vector is therefore `[open, direct object, governed content, governed content]`, not four interchangeable slots.

The exact lexical assertion `الأعمال الصالحة والسيئة` enters the second event through two supplied contacts at once: `عَمِلُوا` supplies the act, and `ٱلصَّٰلِحَٰتِ` supplies the selected positive object class. The negative alternative `السيئة` remains a real but unoccupied position in that lexical contrast. It emits no denied surface act. The completed configuration changes how the rest of S3 is read: later content complements cannot be redistributed backward into the direct-object port, and the first predicate's lexical endpoint cannot be promoted into its open surface complement.

Backward, the two final recommendation events make the controller relay perceptible as repetition-with-differentiation rather than simple accumulation. Forward, the same participant is successively controller while each event changes its affected/content structure. The operation is complete even though S3.w3 keeps its typed surface opening.

### Two Reciprocal Transmissions Under One Controller

The exact repeated form `وَتَوَاصَوْا۟` and the source assertions `تواصى القوم أي أوصى بعضهم بعضا` and `تواصى القوم إذا أوصى بعضهم إلى بعض` license two role re-instantiations:

```text
U-WSY-B003: بعضهم -> أوصى -> بعضا
  -> R[reciprocal recommendation-role schema]
  -> E-S3.6, controller/group supplied by ٱلَّذِينَ,
     content supplied by بِٱلْحَقِّ

U-WSY-B003: بعضهم -> أوصى -> بعضا
  -> R[same role schema, new occurrence]
  -> E-S3.8, controller/group supplied by ٱلَّذِينَ,
     content supplied by بِٱلصَّبْرِ
```

Within each event, initiator and endpoint roles are distributed among the surface group; the exact allocation of individual members remains open. Across events, event identity and content occupant change. The complete invariant is `[3MP controller-group; perfect measure-VI reciprocal act; internal some-to-some direction; بِ-governed definite masculine genitive content]`. The complete delta is `[first recommendation ordinal + حق content] -> [second recommendation ordinal + صبر content]`.

The local lexical `القوم` never occupies the surface group port. Its topology is what is re-instantiated. Conversely, exact surface recurrence never transports the `حق` content into the `صبر` event or makes the second act a continuation of the first. The two events form an operational pair because surface syntax supplies the same controller occurrence and differentiates their explicit contents.

The pair sends a backward effect into `ءَامَنُوا`: `إذعان النفس للحق على سبيل التصديق` makes `الحق` an exact lexical endpoint inside an `إيمان` tuple, while S3 later makes `الحق` the content of a recommendation tuple. This is `F[exact الحق] + K[endpoint/content role change]`, not an occupant handoff. The earlier faith event becomes structurally answerable by a later form without receiving that later content as its unstated surface argument.

### The Predicated Loss Occupancy

S2 contains one exact participant-state operation:

```text
ٱلْإِنسَٰنَ@S2.w2
  -> O[governed ism of إِنَّ]
  -> same surface human occurrence
  -> O[predication through لَفِى]
  -> condition/location خُسْرٍ@S2.w4
```

`إِنَّ` is the grammatical head, `ٱلْإِنسَٰنَ` the governed participant, `لَـ` the emphasis carrier, `فِى` the relation, and `خُسْرٍ` the indefinite governed predicative state. No semantic initiator, cause, measure, prior capital, transaction, balance, or aftermath is supplied. The lexical loss tuples remain differentiated local realizations: general diminution, merchant capital decrease, deficient measure, and the augmented named forms. They change the available relational shape of `خُسْرٍ`, but none supplies an occupant to the surface event.

Backward from S3, the exception scope changes the formal environment around this S2 configuration but does not corefer its singular human with the plural relative group. The mature operation therefore includes a hard identity boundary: S2's participant-state relation is closed, while the cross-line participant relation remains `OPEN[no supplied coreference]`.

### Pressed Material Becoming Expressed Product

The source-local pressing assertions form a literal transformation lineage whenever their own pronouns or equations preserve material identity:

```text
شيء / عنب / local material
  -> O[affected material under ضغط / عصرت]
  -> pressure until يتحلب
  -> O[its expressed water/material]
  -> ما سال عن العصر / ما تحلب من شيء تعصره
  -> resultant role عصارة / عصير
```

In `عصرت العنب واعتصرته`, the local grape occupant remains the affected object across the paired verb forms. In `كل شيء عصر ماؤه فهو عصير`, the same local thing supplies its water and enters the resultant `عصير` role. In `المعصرة ما يعصر فيه العنب`, the press is medium/container and grapes are affected material; in `المعصار شيء كالمخلاة يجعل فيه العنب ويعصر`, an open controller places grapes into the bag-like instrument and presses them. Controller, instrument, source material, expressed liquid, and result are therefore differentiated rather than compressed into a generic extraction image.

The closing surface word contributes a decisive later contact: `الصبر بكسر الباء عصارة شجرة` asserts a local medicinal material as `عصارة شجرة`. Exact `عصارة` activates the pressing lineage, but it does not identify the B008 tree with a particular B002 pressed thing or assert a pressing occurrence for that tree. The completed relation is split exactly:

```text
local صبر material -> O[asserted identity] -> عصارة شجرة
عصارة -> K[exact technical product-role + source/product topology]
        -> pressing lineage
OPEN[particular tree-to-pressing event identity]
OPEN[controller and instrument for medicinal extract]
```

The backward effect is structural: passage-final `صبر` reopens the passage-initial `عصر` as a source/product operation through an exact lexical term. The operational configuration itself remains source-local and identity-disciplined.

### A Gift Leaves and Returns

`أعطيت فلانا عطية فاعتصرتها أي رجعت فيها` supplies a complete two-phase occupant path:

```text
first-person controller/possessor
  -> إعطاء
  -> same عطية occupant reaches فلان
  -> فـ ordered reversal
  -> first-person controller reclaims same عطية
  -> رجعت فيها
```

The gift is the same occupant through both phases; direction reverses, controller returns, and the recipient loses possession. This local event differs from `العصر العطية`, which is a lexical equation, and from `يعتصر الوالد على ولده في ماله`, where a parent prevents a child from receiving or retaining the child's local property. Those tuples enter a direction/controller coalition later, but neither contributes an occupant here.

The operation retroactively differentiates every giving interface in the field. `الأمان إعطاء الأمنة` has an open giver and recipient; work compensation ties wage to prior work; pasture brings abundance to animals; instruction sends something to another. None inherits the gift's reversal without an identity edge.

### Measure, Weigh, Then Make Deficient

`كلته ووزنته فأخسرته أي نقصته` preserves one affected object through a three-operation chain:

```text
local object ه
  -> O[volume measurement: كلته]
  -> O[weight measurement: وزنته]
  -> O[same object affected: أخسرته]
  -> changed result: نقصته
```

The first-person controller, object, two measurement procedures, ordered `فـ`, and decrease result are all explicit. `لا تخسروا الميزان` separately supplies plural addressees and a denied/prohibited operation; it emits no diminished balance. `ينقصون في الكيل والوزن` supplies another plural-controller occurrence whose measured goods remain open.

This operational configuration produces a precise vacancy interface to `اشتريت الشيء صبرة أي بلا وزن ولا كيل`: the first tuple performs both procedures and compromises the result; the second sale omits both procedures. Their comparison matures coalitionally below. The identity chain itself never leaves the first tuple.

### Self as Controller and Held Participant

`صبرت نفسي أي حبستها` makes first-person controller, possessor, and affected self converge on one local occupant. The complete event is:

```text
نفسي
  -> O[possessed self and affected participant]
  -> صبرت / حبستها
  -> directed away from الجزع in الصبر حبس النفس عن الجزع
  -> bounded by ما يقتضيه العقل والشرع
```

The state change is not an unspecified endurance theme: the supplied operation is holding the self away from a named opposed state and upon a constrained requirement. `الصبر نقيض الجزع` preserves polarity; `حبس النفس على ما يقتضيه العقل والشرع` preserves the boundary/content. Controller consent here is internal/self-directed.

This configuration changes expectation elsewhere because the same exact relation `حبس` recurs under radically changed controllers: an open coercer confines a living being toward death, a ruler compels an oath or requital, a parent withholds property, a household confines a maturing girl, a crop becomes enclosed in husks, and a stopper closes a flask. Those are not phases of this event. Their full controller counterfield matures as a distributed coalition.

### One Patient Across Three Requital Verbs

`أقاد السلطان فلانا وأقصه وأصبره بمعنى واحد إذا قتله بقود` binds the same local patient across three asserted equation terms:

```text
السلطان = controller
فلان / ه = O[same affected participant]
أقاد -> أقصه -> أصبره = asserted one-meaning relation
result = قتله بقود
```

The prior act that licenses requital is not supplied and remains delayed/open; the killing result is explicit. `فليصطبر معناه فليقتص` separately re-instantiates the requital-role schema under an imperative whose addressee is open. No negated or missing prior offender is manufactured.

### A Sealed Opening

`أصبر سد رأس الحوجلة بالصبار وهو السداد` differentiates controller, affected boundary, instrument, and result:

```text
OPEN[closer]
  -> uses بالصبار
  -> acts on رأس الحوجلة
  -> سد
  -> opening becomes closed
الصبار = السداد = صمام القارورة
```

The exact `رأس` contact reaches `سقط على حاق رأسه`, but the human head and flask head remain separate occupants. The branch image's well closure stays an actable but unenacted role because the intact assertions enact flask/bottle closure only.

### Perceptual Object Retention

The `ء ن س` assertions supply several exact equation-bound operations rather than one blended faculty:

- `آنست الشيء إذا رأيته` keeps the same local thing across perceiving and seeing.
- `آنسته أبصرته` keeps the same pronominal object across two sight forms.
- `آنست الصوت سمعته` keeps the local sound across noticing and hearing.
- `آنست منه رشدا علمته` keeps `رشدا` as content/result while direction/source `منه` remains explicit.
- `آنَس من جانب ... أبصر نارا` keeps side/source and fire object in one local sight occurrence.

Each is a mature local operation with its own medium and outcome. Their comparison can carry the exact relation `آنست = رأيت/سمعت/أبصرت/علمت`, but it cannot make a seen thing a heard sound or a known guidance occupant. The later eye-image and long-sighted-eye tuples enter a coalition through exact `عين/يرى/نظر`, not through identity.

## Mature Structural Constellations

### The Two-Way Boundary Dictionary

The passage's first and last words form a complete bidirectional lexical circuit. Its smallest exact carriers are the definite genitive boundary forms `ٱلْعَصْرِ@S1` and `ٱلصَّبْرِ@S3`, both ending a line and, respectively, opening and closing the passage. Two independent source paths run in opposite directions:

```text
opening ٱلْعَصْرِ
  -> F[lemma/base عصر]
  -> exact assertion العصر الحبس
  -> F[exact relation الحبس]
  -> assertion الصبر حبس النفس عن الجزع
  -> closing ٱلصَّبْرِ

closing ٱلصَّبْرِ
  -> F[base صبر]
  -> assertion الصبر بكسر الباء عصارة شجرة
  -> F[exact term عصارة]
  -> assertions العصارة ما سال عن العصر /
                العصارة ما تحلب من شيء تعصره
  -> operation/base عصر
  -> opening ٱلْعَصْرِ
```

The first path maps opening-word branch relation to closing-word operation: `عصر = حبس`, while `صبر` performs `حبس النفس`. The second maps the closing word's named material to the opening word's product relation: `صبر = عصارة شجرة`, while `عصارة` is what flows from pressing. The two paths are exact but nonidentical. `الحبس` transports a relation; `عصارة` transports a technical product-role topology. Neither path says that the passage's opening occupant is a particular act of self-restraint or that its closing content is a particular pressed liquid.

The complete constellation changes both boundaries. Forward, the one-word opening already contains a relation later enacted at the passage's last content position. Backward, the final word returns a product term to the opening operation. The circuit is not a single metaphorical event: it is a structural dictionary whose two directions preserve separate local tuples, media, controllers, states, and results.

Two further exact interfaces remain attached without being swallowed by the circuit. `العصر الملجأ/المنجاة` exposes an available refuge, while `أم صبور أمر لا منفذ له عنه` exposes a terminal absence of exit. `العنصر ... في الأصل العصر وهو الملجأ` additionally preserves the inserted `ن`, recovered base `العصر`, and refuge equation. These relations produce a vacancy counterfield developed later; they do not change the two-way dictionary's payload.

### The Predicate Lexicon Cross-Weave

The four S3 predicates are connected by source assertions that name one another's exact surface forms or roles. The complete weave is:

```text
ءَامَنُوا@S3.w3
  -> F[lemma/root]
  -> إذعان النفس للحق على سبيل التصديق
  -> F[exact الحق]
  -> بِٱلْحَقِّ@S3.w7, content of first تَوَاصَوْا

تَوَاصَوْا@S3.w6/w8
  -> F[تواصى / الوصية]
  -> التقدم إلى الغير بما يعمل به مقترنا بوعظ
  -> F[exact يعمل / work-role schema]
  -> عَمِلُوا@S3.w4

عَمِلُوا@S3.w4 + ٱلصَّٰلِحَٰتِ@S3.w5
  -> F[surface binding + complete lexical assertion]
  -> الأعمال الصالحة والسيئة
```

The first path makes a lexical endpoint of the first predicate reappear as the explicit content of the third. The second makes the later reciprocal act's instruction-content something described as actable, pointing back to the earlier action predicate under a different controller and event identity. The third closes exact verb-object selection and preserves the unselected negative alternative.

No path transports occupants. The `النفس` in the faith assertion is not the surface relative group; the lexical `الحق` endpoint is not automatically S3.w7's same entity; `ما يعمل به` is a content schema, not an assertion that either S3 recommendation was acted upon; and the earlier `الصالحات` object is not silently the later recommendation content. What matures is a role architecture: assent exposes a truth endpoint, reciprocal recommendation accepts truth as content, recommendation defines content as actable by another, and the surface already contains a separately controlled intentional act with a selected positive object. Later forms change the functional readability of earlier positions, and earlier positions supply roles that keep later content from remaining formless.

Backward, `بالصبر` adds another exact relation: self-restraint has a constrained standard `ما يقتضيه العقل والشرع`, so the second recommendation content also exposes an act-guiding schema. It remains a second content occurrence with its own lexical tuple, not a completion of `ما يعمل به`.

### Night Joined to Day Inside the Opening Form

The opening root's time assertions give the exact dual pair `العصران الليل والنهار`. The later `و ص ي` field supplies `وصيت الليلة باليوم وصلتها`: night is connected to day, with direction from `الليلة` to `اليوم`. The contact preserves the written difference `الليل/الليلة` and the feminine ending rather than treating them as identical surface words.

```text
ٱلْعَصْرِ@S1
  -> F[عصر]
  -> العصران الليل والنهار
  -> F[الليل/الليلة base-form relation + exact اليوم]
  -> وصيت الليلة باليوم وصلتها
  -> F[وصيت/تواصى root field]
  -> تَوَاصَوْا@S3.w6 and S3.w8
```

This constellation does not say that S3's people join literal night and day. It makes the later connective lexeme perform an exact relation on a pair named inside the opening lexeme: an opening dual alternation can be read through a later source-supplied joining operation. In reverse, the two surface recommendation events return to the opening as a connection relation already instantiated among its temporal terms. The local night and day remain source-local occupants.

The alternate pair `العصران الغداة والعشي`, the equation `العصر العشي`, and `العصار الحين` remain attached as additional temporal positions. No assertion connects those exact occupants through `وصيت الليلة باليوم`; their role is to preserve the opening's non-single temporal distribution.

### The Expanding Positioned Lattice

The complete formal vectors mature together rather than as three unrelated counts:

```text
line ordinal             1              2                 3
word count               1              4                 9
morpheme count           3              6                21
syntax-edge count        1              3                 7
line opening             وَ             إِنَّ             إِلَّا
line ending              definite GEN   indefinite GEN    definite GEN
ending carrier           عَصْرِ          خُسْرٍ             صَبْرِ
participant architecture governed noun  predicated human  excepted group + 4 acts
```

At position 1, one complete word contains oath particle, determiner, and governed noun; line beginning and ending coincide. At position 2, the line separates head, participant, emphatic preposition, and state, while the three supplied syntax relations converge on one predication. At position 3, exception, relative controller, four perfect predicates, one direct object, and two content complements distribute across nine words and seven syntax edges.

The expansion is not mere increase. Formal work differentiates as the carrier set grows: `[single government] -> [government plus predication] -> [exception plus controller relay plus typed complements]`. The opening particles change from attachment, to asserted predication, to scoped alternative. The ending nouns form a definite/indefinite/definite enclosure, all masculine genitives with final `ر`, while only the final line turns its ending noun into the content of a repeated reciprocal event.

Backward, the final dense line reveals that S1's one-word compression is itself internally articulated rather than empty. Forward, S1's exact government prevents the count pattern from inventing a participant or result. Every indexed carrier retains its grammatical operation.

### The Doubled Frame and the Two-Pair Suffix Enclosure

Within S3, the exact predicate/suffix distribution is:

```text
predicate ordinal     1          2           3               4
surface stem          ءَامَنُ     عَمِلُ       تَوَاصَ          تَوَاصَ
suffix                وا۟         وا۟          وْا۟              وْا۟
prefix                none       وَ           وَ               وَ
filled endpoint       OPEN       direct obj  بِ-content        بِ-content
```

The first two suffixes are exactly written `وا۟`; the last two are exactly written `وْا۟`. Removing vocalic decoration discloses a fourfold `وا`, but exact writing produces a `2 + 2` enclosure. The last pair additionally repeats the same verb stem and the same three-morpheme word structure `[وَ + تَوَاصَ + وْا۟]`, followed each time by a three-morpheme complement `[بِ + ٱل + noun]`.

Thus the doubled frame is complete only with its changed content: `تواصوا / بالحق` followed by `تواصوا / بالصبر`. The recurrence carries form, controller, topology, aspect, measure, and complement grammar; the change carries content occupant and passage-final position. The pattern's recognition acts backward on the first two predicates: their shared suffix establishes controller continuity, but their differing complement structures prevent the double frame from being projected over them.

### Exact Opposition as a Distributed Lattice

The supplied relation words `ضد`, `نقيض`, and `خلاف` recur as explicit binary operators. They assemble a structural lattice without exchanging local terms:

```text
الأمن       ضد      الخوف
الأمانة     ضد      الخيانة
الحق        نقيض    الباطل
الحقيقة     خلاف    المجاز
الصبر       نقيض    الجزع
الصلاح      نقيض    الطلاح
الصلاح      ضد      الفساد
الإصلاح     نقيض    الإفساد
الإنس       خلاف    الجن
الإيناس     خلاف    الإيحاش
الإنس       خلاف    الوحشة
الأنس       خلاف    النفور
كلب أنوس    نقيض    العقور
```

The invariant is not a generic positive/negative theme; it is an exact asserted two-position relation whose terms are locally fixed. Parameter changes include state versus betrayal, proposition versus invalid opposite, self-control versus agitation, repair versus corruption, human/relational presence versus estrangement, and animal disposition versus aggressiveness. The surface directly carries `حق`, `صبر`, `آمن`, `إنسان`, and `صالحات`, so several lattice terms or derivational bases occupy positioned surface carriers. Their opposed terms remain lexical positions and do not become absent surface occupants.

The lattice sharpens the exception line structurally: multiple surface words open a supplied opposed position, but the syntax asserts only the present forms. A negated or opposed lexical term emits no surface event or result.

### Derivation With the Added Material Left Visible

Four explicit histories produce a structural constellation of base retention under formal change:

```text
العنصر -> ن زائدة -> العصر -> الملجأ
خنسرى / خيسرى -> ن / ي زائدتان -> موضع الخسران
اليعملة -> مشتقة من العمل
الوصية -> كلام يوصى أي يوصل
```

The first directly contacts `ٱلْعَصْرِ`; the second contacts `خُسْرٍ`; the third contacts `عَمِلُوا`; the fourth contacts both surface `تَوَاصَوْا` and the connection assertions. Full forms, bases, added letters, inflections, and resulting named roles remain visible. The formal operation does not transport the origin/refuge occupant into `العنصر`, the surface loss occupant into a `خنسرى` person, the surface act into a named camel, or a surface recommendation into a particular connected speech.

At passage scale, the constellation makes recovered bases an ordered return across S1, S2, and S3 rather than an etymological ledger: added material discloses how exact surface lexemes can generate other supplied forms while retaining a base circuit. Backward, later `اليعملة` and `الوصية` histories prevent root recurrence from being mistaken for event continuity.

### Homographic Role Rotation Without Occupant Leakage

Exact complete forms recur in sharply differentiated source roles:

- `الصبير` is guarantor, white cloud, or mountain.
- `الصبار` is sour tree fruit or flask stopper.
- `العصرة` is rescue/refuge term, low affiliated status, or tree name.
- `الحق` is opposed-to-falsehood term, owned claim, camel class, joint/receptacle, and other locally asserted roles.
- `صالح` is an act/person quality and a proper personal name; recovered `صالح` also sits inside surface plural `الصالحات`.
- `صلاح` is a condition and a place name; `الصلح` is reconciliation and a river name.
- `الإنسان` names the human class, the image in the eye, and in one assertion a fingertip.

Exact recurrence licenses each comparison, while complete tuple parameters block identity. The constellation's mature work is a formal firewall: a passage word can open all complete source relations, but exact form alone cannot choose a local occupant or merge incompatible roles. This firewall acts on every broader formation, especially the boundary dictionary, where shared `عصر` and `صبر` forms must retain their local operations.

### Small Exact Returns That Reposition Their Hosts

Several initially slight contacts mature into complete two-position structures:

- `الشتاء` joins `جئته في حاق الشتاء` to `أتيته في صبارة الشتاء أي في شدة البرد`. Both have first-person arrival, but temporal position (`حاق`) changes to severe-state enclosure (`صبارة`); endpoints remain local.
- `رأس` joins falling `على حاق رأسه` to closing `رأس الحوجلة بالصبار`. Position changes from impact endpoint on a human head to sealable aperture of a container.
- `عين` joins `إنسان العين ... المثال الذي يرى في السواد` to `ترقبه بعاملة قذوف ... بعين بعيدة النظر`. The eye changes from containing field/image to instrument of distant watching.
- `الرمح/السنان` join `يعمل ... رمحه` to the precisely located `عامل الرمح ما يلي السنان`. Action on a spear and its named working/front part remain distinct, but the later part definition makes the earlier instrument internally articulated.
- `الذين` joins the surface relative group form to `العاملين عليها هم السعاة الذين يأخذون الصدقات`. Only relative-form recurrence travels; the return makes visible how a relative form can introduce a role-defining clause without making the surface group collectors.

Each small constellation is complete as an exact positional or role transformation. None needs a single scene, and none is absorbed into a broad body, weather, or tool topic.

## Mature Distributed Coalitions

### The Holding Relation Changes Hands

Exact `حبس/الحبس`, complete enclosure topologies, and changed controllers assemble a counterfield whose local parameter vectors remain atomic:

```text
C-H1  صبرت نفسي أي حبستها
      controller=self; affected=self; consent/internal agency=self-directed;
      direction=away from الجزع; boundary=ما يقتضيه العقل والشرع;
      outcome=held self

C-H2  المصبورة المحبوسة على الموت / الصبر نصب الإنسان للقتل
      controller=OPEN coercer; affected=living being/human;
      voice=passive/resultant; consent=compelled;
      direction=toward death; outcome=terminal exposure

C-H3  صبرت يمينه أي حلفته / الصبر الإكراه
      controller=first-person coercer; affected=other person through oath;
      medium=يمين; consent=compelled; outcome=sworn act

C-H4  العصر الحبس
      controller/affected/outcome=OPEN; relation schema=confinement

C-H5  يعتصر الوالد على ولده في ماله أي يمنعه إياه ويحبسه عنه
      controller=parent; affected material=child's local property;
      endpoint denied=child; direction=away from child;
      outcome=withheld property

C-H6  المعصر ... تحبس في البيت يجعل لها عصرا
      controller/cause=household construction left implicit;
      affected=maturing girl; medium=house;
      outcome=confinement associated with named stage

C-H7  عصر الزرع صار في أكمامه ... تحرز في غلفه
      controller=growth/state transition; affected=crop;
      medium=its husks/coverings; outcome=protected enclosure

C-H8  أصبر سد رأس الحوجلة بالصبار
      controller=OPEN closer; affected=container opening;
      instrument=stopper; outcome=sealed boundary
```

`K[exact حبس + whole hold/boundary topology]` carries only `[controller acts or state transition places an affected position under a boundary, changing access or motion]`. Deltas are decisive: self versus other versus parent versus open controller versus nonpersonal growth; consent versus compulsion; animate self versus oath-bound person versus property versus crop versus aperture; restraint away from agitation versus progress toward death versus denied possession versus protection versus closure.

The coalition matures further when its vacancy consequences re-enter. H2 terminates in death-facing captivity; H5 makes a beneficiary endpoint unavailable; H7 creates a protective inside; H8 closes a passage; H1 preserves action inside a stated standard. These are not degrees on one scale and cannot be recombined into an unseen event. Together they make the surface-final `صبر` a changed-controller field, not a single local scene. Backward, they reopen `عصر` at S1 through `العصر الحبس` while preserving the opening's unknown controller and affected position.

### One Internal Group Topology, Four Outcomes

Exact `القوم`, reciprocal verb forms, and complete some-to-some/between-them relations connect four local tuple families:

```text
C-R1  تواصى القوم: بعضهم -> recommendation -> بعضا
      outcome=transmitted instruction / mutual connection

C-R2  تصالح القوم بينهم / اصطلحوا / تصالحوا
      participants=group internally related;
      affected state=النفار; outcome=its removal

C-R3  تحاق القوم واحتقوا / حاققته ... في الحق فغلبته
      participants=mutual disputants; content=claimed الحق;
      outcome=continued contest or asymmetric victory

C-R4  عاملت الرجل أعامله معاملة في المبايعة وغيرها
      participants=first-person and counterpart;
      setting=transaction or scoped alternative;
      outcome=ongoing reciprocal dealing, result OPEN
```

The invariant is a specifically supplied internal participant topology: a plurality or pair distributes action across its own members/counterparts. The operation and outcome deltas are `[send/connect]`, `[remove estrangement]`, `[contest ownership of truth/right, possibly defeat]`, and `[deal/exchange]`. Surface S3 re-instantiates R1 twice under one group controller with truth and restraint contents. It does not re-instantiate reconciliation, dispute, or trade.

The coalition's backward effect on `بالحق` is exact. In R3, `الحق` is contested content and each party claims it with itself; on the surface, `الحق` is transmitted/recommended content. The changed operation is `mutual claim against -> mutual recommendation toward`, with local controllers and contents distinct. In R2, reciprocal relation removes a separating state; in R1, reciprocal relation carries content. This changed-outcome field becomes perceptible only after complete tuples are preserved.

Weather groups (`أعصر القوم إذا أتاهم المطر`), groups whose livestock fatten (`أحق القوم`), and groups falling into no-exit severity (`وقع القوم في أم صبور`) share exact `القوم` but do not instantiate an internal reciprocal topology. They remain formal contacts, not members of this coalition.

### Matching Becomes Truth, Fit, Placement, and Provision

The exact source assertion `أصل الحق المطابقة والموافقة` emits two named relational interfaces. They are fully enacted in four further tuples:

```text
C-M1  truth relation: OPEN item -> المطابقة/الموافقة -> OPEN standard
C-M2  رجل الباب -> مطابقة -> في حقه
      moving/fitted part occupies its exact receptacle
C-M3  horse's رجله -> يضع -> موضع يده
      later limb placement matches prior limb position
C-M4  هذا الشيء -> يصلح لك -> هو من بابتك
      thing fits addressed beneficiary's domain
C-M5  المرعى -> أطاع / وافق -> السائمة
      suitable pasture reaches animals as رغدا
```

`K[exact المطابقة/الموافقة or complete fit-to-position topology]` carries `[theme; standard/recipient/position; matching operation; fit consequence]`. M1 leaves both occupants open. M2 supplies door part and socket with static fitted result. M3 supplies one horse's two limbs and sequential placement. M4 supplies a deictic thing and addressee, with suitability result. M5 supplies pasture and grazing animals, with abundance reaching the beneficiary.

No parameter is borrowed across tuples: the truth relation does not acquire a door, the horse does not acquire pasture, and the addressee does not become an animal. The mature coalition acts backward on surface `الحق`: its branch relation is no longer only an opposed term but a complete standard-matching interface. It also acts on `الصالحات`: the recovered `صالح` base can carry suitability as a relation, but the surface feminine-plural object is not identified with M4's deictic thing.

### Deficient Measure Beside Absent Measure

Two complete transaction/material tuples share exact `وزن` and `كيل` but differ in vacancy type:

```text
C-V1  local object -> كلته -> وزنته -> أخسرته/نقصته
      measurement status=performed twice
      controller=first person
      consequence=deficient measured object

C-V2  اشتريت الشيء صبرة أي بلا وزن ولا كيل
      measurement status=ABSENT[weight] + ABSENT[volume measure]
      controller=first-person buyer
      consequence=sale mode صبرة; deficiency not asserted
```

The transported payload is `[measurement procedure positions + their status + local consequence]`. V1's procedures are present but compromised by a later reduction. V2's procedures never occur; the absence changes how the heap is bought. Neither absence fills the other's action, and V2 emits no loss result. `ولا تخسروا الميزان` adds a third, denied realization: addressees are prohibited from performing the compromising operation, so the denied deficient result remains unproduced.

This vacancy coalition returns to S2's `خُسْرٍ` with sharper differentiation. Its lexical field includes both an active compromised measure and a trade deficit, but the surface state supplies neither procedure nor sale. It also returns to the stacked-food tuple: `الصبرة من الطعام بعضه فوق بعض` gives the material arrangement that can enter the unmeasured-sale role, yet no source assertion identifies the particular food heap with the bought `الشيء`.

### Refuge, No Exit, Protective Inside, and Closed Passage

Four vacancy mechanisms form a complete spatial counterfield:

```text
C-E1  اعتصرت بفلان / بالمكان -> التجأت إليه
      endpoint=available refuge; seeker moves/attaches toward it

C-E2  أم صبور أمر لا منفذ له عنه
      occupant=local group/matter relation;
      exit=NEGATED and terminal within assertion

C-E3  crop -> صار في أكمامه / تحرز في غلفه
      inside=protective resultant state; exit not at issue

C-E4  رأس الحوجلة -> سد بالصبار
      passage initially actable/open; closer makes it closed

C-E5  أصبار الإناء / القبر; صبر كل شيء أعلاه/جانبه
      boundary=sides/top; occupant/filler remains OPEN
```

The invariant is not generic containment; each tuple explicitly supplies a boundary and its consequence. E1 has an available destination and produces rescue/attachment. E2 denies a route out. E3 produces guarded enclosure. E4 changes an aperture from open to shut. E5 names a containing boundary while leaving occupancy open. The vacancy types are therefore available, terminally absent, protective, changed from open to closed, and structurally open.

Exact `العصر الملجأ/المنجاة`, `العنصر ... العصر وهو الملجأ`, and `أم صبور ... لا منفذ` make the first two mechanisms touch the passage boundaries. Opening `عصر` contains the available refuge relation; closing `صبر` contains the no-exit relation. The two-way boundary dictionary now acquires a second, orthogonal transformation: its endpoints can oppose available endpoint and terminally absent route without becoming one trapped-or-rescued event.

The coalition also keeps a small return active: `إنسي القوس ما أقبل عليك` and the door-leg/socket tuple orient an object side toward a user or receptacle, but they do not specify entrance, exit, or closure. They remain adjacent structural interfaces rather than occupants of this vacancy field.

### Outward Product, Withheld Property, Returned Gift, and Yield

Complete `ع ص ر` tuples repeat a source-to-recipient/material direction under actual controller changes:

```text
C-X1  pressed thing -> pressure -> expressed عصارة/عصير
      controller=open/first/plural; material direction=outward;
      result=new product role

C-X2  اعتصرت ماله إذا استخرجته من يده
      controller=first person; material=other's property;
      direction=out of possessor's hand; result=extracted property

C-X3  parent -> يمنعه إياه ويحبسه عنه -> child
      controller=parent; material=property;
      direction=blocked away from child; result=withholding

C-X4  gift -> إعطاء to فلان -> اعتصرتها / رجعت فيها
      controller=first person; direction=outward then reversed;
      result=repossessed same gift

C-X5  landholders -> يستغلون بأرضيهم
      controller=plural landholders; source/medium=their lands;
      result=yield/gain, material specifics OPEN

C-X6  المعتصر الذي يصيب من الشيء ويأخذ منه
      controller=recipient/taker; source=thing;
      direction=from thing to taker; result=share received
```

The invariant is a supplied source boundary and a changed direction of something available, produced, prevented, reclaimed, or obtained. Only X1 and X4 contain literal identity lineages across phases. The coalition carries topology and deltas elsewhere. `العصر العطية` and `العصارة الغلة` expose exact role names; they do not identify a particular gift with a particular yield or expressed liquid.

The complete controller counterfield contains producer, extractor, parent, giver/reclaimer, landholder, and taker. Consent is open in pressing, adverse in extraction/withholding, initially benefactive then reversed in gift return, and locally unspecified in yield. Outcomes are product creation, dispossession, denied access, restored possession, and gain. The field acts backward on S1's exact form by keeping giving and withholding as opposed local realizations and forward on S2's loss field by exposing direction changes without asserting that any extracted material is the surface loss occupant.

### The Self Under Quieting, Assent, Restraint, and Inquiry

Exact `النفس` connects four complete local tuples:

```text
C-N1  النفس -> زوال الخوف -> طمأنينة
      operation=security/quieting; controller/cause=OPEN

C-N2  النفس -> إذعان -> للحق على سبيل التصديق
      operation=assent/yielding; endpoint=truth;
      evidentiary manner=التصديق

C-N3  first-person self -> حبس -> away from الجزع
      operation=self-restraint; controller=self;
      boundary=ما يقتضيه العقل والشرع

C-N4  ابن إنسك -> يعني نفسه / للنفس
      operation=addressed inquiry and exact named-reference equation;
      state asked about=OPEN
```

The coalition transports `[exact named participant class النفس + changed relation]`, never a particular self. N1 removes one state. N2 changes orientation toward a content endpoint. N3 changes controller so self acts upon itself. N4 makes self the referent of an interpersonal question without asserting a state. Exact `الحق` chains N2 into the verification/content coalition; exact `حبس` chains N3 into the holding coalition. Coalition membership exposes only those interfaces, so N1 does not inherit truth and N4 does not inherit restraint.

The surface carriers `ٱلْإِنسَٰنَ`, `ءَامَنُوا`, `بِٱلْحَقِّ`, and `بِٱلصَّبْرِ` touch four corners of this mapping by base, endpoint, or operation. No surface edge says that S2's human is any lexical self or that the S3 group is one self. The passage-level recognition is therefore relational: successive surface words activate different supplied ways a self can be quieted, oriented, controlled, or addressed, while participant identity remains closed.

### Speech Becomes Credited, Tested, Carried, and Actable

Specifically named content classes and exact verbs connect a distributed content architecture:

```text
C-S1  الإيمان = التصديق;
      مؤمن لنا = مصدق لنا (inside negation in the cited utterance)
      content=OPEN report/claim; recipient=لنا

C-S2  المؤمن ... يصدق ما وعد عبده
      content=promise; speaker/promiser role locally supplied;
      recipient=servant; fulfillment OPEN

C-S3  حققت قوله وظنه ... صدقت
      controller=verifier; contents=saying and supposition;
      result=credited/confirmed

C-S4  حقق الرجل إذا قال هذا الشيء هو الحق
      controller=speaker; content=quoted predication;
      result=truth-asserting role

C-S5  الوصية ... كلام يوصى أي يوصل
      content=speech; operation=connected/carried onward;
      endpoint=another, locally open

C-S6  التقدم إلى الغير بما يعمل به مقترنا بوعظ
      controller=adviser; endpoint=other;
      content=actable schema; accompaniment=admonition

C-S7  يعمل رأيه أو كلامه
      controller=operator; affected content=opinion or speech;
      result=operation on content, OPEN
```

The exact interfaces are `التصديق/صدقت`, `قول/كلام`, carried instruction, and `يعمل/عمل`. The topology is content produced or present -> credited/tested/asserted -> carried to another -> available to be acted upon or operated on. No source assertion supplies one content occupant across all phases. Consequently this is a coalition and content-role succession, not an identity chain.

Where a target tuple explicitly accepts a schema, a new occurrence is created. S3's two `تواصوا` events accept explicit contents under the reciprocal recommendation role. S3's `عملوا` accepts `الصالحات` as direct object. The supplied field does not say that either recommended content becomes that object, so the actable continuation remains `OPEN[performance of recommended content]`. The coalition nevertheless matures because its role succession is complete through actability.

`آمين` supplies a neighboring but distinct utterance operation: it is said in prayer, is `اسم للفعل`, and means `استجب/اللهم افعل/كذلك فليكن`. It produces a requested-act schema whose performer and act remain addressed/open. It joins through exact speech/act naming, but it neither becomes faith nor supplies the missing performance in S7.

### Appointment, Requested Work, and Role Succession

Two roots supply complete changed-controller maps for making another bear a role:

```text
C-A1  أعمله غيره واستعمله بمعنى
      controller=غيره; affected/appointed actor=ه;
      role=work; result=other made/used to work

C-A2  استعمله أيضا أي طلب إليه العمل
      controller=requester; addressee=ه;
      content=requested work; performance OPEN

C-A3  استعمل فلان إذا ولي عملا من أعمال السلطان
      role-bearer=فلان; office=one ruler-work;
      appointing controller=OPEN; result=assumed office

C-A4  التعميل تولية العمل
      controller/role-bearer=OPEN; result=work-role conferred

C-A5  أوصيت إليه إذا جعلته وصيك
      controller=first person; endpoint=ه;
      result=endpoint becomes وصي

C-A6  الوصي الموصي والموصى إليه جميعا
      one exact role form can name sender and recipient positions;
      occupants remain distinct
```

The invariant is `[controller addresses/selects another -> repeatable work or representative role -> recipient becomes responsible or actable]`. Deltas distinguish coercive/causative use, mere request with open performance, office assumption, generic conferral, and appointment as representative. The work or role schema can pass; the original appointee cannot.

The alms-worker assertions add one completed role realization: `العاملين عليها هم السعاة الذين يأخذون الصدقات` and `هم المتولون على الصدقة`. Here role-bearers, affected material, and taking operation are explicit. No edge identifies them with A3's `فلان` or the surface `ٱلَّذِينَ`. The exact relative form only makes role definition structurally visible.

Backward, this coalition refines `التقدم إلى الغير بما يعمل به`: content can be addressed without performance, while a role can be conferred and enacted only where the target tuple supplies the act. It therefore prevents the predicate cross-weave from silently turning recommendation into action.

### Clouds Under Pressure and Clouds in Layers

Exact `السحاب/السحائب` and the specifically named cloud class connect two complete topology families:

```text
C-C1  المعصرات = السحائب تعتصر بالمطر
      cloud role=rain-bearing/expressing; medium/content=rain;
      controller/force=cloud-local/open

C-C2  المعصرات سحائب تجيء بمطر
      cloud moves/arrives with rain

C-C3  السحابة المعصر تتحلب بالمطر
      singular cloud undergoes expression; result=rain

C-C4  الصبر سحاب مستو فوق السحاب الكثيف
      one cloud layer=level above dense lower cloud

C-C5  السحاب الأبيض يصبر بعضه فوق بعض درجا
      white cloud parts stack above one another in stages

C-C6  الصبير السحاب الأبيض / الاصبار السحائب البيض
      color/class and number roles
```

The invariant is a specifically named cloud carrier with an internal or outward relation. Deltas are `[expresses rain]`, `[arrives with rain]`, `[level above dense layer]`, and `[white parts stacked in stages]`. No local cloud identity crosses. The coalition does not manufacture a white layered rain cloud.

Exact pressing vocabulary `تعتصر/تتحلب` maps C1-C3 to the pressed-product operation at a changed scale and medium, carrying only `[source carrier -> expression -> outgoing liquid]`. Exact stacking `فوق بعض` maps C5 to `الصبرة من الطعام بعضه فوق بعض`, carrying only `[parts of one named material arranged vertically]`. These orthogonal interfaces remain separate: rain expression does not imply stacking, and stacking does not imply rain.

Backward, the cloud coalition links passage-final `صبر` to passage-initial `عصر` a second way, now through exact cloud classes and pressure terms rather than the medicinal `عصارة` circuit. All local sky, water, food, and plant occupants remain distinct.

### Thresholds That Confer Different Roles

Complete transition tuples share explicit `بلغ/دخل/قارب/انتهى` boundary work:

```text
C-T1  camel offspring: three years -> دخل في الرابعة
      -> استحق أن يحمل عليه; female role=حقة

C-T2  she-camel: reaches حقها = الوقت الذي ضربت فيه
      -> prior mating-time relation becomes role condition

C-T3  girl: أدركت/قاربت الحيض/بلغت عصر شبابها
      -> أعصرت / معصر; one tuple includes self-perceived increase

C-T4  livestock/property: سمن -> انتهى سمنه
      -> completed fatness role

C-T5  crop: husks become visible -> قد عصر الزرع
      -> crop enters its coverings/protective state
```

The invariant is `[prior state -> explicitly supplied threshold -> named resultant role with a new consequence]`. T1 permits carrying; T2 points back to a prior timed act; T3 confers a maturity name and, in one tuple, household confinement; T4 terminates a bodily/property condition; T5 produces enclosure. Species, gender, controller, bodily state, and result never mix.

The coalition returns to `الحق` and `العصر` surface forms with a nonstatic relation: each can name a boundary-crossing role in its own lexical field. It also keeps `OPEN[who carries on the eligible camel]`, `OPEN[particular actor in prior mating]`, and `OPEN[controller of crop transition]` intact. No opening is filled by another threshold's participant.

### Perception Rotates From Object to Side to Image to Instrument

The mature perceptual coalition begins from exact operations rather than a shared organ:

```text
C-P1  آنست الشيء = رأيته / أبصرته
      perceiver=open/encoded; object retained; medium=sight
C-P2  آنست الصوت = سمعته
      perceiver=encoded; object=sound; medium=hearing
C-P3  آنست منه رشدا = علمته
      source=منه; content/result=رشدا; mode=knowing
C-P4  إنسي الدابة/القوس = side facing rider/archer
      perceiver/operator becomes orientation endpoint
C-P5  إنسان العين = المثال الذي يرى في سواد العين
      eye-darkness=container/field; image=contained perceived form
C-P6  ترقبه بعاملة قذوف = بعين بعيدة النظر
      watcher=controller; eye=instrument; distant object=ه
C-P7  girl رأت في نفسها زيادة الشباب
      perceiver and bodily self locally corefer; result=threshold recognition
```

Interfaces are exact `رأى/أبصر/سمع/علم/عين/نظر` plus the complete facing-observer topology. Parameters rotate: object of perception, sensory medium, spatial orientation, contained image, bodily instrument, distance, and self-reference. The coalition transports those relations only. The sound never becomes an image, the bow side never becomes an eye, and the watched object never becomes the girl's self.

The surface `ٱلْإِنسَٰنَ` activates the human-class and eye-image forms by exact base contact. Backward from the mature coalition, its surface role is sharpened by a field in which human naming can arise from visibility (`سموا لظهورهم`) and in which a human image occupies the eye's dark field. No supplied relation identifies the S2 human with any perceiver or image.

### Work Travels Through Body, Tool, Road, and Walker

Explicit `عمل/عامل/عوامل/معمل`, named limbs, and route conditions connect a distributed working topology:

```text
C-W1  كل فعل يكون من الحيوان بقصد
      actor=living being; operation=intended act
C-W2  العملة يعملون بأيديهم ... حفرا/طيا/طين
      group=controllers; hands=instruments; products/results OPEN
C-W3  عوامل الدابة قوائمه، واحدها عاملة
      animal legs=working parts
C-W4  المسافرون إذا مشوا على أرجلهم يسمون بني العمل
      travelers=controllers; legs=medium; name=result
C-W5  طريق معمل = لحب مسلوك
      road=carrier; prior traversal encoded; traveler OPEN
C-W6  عامل الرمح = part next to point / shaft-front
      tool part=positioned carrier; action OPEN
C-W7  يعمل ... رمحه
      controller operates spear; exact part/result OPEN
```

The invariant is not generic motion: exact work-derived forms place agency in whole actor, hand, animal limb, walking leg, traversed road, or spear part. Deltas rotate controller and carrier across living actor, group, body part, path, and tool. The coalition never constructs one traveler walking a particular road with a spear.

`الحقحقة ... يسار البعير ويحمل على ما يتعبه ولا يطيقه` joins through an explicit gait/animal-capacity topology, not the work root: the camel is passively driven, back affected, and capacity negated. It contrasts with `اليعملة الناقة النجيبة المطبوعة على العمل`, whose local camel is disposed toward work. Changed controller/consent/outcome are `[animal capable/disposed]` versus `[animal compelled beyond capacity]`; the camels remain distinct.

The penetrating-stab tuple joins only at the tool-operation boundary: `طعنة محتقة ... نفذت إلى الجوف` supplies trajectory and result but leaves weapon open; `عامل الرمح` supplies spear part without a stabbing act. The coalition keeps `OPEN[identity of a spear in a penetrating stab]` rather than fabricating it.

### Trade, Wage, Claim, and Reversal Around Possession

Specifically named commercial and entitlement roles create another complete counterfield:

```text
C-ECO1 merchant -> وضع من رأس ماله -> loss;
       transaction -> غير مربحة
C-ECO2 worker's prior act -> العمالة/العملة -> wage/provision
C-ECO3 buyer -> المبايعة / معاملة -> counterpart; result OPEN
C-ECO4 claimant -> استحقها على المشتري -> ملكها عليه
C-ECO5 giver -> gift to recipient -> reclaims same gift
C-ECO6 parent -> child's property -> withholds it from child
```

The explicit interfaces are transaction/buyer, capital, wage tied to prior work, owned claim over a buyer, gift transfer, and property possession. The topology carries `[possessor/controller; material or entitlement; counterparty; direction; resulting possession or deficit]`. Outcomes differ: diminished capital, earned compensation, open exchange result, established claim, reversed gift, and denied access. None supplies material to another.

The coalition returns to S2 `خسر` through the merchant tuple and to S3 `عملوا` through wage's prior-act condition, but it cannot say that the surface action earns a wage or that the surface loss is financial. It also prevents `الحق` from being reduced to abstract matching: one branch makes a claim a possessed relation over a buyer, while the surface uses `الحق` as recommendation content.

### Rising Media Under Changed Scale

Exact `ريح`, `يرتفع/ترفعه`, and emitted-material topologies relate several local `ع ص ر` tuples:

```text
C-RISE1 wind blows -> raises dust -> dust rises to sky like column
C-RISE2 whirlwind = circular rising dust / wind from earth toward sky
C-RISE3 wind agitates soil -> lifts same soil
C-RISE4 perfumed woman passes -> عصرة at hem = perfume emission/agitation
C-RISE5 العصار = bodily wind
```

R1-R3 form a full identity-bearing local wind/material operation only within each utterance. R4 changes controller/carrier to a moving perfumed person and material to scent; R5 is an exact name equation without trajectory. `K[exact ريح/emission vocabulary + changed scale/carrier]` carries medium and scale transformations, not air or material occupants. The mature field therefore preserves atmospheric column, circular dust, lifted soil, trailing perfume, and bodily wind as distinct actual vectors. No hybrid scented whirlwind or rising bodily medium is emitted.

### Support, Sides, and Vertical Accumulation

Exact `فوق بعض`, `تحت`, `نواحي`, and `أعلاه` build a material-position coalition:

```text
C-L1 cloud parts -> فوق بعض -> staged vertical stack
C-L2 food -> بعضه فوق بعض -> heap
C-L3 broad table sheet -> تبسط تحت food -> support
C-L4 vessel/grave -> أصبار = sides
C-L5 each thing -> صبر = top/side
C-L6 stone/pebbled land -> hard material or contained حصباء
```

Payload is the explicit positional relation only: above, below/support, side, top, or contained hard material. C-L1 and C-L2 share whole stacking topology under changed medium and scale. C-L3 introduces an under-support rather than another layer of the same occupant. C-L4/C-L5 expose boundary positions. C-L6 supplies material hardness and land containment but no stacking operation.

The coalition keeps `OPEN[filler to the vessel sides]`, `OPEN[occupant within grave]`, and `OPEN[agent stacking food/cloud]`. It acts backward on the no-exit/enclosure field by differentiating a boundary's existence from closure or denied passage.

## Independent Resurrection Sweep

The organizing names above are now set aside. The remaining exact tuples re-enter in stable source order and are allowed to form independently.

### Security Given, Entered, and Located

`الأمان إعطاء الأمنة`, `استأمن إليه دخل في أمانه`, and `مأمنه منزله الذي فيه أمنه` form a three-operation role succession:

```text
OPEN giver -> gives الأمنة -> OPEN recipient
new seeker occurrence -> moves إليه -> enters another's أمان
local possessor -> مأمنه = منزله -> contains أمنه
```

The first produces a transferable security-role/content but leaves both parties open. The second explicitly accepts another's security as an entered domain; its seeker and endpoint are local. The third locates a possessed security state in a possessed home. Because no identity edge connects the three, the complete architecture is `K[giving -> entry -> located secure domain]`, not one person's journey.

The camel `الأمون الناقة الأمينة الوثيقة أو التي يؤمن فتورها وعثورها` adds a changed carrier: security/trust attaches to an animal whose slackening or stumbling is treated as secure. It exposes reliability rather than a gifted or entered domain. This mature side formation touches the work-capable `اليعملة` camel and the overdriven camel only through a changed animal-role topology: trusted against failure, disposed toward work, or compelled beyond capacity. Their occupants and controller vectors remain separate.

### Protection as Obligation, Guarantee, and Accompaniment

`حامي الحقيقة إذا حمى ما يحق عليه أن يحميه`, `الحقيقة ما يحق على الرجل أن يحميه`, and `فلان يحمي حقيقته` produce an operational role whose prior obligation is required. The affected object may be locally named `الراية/الحرمة/الفناء`, but the defender cannot exist in the tuple without the `يحق عليه` relation.

Exact role topology links it to `الصبير هو الكفيل` and `صبير القوم ... يكون معهم في أمورهم`:

```text
defender -> obligation -> protects required object
guarantor -> كفالة بفلان -> bears/supports another
group-supporter -> معهم في أمورهم -> accompaniment
```

The invariant is a specifically supplied responsibility relation toward another or a protected position. Deltas are defense, guarantee, and accompaniment. The protected object, guaranteed person, and group remain local. The surface `الحق` activates the obligation interface; surface `الصبر` activates guarantor form. No surface participant is appointed defender or guarantor.

### Integrity Across Woven Cloth, Speech, and Deliberation

`ثوب محقق إذا كان محكم النسج`, `كلام محقق أي رصين`, and `أحققت الأمر ... أحكمته وصححته` explicitly rotate the affected carrier while retaining `إحكام`:

```text
cloth -> woven structure becomes محكم
speech -> becomes رصين
matter -> controller أحكمته وصححته
```

Exact `كلام` then meets `يعمل رأيه أو كلامه`, `الوصية ... كلام يوصى أي يوصل`, and `أعمل فلان ذهنه ... إذا دبره بفهمه`. This produces a carrier-succession coalition: speech can be structurally sound, operated on, or connected onward; a mind can be deliberately operated on to arrange constrained content through understanding. No particular speech or matter continues across events. The complete parameter changes are material weave -> linguistic carrier -> deliberated content -> transmitted instruction.

The backward effect on `بالحق` is formal and evidentiary: its source field includes judging something true and making speech sound, but S3's content supplies neither verifier nor speech carrier. Those ports remain open.

### A Blocked Bodily Route and a Little-by-Little Medium

`الاعتصار أن يغص الإنسان بالطعام فيعتصر بالماء وهو أن يشربه قليلا قليلا ليسيغه` is a complete multi-phase local operation:

```text
local human -> food obstructs swallowing -> غص
same local human -> uses water as medium -> يشربه قليلا قليلا
purpose/result -> ليسيغه
```

The second utterance preserves first-person throat, a counterfactual other-than-water condition, and comparison to one whose `اعتصار` is by water. `المعصور اللسان اليابس عطشا` supplies a distinct local bodily state: tongue made dry through thirst. `K[exact ع ص ر field + bodily water-route topology]` connects obstruction/remedial medium and dryness/cause without identifying persons, throat, tongue, or water.

The vacancy mechanism differs from the no-exit matter. Here the route is compromised but water is an available incremental medium and swallowing is an explicit purpose; in `أم صبور` the exit is negated. The comparison carries `[blocked route; medium status; consequence]`, not a bodily occupant.

### Midwinter Position and Severe Cold

The slight exact contact `الشتاء` completes a two-tuple transformation:

```text
جئته في حاق الشتاء
  parameter=time position at حاق; first-person arrival; endpoint local

أتيته في صبارة الشتاء أي في شدة البرد
  parameter=severe cold state; first-person arrival; endpoint local
```

The invariant is first-person arrival located by winter. The delta is central/precise temporal position -> intensity of cold. The events and endpoints are distinct. `صبارة الشتاء شدة برده` closes the second equation. This mature constellation remains independent of cloud and refuge coalitions even though winter can attract them topically; no supplied interface joins those occupants.

### Named Forms Hold Their Local Worlds Apart

Several exact equations mature as a structural constellation of locally complete names rather than unused debris:

- `صالح` is a supplied personal name; `صلاح` names مكة; `الصلح` names a river in ميسان. Formal-weight assertions `صلاح في وزن حذام وقطام` and `صلاح مثل قطام` preserve their exact relation. The surface `الصالحات` recovers base `صالح` plus plural `ات`, but proper person and place occupants remain barred from its direct-object port.
- `الصبر أيضا بطن من غسان` is a named group; `الصبير الجبل` and `الصبير الأقدر وهو الوسط من الجبال` are named landform/position roles. Exact form recurrence with guarantor and cloud carries role rotation only.
- `العصرة شجرة` is a complete tree-name equation. It does not become the tree source in `عصارة شجرة`; the latter leaves its tree class unspecified, and no identity edge is supplied.
- `الصبار التمر الهندي` and `الصبار صمام القارورة` share exact form but have fruit and stopper occupants. Their complete tuples produce taste versus closure consequences, which remain incomparable beyond form/role rotation.
- `العصار الفساء` names a bodily-wind occurrence; atmospheric `إعصار/عصار` tuples supply different form, scale, and material movement.
- `الحق من الخشب`, `الحقة من خشب`, `حق العاج`, `حق الورك`, `حق الوابلة`, and `حق الكهول بيت العنكبوت` preserve material, anatomical, and named receptacle roles. Only `مطابقة رجل الباب في حقه` supplies a fit operation.

The mature consequence is not merely “many senses.” Exact surface contact activates complete local worlds while the occupancy firewall prevents any name equation from entering an event port. This structure remains active whenever a broader coalition uses one of the same forms.

### Affiliation Can Be Origin, Intimacy, or Lower Status

Three complete relation families share specifically named affiliation positions:

```text
العنصر أصل الحسب / كريم العصير كريم النسب
  affiliate -> lineage -> origin from which it is

فلان ابن إنس فلان = صفيه وخاصته / أنيسه
  person -> chosen intimate relation -> another person

هؤلاء موالينا عصرة = دنية دون من سواهم
  deictic group -> affiliation to speaker-group -> lower/near status;
  comparison excludes others
```

The invariant is an explicitly named relation to another or an origin. The deltas are lineage, chosen intimacy, and ranked affiliation. Exact terms `أصل/نسب/قرابة/مودة` also encounter the negation `ما بينهما عصر ولا يصر أي ما بينهما مودة ولا قرابة`: a fourth tuple terminally denies two affiliation types between its local pair.

No people or origin transfer. The vacancy comparison is precise: the intimacy tuple fills a relation, the lower-status tuple fills and ranks it, the lineage tuple points back to source, and the negative `عصر` tuple denies affection and kinship. The opening word's form thus holds both refuge/origin relations and a local denial of relation without resolving them into one passage claim.

### Hardness, Capacity, and Burden Remain Differentiated

`الصبرة من الحجارة ما اشتد وغلظ`, pebbled land, and `أم صبار الحرة أو الصفاة` supply material hardness. `الحقحقة ... يحمل [البعير] على ما يتعبه ولا يطيقه` supplies exceeded animate capacity. `لا تتعمل ... لا تتعن` supplies a denied self-burden, while `سوف أتعمل في حاجتك أي أتعنى` supplies willing future effort for another's need.

The exact interface is not hardness alone; the complete mapping is `[resistance/burden carrier; controller; capacity status; consequence]`. Stone has no controller or fatigue. The camel is passively driven beyond capacity. The addressed person is told not to undertake burden. The speaker voluntarily undertakes effort toward another's need. These actual vectors form a changed-consent coalition without making hard material a burden occupant.

### Rain Reaches a Group Under Controller Rotation

The rain assertions preserve three grammatical arrangements:

```text
أعصر القوم إذا أتاهم المطر
  group=recipient; rain=semantic initiator moving toward them

عصر القوم أي مطروا
  group=grammatical controller; equation outcome=rain event

يعصرون أي يأتيهم المطر / تعصرون أي تمطرون
  plural grammatical controller under supplied reading;
  semantic rain arrival/production changes with equation
```

The invariant is an exact rain relation in the `ع ص ر` form field. Controller, address, and direction rotate: rain comes to group; group bears the verb equated to being rained upon/raining; addressed plural receives the `تمطرون` equation. The cloud coalition supplies rain-bearing carriers, but no source edge identifies a particular cloud with any group's rain. This mature changed-controller field preserves grammatical control even where semantic initiative shifts.

### Completion, Entitlement, and Exhaustion Split Apart

The `ح ق ق` field supplies several terminal states linked by exact form but differentiated outcome:

- camel reaches age and becomes entitled to be carried upon;
- she-camel reaches a prior mating time;
- livestock reaches completed fatness;
- stab reaches the interior without deviation;
- cloth/speech/matter reaches a firm/correct state;
- severe gait exhausts a back and exceeds capacity;
- horse placement matches one hoof position to another, while a separate horse becomes lean.

Exact `حق/استحق/احتق` licenses structural comparison. Whole-topology links emit only where a named interface exists: threshold coalition for age/fatness; matching coalition for hoof placement; integrity coalition for firm state; movement coalition for gait/stab. Membership in one root never globalizes these interfaces. The result is a mature branching constellation whose exact form can mark attainment, fit, penetration, completion, or exceeded limit without producing a hybrid terminal state.

## Interaction After Precision

### Boundary Dictionary x Holding Counterfield x Vacancy Field

The two-way boundary dictionary supplies exact paths `عصر -> حبس -> صبر` and `صبر -> عصارة -> عصر`. The holding counterfield expands only the first relation into actual controller vectors; the extraction coalition expands only the second into source/product and direction vectors. The vacancy field adds `ملجأ` versus `لا منفذ` as an orthogonal consequence comparison.

The interaction remains payload-separated:

```text
boundary F/K circuit: exact words and definitions
  + holding K node: controller/consent/outcome transformations
  + extraction K node: source/material/direction transformations
  + vacancy K node: available endpoint vs terminally absent route
  != one event, one material, or one participant
```

After interaction, the opening and closing forms perform more than echo: each is a junction through which separately complete relation types return to the other. Yet each local tuple remains recoverable: self-restraint is still self-restraint, medicinal sap is still a local material identity, a pressed grape is still a local source/product chain, and a trapped group is still local to its no-exit assertion.

### Predicate Cross-Weave x Speech Succession x Controller Relay

The predicate cross-weave produces exact role links among `آمنوا`, `عملوا`, `الصالحات`, `تواصوا`, and `الحق`. The speech coalition adds content production, crediting, verification, transmission, and actability. The surface controller relay supplies one literal group occurrence across four distinct events.

Their interaction yields a complete distributed architecture:

```text
surface group -> O[controller continuity] -> four event occurrences
faith source -> K[exact الحق endpoint] -> later recommendation content
recommendation source -> R[actable content role] -> OPEN[new performance]
surface عمل -> O[direct object binding] -> الصالحات
truth source -> K[verification/assertion topologies] -> content field
```

The architecture is complete without performing the open actable continuation. It changes backward reading: `عملوا` becomes an available but already independently filled action occurrence, not the automatic performance of later advice; `آمنوا` becomes an event whose lexical truth endpoint later receives a surface carrier but not an occupant handoff. It changes forward reading: later recommendation is no longer contentless, but its effects remain unasserted.

### Formal Expansion x Controller Relay x Double Frame

The count lattice supplies positions; the controller relay supplies occupant continuity; the doubled frame supplies exact recurrence with changed content. Their interaction makes line 3's nine-word expansion functionally ordered:

```text
positions 1-2: exception + controller
positions 3-5: two predicates, second closed by direct object
positions 6-7: reciprocal predicate + truth content
positions 8-9: reciprocal predicate + restraint content and passage boundary
```

This grouping is not manufactured: it follows supplied words, morphology, and syntax. The `2 + 3 + 2 + 2` functional segmentation is a description of complete surface bindings, not a replacement count vector or arbitrary carrier subset. The primary formal vectors remain `[1,4,9]`, `[3,6,21]`, and `[1,3,7]`.

The final content is simultaneously the second doubled-frame endpoint, the line ending, and the passage ending. The opening line's single word is simultaneously particle host, governed complement, line ending, and passage beginning. These indexed role accumulations make the boundary dictionary formally available without transporting lexical occupants.

### Loss Predication x Measurement Vacancy x Possession Counterfield

S2 supplies a bare participant-in-loss operation. The measurement coalition distinguishes performed-but-deficient procedure from absent procedure. The possession coalition distinguishes diminished capital, wage, claim, trade, gift reversal, and withholding.

The interaction does not specify which mechanism realizes S2. Instead it matures `خسر` as a typed result interface with several fully enacted source mechanisms whose parameters are mutually exclusive unless a source tuple joins them. A balance can be reduced; capital can diminish; a transaction can lack profit; a heap can be sold without measure; a wage can follow work; a claim can be established against a buyer. None fills S2's missing cause. The surface's openness is now exact rather than vague: `OPEN[which supplied or unsupplied mechanism, controller, material, and scale]` remains after every generated relation is enacted.

### Perception x Matching x Truth Verification

Perception converts a retained local object into seen/heard/known result; matching relates an item to a standard; verification moves a controller into certainty about a matter or credits a saying. Exact `علمته`, `يقين`, `المطابقة`, and `صدقت` expose an evidentiary topology:

```text
local perceiver encounters content/object
  -> local perception/knowledge result
separate matcher/verifier compares or confirms local matter
  -> certainty/credited result
```

No exact source assertion carries one content occupant between these events, so the interaction remains coalition-only. It sharpens controller roles: perceiver, verifier, speaker, and assenting self do not collapse. Surface `الحق` can activate standard and verification relations while remaining the content of reciprocal recommendation.

## Vacancy and Negation Re-entry

The matured formations expose vacancy types that now compare through their consequences. Each remains scoped to its own assertion.

### Denied Passage and Incomplete Penetration

`طعنة محتقة` reaches `الجوف`, has `لا زيغ فيها`, and `قد نفذت`. The supplied branch contrast preserves `المشرم` as a wound that injures but does not penetrate. Its complete comparison is:

```text
penetrating stab: trajectory straight; endpoint interior; result achieved
المشرم: injury achieved; penetration result NEGATED
```

This differs from `أم صبور ... لا منفذ له عنه`, where a participant's exit is terminally absent, and from the sealed flask, where an open passage is actively closed. The invariant is a passage/endpoint position; the consequences are reached interior, denied inward continuation, denied outward route, and closure. No blade, trapped participant, or stopper transfers.

### Absent Occupant, Absent Singular, and Absent Procedure

The supplied `ء ن س` summary preserves `ما بالدار أنيس` with `أنيس` meaning `أحد`: a house has a negated human occupant. `الخناسير الهلاك لا واحد له` supplies a lexical-number vacancy: no singular form/one belongs to the named plural. `اشتريت الشيء صبرة أي بلا وزن ولا كيل` supplies two absent procedures. Their consequences differ:

```text
NEGATED house occupant -> local place remains unoccupied by an أحد
ABSENT lexical singular -> number paradigm remains without one form
ABSENT measuring acts -> sale proceeds in صبرة mode
```

None is a generic empty socket. The first cannot receive S2's human; the second cannot emit a missing person; the third cannot receive measurement operations from the deficient-measure tuple.

### Denied State, Act, Capacity, Relation, and Result

The field keeps the following negations independently active:

- `وما أنت بمؤمن لنا أي مصدق لنا`: addressed assent/credit role is denied; no credited content results.
- `لم يستوحش منه`, `كلب أنوس نقيض العقور`, and security against camel slackening/stumbling: estrangement, aggression, or expected failure is locally absent/denied, with different scopes.
- `صفقة خاسرة أي غير مربحة`: profit result is denied while transaction remains.
- `لا تخسروا الميزان`: compromising action is denied/prohibited; diminished balance is not produced.
- `لا تتعمل ... لا تتعن` and `لا تعمل`: burdening act is denied; no effort result follows.
- `ما بينهما عصر ولا يصر ... ما بينهما مودة ولا قرابة`: two relations are denied between the local pair.
- `ما نام لعصر`: a temporally qualified sleep event is denied while the adjacent positive `نام` stays distinct.
- `الأحق من الخيل الذي لا يعرق`: sweating is absent within the named horse role.
- the overdriven camel `لا يطيقه`: capacity is denied although imposed travel/load occurs.

Vacancy comparison carries `[typed position; negation/absence status; effect on local operation]`. It never produces the denied state or lets another tuple fill it. These negations re-enter the opposition lattice only as scoped outcomes, not as surface assertions of every opposed term.

### Open, Delayed, Replaceable, and Reversed Positions

- S3 faith has an open explicit surface complement. It is constrained only as `OPEN[surface argument]`; exact lexical `الحق` provides a coalition endpoint but not an occupant.
- Recommendation content is actable under `ما يعمل به`; performance is delayed/open. S3 supplies two recommendation contents but no later performance edge.
- The eligible camel has an open carrier/rider position. Work-capable camel and traveler tuples cannot fill it.
- The requital tuple presupposes a prior licensing act but does not name its actor or event; the result is explicit while the prior position stays delayed.
- `الوصية بعد الموت` supplies delayed temporal activation of instruction; no deceased occupant or executor is identified.
- Official work and representative appointment roles are replaceable/repeatable schemas, but each enactment has a new role-bearer unless identity is supplied.
- The gift tuple contains a true reversed occupant path: the same gift leaves and returns. It is not merely replaceable or open.
- The parent-property tuple contains denied access, not reversal; the child's possession/result remains withheld.
- Vessel sides and fill-to-`أصبار` expose an open filler/extent position. No food heap or cloud layer fills it.

These distinctions survive every coalition chain. A completed structural constellation may include an open occupant-dependent port without weakening its exact relational work.

## Dual-Frontier Fixed Point

### Occupancy Frontier Closure

The full reread of every source assertion against every surface token and recovered form produces no passage-level occupant edge beyond:

1. S2's human occurrence continuing through `إِنَّ` government into the `لَفِى خُسْرٍ` predication.
2. S3's relative-group occurrence continuing as controller of all four coordinated predicates.
3. `الصالحات`, surface `الحق`, and surface `الصبر` filling only their exact direct-object or governed-content ports.

Source-local identities and lineages remain complete inside their own operations: measured object through measure/weight/decrease; gift through giving/reclamation; property through extraction/withholding; material through pressing/product; self through self-restraint; patient through three requital verbs; objects through perception equations; instruction-content through `ما أوصيت به`; appointee through `جعلته وصيك`; and each locally coreferred pronoun path.

The content/role re-instantiation closure adds exactly these new-occurrence schemas:

- reciprocal `بعضهم -> بعضا` recommendation topology into each surface `تواصوا` event;
- `الأعمال الصالحة والسيئة` act/object class into the surface `عملوا الصالحات` event, with negative alternative unoccupied;
- instruction as connected speech and as `ما يعمل به` into fresh recommendation/actable roles, while performance stays open;
- appointment/work/guardian roles into their separately controlled source occurrences;
- matching, protection, guarantor, and threshold roles only where accepting tuples explicitly enact them.

No further accepting tuple binds a surface occupant. Generic compatibility among human, group, thing, material, animal, place, or content positions emits nothing.

### Coalition Frontier Closure

Re-entry of every new coalition node exposes the following shared interfaces and no occupant globalization:

```text
Two-Way Boundary Dictionary
  -> exact حبس -> Holding Relation Changes Hands
  -> exact عصارة/pressing -> Outward Product coalition
  -> ملجأ / لا منفذ -> Refuge and No-Exit vacancy field
  -> cloud/pressure contacts -> Clouds Under Pressure and in Layers

Predicate Lexicon Cross-Weave
  -> exact الحق -> Matching + Truth/Speech coalitions
  -> exact يعمل -> Appointment/Actability coalition
  -> exact الصالح -> Opposition lattice + suitability mapping
  -> repeated تواصى -> Reciprocal Group topology

Expanding Positioned Lattice
  -> indexed S3 positions -> Four-Act Controller Relay
  -> repeated final frame -> Two Reciprocal Transmissions
  -> ending carriers -> Two-Way Boundary Dictionary

Measurement Vacancy
  -> exact trade/buyer/material interfaces -> Possession counterfield
  -> loss result interface -> S2 predication, with mechanism still open

Perception Coalition
  -> exact عين -> image/instrument constellation
  -> knowledge/certainty interface -> verification coalition
  -> facing relation -> matching orientation, without entrance/fit payload

Work-Carrier Coalition
  -> legs/gait -> capacity counterfield
  -> spear/point -> penetration attraction with weapon identity open
  -> path/travel -> route carrier, traveler identity open
```

Each node exposes only the listed interface. For example, boundary-dictionary membership does not give clouds a refuge interface; speech-coalition membership does not give a promise a direct-object relation; possession-coalition membership does not give wages a loss result.

The second independent empty-cycle reread recovers the small exact returns (`الشتاء`, `رأس`, `عين`, `الرمح/السنان`, `الذين`), derivational full/base/affix relations, proper names, group names, and body/material equations already matured above. No newly recovered complete form remains outside comparison. A further pairwise pass yields only generic shared actors, objects, directions, states, shapes, or materials and therefore emits no new coalition.

## Forward Rereading After Maturation

### S1: Compressed Junction

`وَٱلْعَصْرِ` remains one governed word, but its exact base now participates in several independently complete structures:

- the two-way boundary dictionary through `الحبس` and `العصارة`;
- the night/day connection through `العصران الليل والنهار` and `وصيت الليلة باليوم وصلتها`;
- the holding controller counterfield through `العصر الحبس`;
- the source/direction coalition through pressing, extraction, withholding, gift, and yield;
- the refuge/no-exit vacancy comparison through `العصر الملجأ/المنجاة`;
- the derivational history `العنصر -> ن -> العصر`;
- rain, wind, maturity, crop enclosure, affiliation, bodily route, and named-form realizations, each still local.

The mature formations do not choose among them. They change the word's formal work: the compressed opening is a junction whose exact interfaces are answered at later surface positions, while its oath complement still has `OPEN[discourse endpoint/result]`.

### S2: Exact State With Mechanism Withheld

`إِنَّ ٱلْإِنسَٰنَ لَفِى خُسْرٍ` remains the only predicated participant-state tuple. Human lexical forms now make visibility, perception, proximity, facing side, eye image, self-reference, and absent-house-occupant relations available. Loss lexical forms now make diminution, capital loss, deficient measure, unprofitable transaction, and augmented forms available. The mature measurement and possession coalitions expose complete possible mechanisms as source-local architectures, but no relation selects or inserts one.

Thus maturation increases precision without filling the cause: the participant and state are exact; controller, process, scale, measure, and aftermath remain open. The line's indefinite ending continues to sit between definite `عصر` and definite `صبر` in the formal enclosure.

### S3: Differentiated Acts and Re-entering Content

`إِلَّا ٱلَّذِينَ` opens a scope whose one group controller continues through four events. The first predicate activates security and assent operations; the second closes on its direct object and exact lexical act-class; the last two re-instantiate reciprocal recommendation with two distinct contents.

Mature coalitions now let each later carrier act backward without moving occupants:

- `الحق` returns to faith as an exact endpoint schema, to dispute as contested content, to matching as standard relation, to verification as judged/credited content, to defense as obligation, and to claim as possession.
- `الصبر` returns to self-control, coercion, guarantee, material extract, cloud, heap, boundary, no-exit severity, requital, names, and closure; exact `حبس` and `عصارة` send it farther back to `عصر`.
- `تواصوا` returns to thing-connection, night/day connection, carried instruction, role appointment, mutual connection, and pasture suitability, while its surface events retain only truth and restraint contents.
- `الصالحات` retains direct-object occupancy while recovered `صالح` opens repair, reconciliation, suitability, and proper names without admitting their occupants.

The exception line therefore remains a sequence of exact events surrounded by mature relation fields, not a fusion of lexical scenes.

## Backward Rereading After Maturation

Starting at passage-final `بِٱلصَّبْرِ`, the reader first encounters the doubled recommendation frame, then the holding/extraction boundary dictionary, then the changed-controller and vacancy fields. `الصبر` can be self-held, coerced, guarantor role, side, hard matter, severe no-exit condition, cold, medicinal extract, fruit, layered cloud, food heap, requital, group, mountain, or stopper; exact form activates all, while surface grammar selects only recommendation content.

Moving to the preceding `بِٱلْحَقِّ` changes content but preserves the full reciprocal frame. Matching, verification, claim, dispute, defense, entitlement, penetration, integrity, fit, severity, and completion remain distinct actual tuples. The exact faith endpoint now makes `آمنوا` reopen, and the speech/actability coalition makes `عملوا` reopen, but neither creates an occupancy edge.

Moving through `ٱلصَّٰلِحَٰتِ وَعَمِلُوا۟` in reverse preserves direct-object binding. The complete source phrase `الأعمال الصالحة والسيئة` becomes a lexical bridge indivisible from that grammar. Proper names and place names fall away at the occupancy firewall; suitability and repair remain coalition interfaces only.

Moving through `ءَامَنُوا۟ ٱلَّذِينَ إِلَّا`, the same surface group remains controller and the open faith complement remains open. The self coalition places quieting, assent, restraint, and inquiry beside one another without coreference. The opening of exception scope does not retroactively identify this group with S2's human.

Moving through `خُسْرٍ فِى ٱلْإِنسَٰنَ إِنَّ`, the loss mechanism remains withheld even after every measurement, trade, possession, and vacancy relation has been enacted. Human perception and self-reference relations remain lexical. The exact predication is not dissolved by the surrounding coalitions.

Returning to `وَٱلْعَصْرِ`, later `صبر` has supplied both `حبس` and `عصارة`; later `تواصوا` has supplied a joining operation that can connect the opening's night/day pair; later `حق` has supplied matching, threshold, and completion relations that touch several `عصر` tuples; later formal expansion has made the initial compression perceptible. These are backward transformations of function and structure. No later controller, content, state, material, or result becomes the occupant of the opening word.

## Formations Carried Forward With Typed Openings

- **Four-Act Controller Relay:** complete occupant path; first predicate's surface argument remains open.
- **Two Reciprocal Transmissions:** complete reciprocal topology and changed-content mapping; individual member allocation and downstream performance remain open.
- **Predicated Loss Occupancy:** participant and state complete; cause, controller, mechanism, scale, and aftermath open.
- **Pressed Material Becoming Product:** local lineages complete; medicinal tree-to-pressing event identity open.
- **Two-Way Boundary Dictionary:** exact structural circuit complete; no surface event occupant asserted.
- **Predicate Lexicon Cross-Weave:** exact role architecture complete; content handoff and performance deliberately unasserted.
- **Holding, Matching, Measurement, Vacancy, Extraction, Cloud, Threshold, Perception, Work, Possession, Speech, Appointment, and Affiliation coalitions:** every mapped tuple, invariant, and actual parameter delta complete; local occupants remain indexed.
- **Night/Day, formal expansion, doubled frame, opposition, derivation, homographic rotation, and small-return constellations:** complete carrier sets and backward effects preserved.
- **Weapon/stab attraction:** tool-part and penetration tuples complete; identity of a weapon in the stab remains open.
- **Road/traveler attraction:** traversed-road and walking-traveler tuples complete; identity of a road walked by those travelers remains open.
- **Well closure:** branch-image role remains constrained but unenacted because only flask/bottle closure receives a complete source tuple.
- **Fill to vessel sides:** boundary/extent position remains open; no filler identity is supplied.

These typed openings re-enter the next turn as openings, not as missing work and not as permission to insert compatible occupants.

