# V7.5.9 Turn 3: Reciprocal Primary Integration

Continue as the same formation author. The primary contextual scaffold now
enters the supplied field for the first time.

Treat the scaffold as new generative material. Its participants, actions,
relations, directions, contrasts, controller distributions, vacancies,
temporal movement, and formal positions can continue actuality, extend a
mapping, occupy a projected assembly role, join distant positions, or create a
new primary transformation.

Index every scaffold clause as `SOURCE_ACTUALITY` with provenance
`primary scaffold` and the same exact occurrence, identity, grammar,
controller, polarity, modality, scope, and vacancy precision as every other
supplied actuality. Continue to distinguish `LATENT_MAPPING` and
`LATENT_ASSEMBLY`. Add `PRIMARY_TRANSFORMATION` after a completed local
operation, mapping, or assembly has enacted its effect on an exact primary
action, relation, sequence, contrast, boundary, controller pattern, vacancy, or
formal position.

Keep every named operational configuration, structural constellation,
distributed mapping, and projective assembly intact as it meets the scaffold. A formation may deepen,
split, join another whole, or change its working name. It must not disappear
into a generic heading or a list of its former branch parts.

Begin with formations that still have an active relational direction. The
scaffold does not merely comment on what remains open: when it supplies a
participant, destination, force, response, consequence, or return, enact the
completed operation and carry its acquired force forward.

Keep every exact source-utterance circuit, distributed boundary carrier,
identity continuation, proposition or referent continuation, fresh performance,
complete-tuple topology mapping, projective assembly, controller counterfield,
vacancy comparison, functional transformation, formal pattern, and maximal
chain assembled as a relation.
Scaffold incorporation may enlarge or reposition it; it may not leave its
endpoints in separate sections while dropping the operation that made them a
finding.

Preserve exact microcontacts inside those relations. Re-enact mappings across
the actuality vectors supplied by their realizations. Independently project
facets into every assembly role opened by the scaffold or an existing
formation. Each projected vector keeps its source ID while the completed
assembly discovers a new division of labor, operation, and return.

## Scaffold-led projection through all four statuses

The scaffold supplies exact participants, actions, destinations, affected
scopes, polarity, sequence, boundaries, controller changes, vacancies, formal
positions, and passage consequences that earlier lexical formations could not
name alone. Represent each as an actuality node when identity is involved, a
mapping interface when a relation or form recurs, an assembly role wherever it
can perform a function inside a latent whole, and a primary-transformation
target wherever the completed whole changes the passage.

For each mature and mature-open formation, generate every scaffold actuality
continuation, mapping, projective assembly variant, and primary-transformation
path. Work in both directions: formation into every primary topology, then
every primary participant and relation into the formation. Enact every variant
fully before assigning its relation to the primary field.

A completed formation that changes an exact primary action, participant
distribution, controller relation, direction, polarity, boundary, vacancy,
sequence, temporal form, formal architecture, or consequence receives
`PRIMARY_TRANSFORMATION`. Other completed formations retain their
`SOURCE_ACTUALITY`, `LATENT_MAPPING`, and `LATENT_ASSEMBLY` statuses with their
complete local, relational, and projective effects. These are relation statuses,
not scores.

When a formation ends in a participant, state, result, proposition, referent,
repeatable role, or possible performance, continue it under its exact status
through every scaffold relation and projected assembly position. Let the
scaffold supply its own controllers and grammar. Let the latent assembly supply
its explicitly projected role vector. Follow reception, changed agency,
response, further transmission, and aftermath wherever the completed field
opens them.

Propagate every primary negation, exception, absence, and delayed participant
through every formation that contains the same typed position or a surface-
bound counterpart. Separately compare it with every complete secondary vacancy
mechanism whose absence type or consequence maps onto the primary relation.
Carry every enabled, prevented, redirected, renewed, terminal, irrecoverable,
or bounded consequence, not merely the fact that a word is negated. Keep the
primary polarity and scope on its actuality node while the vacancy consequence
operates through mappings and projected assemblies.

Throughout this projection, morphology and syntax retain control. A continuous
passage referent does not acquire a verb's agreement merely because it is the
relation's endpoint. Keep agreement controller, semantic initiator, referent,
possessor, beneficiary, predicate, complement, and delayed argument distinct
through every governing synthesis.

## Two-way rereading

For every mature and unfinished operation, constellation, mapping, and assembly, work
in both directions:

1. What does the formation make newly perceptible inside the primary
   proposition?
2. What does the primary proposition make newly perceptible or newly forceful
   inside the formation?

Do not settle for attaching an image or pattern to a conventional paraphrase.
Let the primary action enter an operational configuration and let that
configuration change the experienced geometry, agency, material, duration,
relation, pressure, or consequence of the action. Let the scaffold reveal what
a structural constellation's cross-definition, repetition, axis, convergence,
formal variation, or closure does to the direct sequence and contrast. Neither
mature form must be converted into the other.

After each reciprocal change, return to the exact passage and branch field.
The new integration may:

- complete an open role;
- join formations previously kept apart;
- separate formations that only looked identical;
- reveal a sequence of phases;
- change which participant carries an operation;
- turn a static image into a transformation;
- make an earlier small detail structurally decisive;
- reveal a changed-controller counterfield or a new-carrier succession;
- turn two local vacancy mechanisms into a passage-scale contrast;
- turn an ordered count or grouping vector into a formal enactment;
- create a new formation that neither source revealed alone.

Return also to every exact assertion in `source_phrase_ar` and every ordered
line or passage boundary. The scaffold may turn a small lexical equation,
definition, event statement, negation, repeated ending, or changed grammatical
position into a passage-scale circuit that was not visible before contextual
roles entered.

Return specifically to every formation that previously ended at an apparent
result or local endpoint. Index the result, affected participant, changed
relation, content schema, repeatable role, vacancy, topology, and formal
position, then generate every newly available actuality continuation, mapping,
and assembly role. Re-instantiate content or role under each fresh carrier and
project every result into every downstream position it can perform. Let
topology mappings, vacancy comparisons, controller counterfields, formal
patterns, and projective mechanisms continue the distributed movement. The
scaffold may reveal that an apparent endpoint was only the operation's middle.

Revisit all unfinished formations after incorporating the scaffold. Do not
limit this turn to formations already mature at the end of Turn 2.

## Reciprocal expansion

Bring every role-complete operation, relation-complete constellation,
interface-complete mapping, and role-complete assembly through reciprocal
scaffold search. Enact every generated actuality path, latent variant, and
primary-transformation path, whether its primary
effect is governing, local, bodily, material, relational, temporal, formal,
compositional, or a precise secondary recognition. A formation without such a
path remains complete in the latent reservoir with its own operation and return.

For each operational formation with a primary actuality or transformation
binding, complete this movement:

```text
formed configuration
+ exact primary proposition
-> the scaffold binds or redirects a typed port inside the configuration
-> the configuration changes the experienced primary action or relation
```

For a structural constellation, complete the parallel movement:

```text
formed constellation
+ exact primary sequence or contrast
-> the scaffold gives the supplied relation contextual force
-> the constellation changes the experienced architecture of the passage
```

For a distributed mapping, complete this movement:

```text
complete local tuples + exact mapping interfaces
-> invariant relation and every changed parameter become perceptible
-> an exact primary relation supplies direction, contrast, sequence, boundary,
   controller distribution, vacancy, or formal embodiment
-> the mapping changes that primary relation and acts backward on its local
   lexical carriers
```

For a projective assembly, complete this movement:

```text
source-indexed facets + projected functional roles
-> one fully inhabited latent operation, image, process, or structural mechanism
-> every result, response, aftermath, and backward return
-> the completed assembly changes an exact primary relation
```

State that completed change at full force. Keep its exact lexical bridges and
distinct roles attached to it. Do not merge it into a broader formation merely
because both contribute to the same primary proposition.

The activation reservoir remains available throughout the run. Material that
is still unfinished remains there for later recruitment rather than being
expanded into a generic macro-theme.

Preserve different formations when they alter different participants, forces,
directions, temporal relations, formal architectures, or closure effects.
`SOURCE_ACTUALITY` continues occupants through exact identity.
`LATENT_MAPPING` relates complete tuples through a carried invariant and its
changed parameters. `LATENT_ASSEMBLY` lets source-indexed facets perform
different roles inside one newly enacted whole. Keep all three statuses
recoverable when their effects combine.

After every formation has met the scaffold, place the mature formations in
contact through exact actuality relations, mapping interfaces, and projected
assembly roles. Enact each distinct directional exchange and the passage-level
consequences that none produced alone. Exact identity continues a source
occupant. A mapping carries its named invariant or schema while its complete
tuples remain distinct. A projective assembly creates assembly-local roles from
source-indexed facets. The formations remain independently recoverable while
their effects combine.

Two formations that bind the same primary carrier create parallel convergence
there; the shared destination transports no participant or scene between them.
Keep them independent when they change different controllers, participant
structures, vacancy classes, causal mechanisms, material embodiments, temporal
effects, formal architectures, or backward returns.

## Triple reciprocal fixed point

Scaffold contact reopens three whole-field frontiers. Traverse them in stable
passage and source-record order while preserving full source ID, role,
direction, polarity, modality, voice, scope, position, and route.

The **actuality frontier** contains every exact source or primary occurrence,
identity relation, participant, controller, state, result, vacancy, formal
contact, and released base/full-form/affix tuple. Continue it through exact
local grammar, identity, coreference, asserted derivation, and surface or
scaffold binding.

The **mapping frontier** contains every complete tuple, exact interface,
carried proposition, referent, role or performance schema, topology map,
controller counterfield, vacancy relation, ordered pattern, parameter change,
endpoint, and backward return. Generate every mapping supported by a named
relational invariant or explicit interface and retain all actual tuple fields.
This structural typing belongs to `LATENT_MAPPING`; it does not govern entry
into projective assembly.

The **assembly frontier** contains every source-indexed facet and every role
opened by an explicit source, passage, scaffold, mapping, or already formed
topology. Project every facet through every opened role as its own variant.
Fully inhabit the resulting operation, image, process, counterfield, or
structure; follow every supplied controller, participant, medium, direction,
phase, result, response, aftermath, return, and alternative assignment. A
generic role resemblance or functional complementarity opens this search; it
does not assert source identity or co-occurrence. The completed division of
labor is `LATENT_ASSEMBLY`.

For every completed actuality continuation, mapping, and assembly, generate
every reciprocal binding to every exact primary action, relation, participant
distribution, polarity, vacancy, sequence, boundary, controller pattern, and
formal position. Enact the full variant and backward return before recording
its `PRIMARY_TRANSFORMATION`. A primary transformation may expose new actuality
nodes, mapping interfaces, assembly roles, or topologies; return each one to
its own frontier.

Restart the triple cycle whenever any frontier changes. Continue until a
complete pass through raw records, the two earlier artifacts, the passage, and
the scaffold yields no new exact node, contact, mapping, projected role,
assembly variant, result, consequence, primary transformation, or backward
return. Only then organize the reciprocal field. Do not rank, grade, prune, or
estimate the strength of any synthesis while this formation cycle is active.

Use canonical novelty throughout: a transformation is a simple path through
typed states; a return to a visited state records one cycle and emits no longer
wrapper. A new projected role changes the canonical role system. Repeated
generic resemblance, self-embedding, or an unchanged result creates no new
item. Preserve every distinct filler, assignment, source vector, parameter
vector, consequence, and nonrepeating route.

An apparently empty cycle triggers four independent re-entries. First, repeat
exact form and base-term closure across every positioned passage carrier and
explicitly supplied form. Second, continue every actuality relation forward
and backward. Third, compare every complete tuple and formed assembly through
every mapping interface and parameter relation. Fourth, project every
accumulated source facet, primary facet, result, vacancy, consequence, and
formed assembly into every explicit topology and into pairwise and larger
division-of-labor interactions. Any new item restarts all four movements and
the triple cycle.

## Orthogonal primary-transformation field

For every exact primary carrier, relation, participant structure, sequence,
contrast, boundary, vacancy, controller distribution, and formal position,
preserve each completed route in this form:

```text
exact primary carrier or relation
+ complete local operation, mapping, assembly, or interaction
+ changed dimension
+ exact source route and relation payloads
+ backward effect
+ every independently convergent route
```

Two routes remain distinct when they differ in controller, participant
structure, vacancy type, causal mechanism, material embodiment, temporal
effect, formal architecture, or backward return. Their shared primary endpoint
does not consume either route. Complete all routes before later prose hierarchy.

## Primary meaning

Treat each exact contextual proposition as supplied meaning, not an ambiguity
to solve. Let latent activations deepen, reconfigure, or shift how its actions
and relations are perceived while keeping that exact proposition recoverable.
Do not select among lexical senses or present latent formation as
disambiguation. Express the enlargement positively through changed perception,
not through repeated methodological boundaries.

## Output

Write a complete reciprocal field to the path named in the task. Organize it
by named transformations, structural recognitions, and interacting formations
rather than passage sections or abstract categories. Preserve:

- the direct proposition before and after reciprocal rereading;
- each operational configuration's concrete operation and role system;
- each structural constellation's complete carrier set, exact relation, and
  passage topology;
- each distributed mapping's complete local tuples, interfaces, invariant
  relation, changed parameters, and passage-scale movement;
- each projective assembly's source-indexed facets, differentiated role
  assignments, complete operation, results, and returns;
- every exact source-utterance and surface-boundary circuit that acquires
  contextual force;
- every completed actuality continuation, schema or topology mapping,
  changed-controller counterfield, vacancy consequence, projective assembly,
  formal pattern, and primary-transformation binding;
- the distinction among agreement controller, continuous referent,
  relational endpoint, and delayed participant wherever they differ;
- details strengthened by the completed whole;
- formations created or completed by scaffold incorporation;
- interactions, backward changes, and passage closure;
- the new passage-level consequence produced by interaction between mature
  formations;
- fine-scale discoveries that sharpen a larger formation.
- the complete orthogonal primary-transformation field, including every
  independent route to a shared primary carrier.

For each named formation, make its operational roles or structural relations,
scaffold contribution, changed primary perception, and backward consequence
immediately recoverable. Use branch IDs and exact anchors where they help the
fresh synthesis author. Do not rank the formations or write publication prose.
