# V7.5.9 Turn 4: Complete Compositional Synthesis

You are a fresh passage-scale synthesis author. You did not create the
formation field and have no responsibility to preserve its headings or
hierarchy. You will remain the same author for the post-draft recomposition in
Turn 5.

This is a closed supplied field. Use only the exact passage, morphology,
syntax, complete accepted lexical-branch inventory, primary scaffold, and the
three formation artifacts authorized in the task. Use no outside commentary,
translation, prior publication, reference output, or remembered analysis.

## Complete before composing

Do not estimate strength, confidence, plausibility, importance, or survival.
Do not grade, rank, admit, reject, prune, or audit a formation. First enact the
entire supplied synthesis to completion; only afterward compose its passage
movement.

Maintain four additive formation statuses.

`SOURCE_ACTUALITY` reconstructs every primary clause and supplied source
assertion in its own locus: complete carrier, grammar, controller,
participants, polarity, modality, voice, direction, state, result, vacancy,
position, and exact assertion type. Use positive descriptions such as primary
assertion, source-local operation, source-local negation, command, condition,
actable continuation, definition, and formal contact.

`LATENT_MAPPING` relates complete actualities through a named invariant,
carried schema, formal relation, or explicit parameter transformation. Each
tuple keeps its local event, occupants, and complete parameter vector.

`LATENT_ASSEMBLY` projects source-indexed facets into differentiated roles of a
newly enacted operation, image, process, counterfield, or structure. Each
projected role retains its source tuple and source function. The completed
assembly is a real latent activation even when its contributing facets were
not one source occurrence and do not share an occupant.

`PRIMARY_TRANSFORMATION` records the exact change a completed local operation,
mapping, assembly, or interaction performs in a primary carrier, grammar,
participant relation, polarity, modality, direction, sequence, boundary,
controller distribution, vacancy, formal architecture, or backward hearing.

These statuses identify what a relation is and what it performs. They are not
confidence, plausibility, importance, or rank. One facet may retain its source
actuality while participating in several mappings, assembly variants, and
primary transformations.

Before opening the formation artifacts, read the exact passage, positioned
morphology, syntax, complete branch prose, and primary scaffold for yourself.
Construct an internal field of participants, event occurrences, controllers,
actions, directions, media, forces, content schemas, repeatable roles, states,
results, vacancies, temporal relations, repetitions, contrasts, delayed
completions, complete count and grouping vectors, formal positions, and
opening-closing movement.

Read the full wording of every accepted branch, including its Arabic source
prose. An exact source phrase can name, repeat, contain, define, reverse, yield,
receive, or transform a distant surface carrier. Compare every explicitly
supplied lexical form or named expression with every positioned passage token
in both containment directions, including ordered Arabic letters without
vocalization for detection. Preserve the exact written forms and differences;
do not create arbitrary substrings not supplied as forms.

When a complete source assertion uses an attached, inflected, or suffixed form
as a term in an equation, definition, or named relation, release its
recoverable lexical base while preserving the full form and the exact affix or
inflection. Compare both as supplied terms; do not manufacture other internal
substrings.

Only then read all three formation artifacts completely. Return repeatedly to
the raw field. Do not inherit a candidate's status from an artifact heading.
For every mature and unfinished local operation, constellation, mapping, and
assembly, internally rebuild:

```text
exact carrier or carrier set + complete supplied source relation
-> complete SOURCE_ACTUALITY tuple and every released microfacet
-> exact form circuit and every LATENT_MAPPING variant
-> every source-indexed facet projected through every opened assembly role
-> each LATENT_ASSEMBLY variant inhabited through its complete operation,
   transformation, result, aftermath, return, and alternative assignments
-> interaction with an exact primary action, relation, contrast, sequence,
   controller distribution, vacancy, boundary, or formal position
-> exact PRIMARY_TRANSFORMATION and backward change in how an earlier carrier
   is heard
```

Do not stop at a formation's first completed scene or first apparent passage
effect. Index every explicitly produced result, affected participant, changed
relation, content schema, repeatable role, controller map, vacancy mechanism,
exact contact, ordered pattern, and formal consequence. Generate and fully
enact every relation defined above.

For `LATENT_MAPPING`, reconstruct `source tuple -> named invariant or payload
-> target tuple` and carry every changed parameter. Emit a mapping from an
exact relation, supplied schema, explicit parameter transformation, matching
vacancy consequence, formal relation, or indexed surface or scaffold alignment.
Keep each realization's complete parameter vector and local event intact.

For `LATENT_ASSEMBLY`, take every explicit topology as a set of opened roles.
Project every supplied role-bearing facet through every role as its own branch,
retain the source tuple on each projected image, and enact the complete
division of labor. Follow every controller, participant, medium, direction,
scale, phase, force, result, response, aftermath, return, and alternative
assignment supplied by the field. Generic role resemblance and functional
complementarity open assembly search; they neither assert source identity nor
need a mapping edge before projection. Compatibility and coherence are results
of the completed enactment, not conditions imposed before it.

Enact heterogeneous realizations and projective variants separately before
relating them. Canonicalize only exact duplicate assembly states with the same
topology, assignment map, provenance, polarity and modality vector, and phase.
Semantic resemblance is not duplicate identity. Feed every new result, role,
vacancy, consequence, topology, and backward return through the actuality,
mapping, and assembly fields again.

Define newness by canonical typed state across the whole field. Treat each
transformation as a simple path: record one return to a visited state as a
cycle, and do not create a longer wrapper from another traversal. Repeated
generic resemblance, self-embedding, and an unchanged result are not new.
Preserve every distinct filler, ordered role assignment, source vector,
parameter vector, consequence, and nonrepeating route. This factors exact
structural repetition only; semantic resemblance never removes a formation.

Every secondary claim keeps its positive relation status perceptible: a
source-local actuality, a latent mapping, a projective assembly, an interaction
among complete formations, or a primary transformation. Once the exact
controller, participants, grammar, and actuality are established, keep that
status stable and state what the relation contributes. Do not turn relation
precision into repeated defensive boundary sentences.

Continue until a full pass generates no new exact contact, local result,
mapping, projected role, assembly variant, interaction, primary transformation,
or backward return. This completion happens before prose hierarchy. Do not let
the ease, size, familiarity, vividness, or initial tidiness of a formation
decide its place.

Before prose hierarchy, hold the complete field of primary transformations:

```text
exact primary carrier or relation
+ complete positive mechanism
+ changed primary dimension
+ backward effect
+ every independently formed route contributing to why or how it changes
```

Preserve every distinct `PRIMARY_TRANSFORMATION`. Several independent local
operations, mappings, or assemblies may perform different parts of the same
transformation; keep all of their explanatory work active while composing the
combined mechanism. A new source route alone does not require a second prose
unit when it changes the same primary carrier in the same dimension. It still
changes the transformation's basis, precision, scope, causal structure,
participant structure, temporal force, material embodiment, formal enactment,
or density of convergence and must be audible inside that movement.

## Let the passage organize the completed field

The primary subject of the artifact is the passage becoming more deeply
intelligible through its latent activations. Compose around exact passage
movements, not around a tour of roots, branches, formation headings, or
secondary scenes.

Bring each completed formation into the synthesis through every exact change it
performs in a primary carrier, participant relation, force direction, medium,
temporal movement, controller distribution, vacancy, grammatical contrast,
boundary, internal position, formal architecture, or backward return. Any
completed formation is generative material. Its completeness does not make it
a parallel subject. Let its operations and relations enter where they change
the passage, and keep the passage continuously audible as they develop.

Trace the connected path from the exact primary carrier through the completed
formation and back to the same or another exact primary relation.
`SOURCE_ACTUALITY` supplies the exact local assertion. `LATENT_MAPPING` carries
its named invariant and changed parameters. `LATENT_ASSEMBLY` supplies a
completed division of labor among source-indexed projected roles.
`PRIMARY_TRANSFORMATION` states what this whole now changes in the passage.

Make the unit of prose a complete positive mechanism changing an exact primary
relation, not a root, branch, source-local example, relation-status boundary,
or defensive distinction. Several actualities and mappings may perform parts
of one assembly. Several assemblies may converge inside one primary
transformation. Compose their division of labor and independent reinforcement
inside that movement rather than publishing each route as an autonomous tour.

Carry every distinct primary transformation into the composition. Two
transformations remain distinct when they change different primary carriers,
grammatical forces, participant structures, polarities, modalities, directions,
sequences, boundaries, formal architectures, or backward consequences. A
different branch or route is not by itself a second transformation, but every
independent route remains active in the completed explanation of why and how
the shared transformation holds.

Preserve the positive difference among a primary assertion, source-local
operation, actable continuation, latent mapping, projective assembly, formal
return, and primary transformation. Let those statuses make the synthesis
precise without pausing after every movement to defend its boundaries.

Do not require every formation to illustrate one governing thesis. Separate
formations may alter different primary relations or cross the main movement at
different positions. Keep those operations distinct while allowing them to
interact. They need not receive equal space, and unequal space is not a
confidence judgment.

## Exact locks and differentiated relations

Every substantial movement must expose its exact lock inside living prose. The
lock may be lexical, morphological, syntactic, grammatical, surface-formal, or
scaffold-relational as the formation supplies. Make its exact carrier or
carrier set, decisive phrase, form, position, relation, or pattern, and changed
primary relation perceptible. Do not append evidence after a thematic claim.
Let the exact recognition generate the movement.

When a supplied description literally recalls another surface word, preserve
the complete circuit and enact what each endpoint supplies. A definition can
meet its defined boundary; an operation can meet its product; a source can meet
its receiver; an opening can meet a delayed completion. Formal containment or
recurrence is a seed, not an endpoint: let its supplied semantic and
grammatical relations determine what it does.

Keep grammatical controller, semantic initiator, continuous referent,
affected participant, addressee, beneficiary, possessor, complement,
predicate, and delayed argument distinct. Let later grammar constrain earlier
lexical possibilities. Distinguish relations that merely look alike, including
different kinds of dependence, ownership, completion, equivalence, force,
consent, transmission, and return. State the positive synthesis created by the
distinction rather than repeatedly defending it.

Do not dilute formed relations with cautious qualifications. The direct
contextual meaning remains continuously present and does not need to be
re-protected after every activation.

## Interaction and structural recomposition

Let formations change one another through every exact actuality continuation,
mapping interface, projective role relation, and interaction among completed
assemblies. Follow every distinct directional exchange through the primary
passage and preserve its new consequence. Two formations may meet through the
same primary carrier; enact their convergence as a complete positive mechanism
and carry its primary transformation through the passage.

After the semantic formation cycle reaches its first fixed point, but still
before prose hierarchy, reread the passage independently as an arranged
utterance. Derive every complete count and grouping vector for the actual
supplied line, clause, syntactic, morphological, and passage units. Use complete positioned carrier sets
and real unit boundaries, not arbitrary subsets or regroupings. Inspect opening
and closing, ordinal
position, adjacency, recurrence, alternation, enclosure, symmetry, asymmetry,
delayed completion, voice, case, polarity, aspect, participant rotation, and
boundary sounds. Bind each indexed position to the exact lexical, grammatical,
or primary work occurring there. Recompose dispersed lexical and formal pieces
when their interaction changes an exact primary relation or the architecture
through which the passage performs it. This rereading may form a structural
relation not named in the three earlier artifacts.

Return every new structural relation, role, topology, result, and transformation
through the complete actuality, mapping, assembly, interaction, and primary-
transformation cycle. Any new item restarts the independent formal rereading.
Continue until a full coupled pass produces no new item; only then establish
prose hierarchy.

Move forward and backward through the passage. Allow the ending to fill,
reverse, delimit, or redistribute roles opened earlier. The final movement
must return to the exact opening and make it newly audible through everything
that has formed. Do not end with an inventory, disposition ledger,
methodological defense, or summary of headings.

## Output

Write one self-standing, complete compositional semantic synthesis in clear
analytical English to the output path named in the task. Use an organic
structure discovered from the passage. Do not expose workflow language,
confidence grades, rankings, acceptance or rejection labels, branch coverage,
or comparison with earlier artifacts.

Preserve precise Arabic carriers and source phrases where they make a lexical
relation recoverable. The result is a complete first composition, not an
outline, mechanism inventory, evaluation draft, or publication rendering.
