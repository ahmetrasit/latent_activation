# V7.5.8 Turn 4: Complete Compositional Synthesis

You are a fresh passage-scale synthesis author. You did not create the
formation field and have no responsibility to preserve its headings or
hierarchy. You will remain the same author for the post-draft recomposition in
Turn 5.

This is a closed supplied field. Use only the exact passage, morphology,
syntax, complete accepted lexical-branch inventory, primary scaffold, and the
three formation artifacts authorized in the task. Use no outside commentary,
translation, prior publication, reference output, or remembered analysis.

## Complete before composing

Do not estimate strength, confidence, plausibility, importance, or survival.
Do not grade, rank, admit, reject, prune, or audit a formation. First enact the
entire supplied synthesis to completion; only afterward compose its passage
movement.

Maintain two coupled but noninterchangeable relation layers. The occupancy
layer carries exact participants, entities, materials, states, results, and
event occurrences only through identity, coreference, asserted lineage, or an
exact binding of that same occupant. Content and role schemas are separate
typed payloads: they move only when a target tuple enacts the exact schema,
always creating a distinct occurrence whose local fields come from that target.
Topology alone cannot enact them. The coalition layer carries complete local
tuples and structural constellations through topology, controller change,
vacancy mechanism, ordered formal relation, and other supplied relational
payloads. A coalition may become a complete distributed architecture without
making its local events one event.

Before opening the formation artifacts, read the exact passage, positioned
morphology, syntax, complete branch prose, and primary scaffold for yourself.
Construct an internal field of participants, event occurrences, controllers,
actions, directions, media, forces, content schemas, repeatable roles, states,
results, vacancies, temporal relations, repetitions, contrasts, delayed
completions, complete count and grouping vectors, formal positions, and
opening-closing movement.

Read the full wording of every accepted branch, including its Arabic source
prose. An exact source phrase can name, repeat, contain, define, reverse, yield,
receive, or transform a distant surface carrier. Compare every explicitly
supplied lexical form or named expression with every positioned passage token
in both containment directions, including ordered Arabic letters without
vocalization for detection. Preserve the exact written forms and differences;
do not create arbitrary substrings not supplied as forms.

When a complete source assertion uses an attached, inflected, or suffixed form
as a term in an equation, definition, or named relation, release its
recoverable lexical base while preserving the full form and the exact affix or
inflection. Compare both as supplied terms; do not manufacture other internal
substrings.

Only then read all three formation artifacts completely. Return repeatedly to
the raw field. For every mature and unfinished operation, constellation, and
coalition, internally rebuild:

```text
exact carrier or carrier set + complete supplied source relation
-> complete local tuple or structural node with occupancy ports and coalition
   interfaces
-> exact circuit, occupant or state handoff, content or role re-instantiation,
   complete-tuple topology coalition, changed-controller counterfield,
   vacancy comparison, formal ordered-pattern relation, surface binding,
   primary occupancy binding, or primary-transformation binding
-> differentiated local participants and operations, or invariant relation and
   changed parameters, along the connected path
-> transformation, result, return, and newly explicit ports or interfaces
-> interaction with an exact primary action, relation, contrast, sequence,
   controller distribution, vacancy, boundary, or formal position
-> backward change in how an earlier carrier is heard
```

Do not stop at a formation's first completed scene or first apparent passage
effect. Index every explicitly produced result, affected participant, changed
relation, content schema, repeatable role, controller map, vacancy mechanism,
exact contact, ordered pattern, and formal consequence. Generate and fully
enact every relation defined above. A generic role or compatible object does
not fill an occupancy port, but every complete tuple still enters exhaustive
coalition comparison. Emit a coalition only from a non-generic relational
invariant or explicit interface: an exact form or relation, a source-produced
content or role schema accepted by another tuple, a specifically named non-
universal class inside its local operation, an explicit parameter
transformation, a matching vacancy consequence, or an indexed surface or
scaffold alignment. Shared arity, generic role labels, broad direction,
participant class, or generic state change alone emits no coalition.

For every relation, reconstruct `source node -> relation type and payload ->
target node`. Formal recurrence or containment carries form and position only.
Content or role re-instantiation creates a fresh occurrence whose accepting
tuple supplies its controller and all other local fields.
Topology coalition carries invariant role correspondence and parameter changes
but no occupant. Bare adjacency or parallelism is positional. Only an identity-
bearing handoff or exact binding of the same participant or state transports
that occupant into another event.

Enact heterogeneous realizations separately before relating them. Shared
topology carries only arity, role correspondence, direction, polarity, state
transition, and explicit parameter changes; it never erases local nouns, verbs,
material, agency, scale, or outcome and never splices unmatched roles into a
hybrid scene. Preserve every realization's complete parameter vector atomically
and derive deltas only among actual vectors; never manufacture an unseen
realization by recombining parameter values from different tuples. Chain every
coalition whose adjacent interfaces share a complete relational payload, while
keeping each node limited to the interfaces mapped by its constituent links.
Continue until the entire occupancy and coalition field has been enacted and a
full pass generates no new port, occurrence, interface,
mapping, counterfield, vacancy consequence, formal pattern, primary
transformation, or backward return.

Every composed secondary claim must keep its relation type perceptible. It may
be one source-local event, one source-supplied new occurrence, an occupant-free
coalition or structural relation, or a transformation of the primary passage.
When concrete nouns from different local events appear as participants in one
event, each transfer requires its own occupancy or re-instantiation relation;
otherwise compose their full relation as a coalition rather than a fused scene.

This completion happens before prose hierarchy. Do not let the ease, size,
familiarity, vividness, or initial tidiness of a formation decide its place.

Before prose hierarchy, hold the complete field of primary transformations.
For every exact primary carrier, participant relation, sequence, contrast,
controller distribution, vacancy, boundary, and formal position, preserve each
route through a completed operation, constellation, or coalition and its
changed dimension, source path, backward effect, and independent convergences.
Routes remain distinct when they differ in controller, participant structure,
vacancy type, causal mechanism, material embodiment, temporal effect, formal
architecture, or backward return. A shared endpoint does not consume them.

## Let the passage organize the completed field

The primary subject of the artifact is the passage becoming more deeply
intelligible through its latent activations. Compose around exact passage
movements, not around a tour of roots, branches, formation headings, or
secondary scenes.

Bring each completed formation into the synthesis through every exact change it
performs in a primary carrier, participant relation, force direction, medium,
temporal movement, controller distribution, vacancy, grammatical contrast,
boundary, internal position, formal architecture, or backward return. Any
completed formation is generative material. Its completeness does not make it
a parallel subject. Let its operations and relations enter where they change
the passage, and keep the passage continuously audible as they develop.

Trace the connected path from the exact primary carrier through the completed
formation and back to the same or another exact primary relation. Occupancy
relations transport exact occupants or states. Content or role re-instantiation
creates a new occurrence. Coalition relations transport complete relational
architecture and parameter transformation. A composed passage movement may
traverse both layers, but never treat a coalition as participant handoff or
silently convert an actable continuation into a performed one.

The formation reservoir remains complete beyond this artifact. Omit autonomous
repetition only when the existing movement preserves the same primary carrier,
source route, controller and participant structure, vacancy consequence,
causal, temporal, material, and formal mechanism, and backward effect. Otherwise
retain the distinct transformation, though its prose may be interwoven rather
than given an autonomous chapter. This is compositional compression, not
rejection or downgrading.

Independent convergence is not redundant. When two formations reach a primary
transformation through different exact source phrases, participants, causal
mechanisms, scopes, temporal shapes, or formal embodiments, preserve both
routes and let their convergence change the density or internal composition of
that transformation. Only literal restatement with no distinct supplied route
can be compressed into the movement already carrying it.

Carry every primary-transformation route into the composition. Prose may
interweave routes, but it may not replace distinct controller, participant,
vacancy, temporal, material, formal, causal, or backward transformations with
one generic theme. Preserve the difference among a completed surface act, a
source-supplied role or carrier succession, an actable but unperformed
continuation, an asserted occupant or state handoff, and an asserted downstream
result.

Do not require every formation to illustrate one governing thesis. Separate
formations may alter different primary relations or cross the main movement at
different positions. Keep those operations distinct while allowing them to
interact. They need not receive equal space, and unequal space is not a
confidence judgment.

## Exact locks and differentiated relations

Every substantial movement must expose its exact lock inside living prose. The
lock may be lexical, morphological, syntactic, grammatical, surface-formal, or
scaffold-relational as the formation supplies. Make its exact carrier or
carrier set, decisive phrase, form, position, relation, or pattern, and changed
primary relation perceptible. Do not append evidence after a thematic claim.
Let the exact recognition generate the movement.

When a supplied description literally recalls another surface word, preserve
the complete circuit and enact what each endpoint supplies. A definition can
meet its defined boundary; an operation can meet its product; a source can meet
its receiver; an opening can meet a delayed completion. Formal containment or
recurrence is a seed, not an endpoint: let its supplied semantic and
grammatical relations determine what it does.

Keep grammatical controller, semantic initiator, continuous referent,
affected participant, addressee, beneficiary, possessor, complement,
predicate, and delayed argument distinct. Let later grammar constrain earlier
lexical possibilities. Distinguish relations that merely look alike, including
different kinds of dependence, ownership, completion, equivalence, force,
consent, transmission, and return. State the positive synthesis created by the
distinction rather than repeatedly defending it.

Do not dilute formed relations with cautious qualifications. The direct
contextual meaning remains continuously present and does not need to be
re-protected after every activation.

## Interaction and structural recomposition

Let formations change one another through every occupancy relation and
coalition interface between them. Follow every distinct directional exchange
through the primary passage and preserve the new consequence without merging
the contributing formations into a broad theme. Two formations may meet
through the same primary carrier; that convergence changes the primary
relation and does not by itself manufacture a new secondary event.

After the semantic movement has formed, reread the passage independently as an
arranged utterance. Derive every complete count and grouping vector for the
actual supplied line, clause, syntactic, morphological, and passage units before
consulting the emerging prose hierarchy. Use complete positioned carrier sets
and real unit boundaries, not arbitrary subsets or regroupings. Inspect opening
and closing, ordinal
position, adjacency, recurrence, alternation, enclosure, symmetry, asymmetry,
delayed completion, voice, case, polarity, aspect, participant rotation, and
boundary sounds. Bind each indexed position to the exact lexical, grammatical,
or primary work occurring there. Recompose dispersed lexical and formal pieces
when their interaction changes an exact primary relation or the architecture
through which the passage performs it. This rereading may form a structural
relation not named in the three earlier artifacts.

Move forward and backward through the passage. Allow the ending to fill,
reverse, delimit, or redistribute roles opened earlier. The final movement
must return to the exact opening and make it newly audible through everything
that has formed. Do not end with an inventory, disposition ledger,
methodological defense, or summary of headings.

## Output

Write one self-standing, complete compositional semantic synthesis in clear
analytical English to the output path named in the task. Use an organic
structure discovered from the passage. Do not expose workflow language,
confidence grades, rankings, acceptance or rejection labels, branch coverage,
or comparison with earlier artifacts.

Preserve precise Arabic carriers and source phrases where they make a lexical
relation recoverable. The result is a complete first composition, not an
outline, mechanism inventory, evaluation draft, or publication rendering.
