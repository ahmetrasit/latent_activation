# Turkish Prose Integration Plan — S001, S100, S103

## Objective

Produce layered Turkish prose for S001, S100, and S103 by integrating:

1. `slm_local` cheap channel-axis candidates;
2. `v11` high-recall discovery findings;
3. `v12` ayah-attached analytical readings;
4. `v3` passage-synthesis/prose architecture where available and useful.

The final output must support different user use cases without forcing one
artifact to do all jobs.

Core rule:

```text
Ayah first, channel later, evidence underneath.
```

Level-separation rule:

```text
Do not put all sources into one undifferentiated finding pool.

Ayah layer:
  v12-led

Surah/pericope channel layer:
  slm_local + v11-led, with v3 synthesis/prose support

In-between navigation:
  slm_local channel membership linked to v12 ayah claims
```

Reviewer constraint:

```text
Do not generate prose until source manifests, reference alignment, source
finding ledger, and closed coverage audit exist for the target slice.
```

## Target Surahs

Initial scope:

- S001
- S100
- S103

These are chosen because they already have the major ingredients:

- `network/slm_local/output/s001`
- `network/slm_local/output/s100`
- `network/slm_local/output/s103`
- `v11/run/s001`, `v11/run/s100`, `v11/run/s103`
- `v12/runs/s001`, `v12/runs/s100`, `v12/runs/s103`
- `v3/run/s001...`, `v3/run/s100...`, `v3/run/s103...` where available

Implementation starts with S103 as the first complete vertical slice. S103 is
short enough to manually inspect while still exercising:

- `slm_local` channel axes;
- v11 discovery reservoir;
- v12 ayah-attached readings;
- v3/pilot synthesis material where available.

S001 is not rendered until the basmalah/reference-alignment issue is solved.

## Source Roles

### `slm_local`

Role: cheap surah/pericope channel-axis detector and ayah↔channel navigation
source.

Use:

- identify candidate surah/pericope channels;
- provide ayah-to-channel membership hints;
- provide root/branch/domain support examples;
- help create navigation links.

Do not use:

- as a final interpretation;
- to delete v11/v12 findings;
- to force every ayah into every stable domain;
- as a direct source for `Dinle` or `Derinleş` except as a small navigation
  hint after the v12 ayah reading is already stable.

Important: `stable` means graph/channel robustness, not interpretive centrality.
Stable channels are reviewed first; they are not accepted automatically.

Accepted channel statuses:

- `stable`: eligible for channel review;
- `mixed`: evidence-only unless v11/v12/v3 strongly corroborate it;
- `weak`: evidence-only by default.

A rendered channel requires:

- distributed, ayah-specific support;
- compatibility with v12 primary readings;
- narratable passage movement;
- corroboration by another source role;
- explicit disposition for the source `slm_local` candidate.

### `v11`

Role: high-recall discovery reservoir for surah/pericope channels, with limited
promotion into ayah expansion only when it clearly attaches to a fixed ayah.

Use:

- `09-final-report.md`;
- `06-mechanism.md`;
- `07-secondary-expansion.json`;
- `10-discovery-ranking.json`;
- `05-activation-pass.json` when needed.

Extract:

- surprising branch/bridge findings;
- passage-level mechanisms;
- high-value candidates v12 may miss;
- weak/exploratory findings that should not silently disappear.

Do not use:

- v11 Turkish prose as exhaustive source;
- v11 novelty as automatic spoken-core content.

Promotion rule:

A v11 finding may enter ayah-level `Derinleş` only if:

1. it attaches to a fixed ayah;
2. it clarifies, reinforces, nuances, or reframes that ayah;
3. it does not conflict with the v12 primary reading;
4. it has a human-readable evidence explanation.

Otherwise, v11 findings belong in `Kanallar`, `İzini Sür`, or
`deferred/rejected`.

### `v12`

Role: primary ayah-level source and ayah-attached analytical control.

Use:

- the exact reader output pinned by `frozen_run.json` as primary ayah-level
  source.

Extract per ayah:

- primary reading;
- fixed-ayah anchor;
- activated readings;
- retrospective surprises;
- root/branch evidence;
- links to candidate channels.

Do not use:

- compact `*-butuncul-okuma.md` as source of complete findings.

Protection rule:

- v12 ayah claims must be extracted and frozen before matching `slm_local`
  channels;
- each in-scope ayah must have at least one `v12_primary` claim;
- other sources may enrich, connect, or challenge v12 claims, but may not
  silently demote or rewrite them.
- `Dinle` and `Derinleş` are v12-led. Other sources enter those layers only
  through explicit promotion or navigation-link rules.

### `v3`

Role: passage-scale synthesis and Turkish prose architecture, especially for
`Kanallar`.

Use:

- `a2/mechanism-map.md`;
- `a1/discovery-integrated.md`;
- publication files as style references only.

Extract:

- governing channels;
- passage-level ordering;
- closure logic;
- Turkish rhythm and non-inventory prose strategies.

Do not use:

- v3 publication prose as exhaustive discovery evidence.

## Canonical Intermediate Model

Before writing prose, create normalized records.

### Source manifest

Each surah must have:

```text
_prose/tr/output/s###/model/source_manifest.json
```

Fields per source artifact:

```text
source_run_id
source_version: slm_local | v11 | v12 | v3
source_role: enumerator | analytical_control | synthesis | style_only
path
sha256
status
notes
```

Rules:

- v12 source must be resolved through `frozen_run.json`.
- v3 runs must be pinned exactly; no glob-based source selection.
- source agreement does not automatically increase confidence when artifacts
  share underlying lexical/network material.

### Reference map

Each surah must have:

```text
_prose/tr/output/s###/model/reference_map.jsonl
```

Fields:

```text
source_version
source_ref
canonical_ref
display_ref
scope_role: ayah | contextual_basmala
notes
```

Rules:

- canonical references are used internally;
- display references are user-facing;
- S001 basmalah alignment must be explicit because v12 uses `1:0` while other
  sources may treat the basmalah as `1:1`;
- S100/S103 contextual basmalas must be explicitly marked as user-facing ayah
  or context-only.

### Source finding ledger

Each surah must have:

```text
_prose/tr/output/s###/model/source_findings.jsonl
```

Every enumerating artifact contributes atomic source findings with:

```text
source_finding_id
source_run_id
source_version
source_artifact
source_pointer
source_ref
canonical_ref
finding_text
roots
branches
finding_type: primary | activated_reading | retrospective_surprise | bridge | channel_candidate | mechanism | ranking_hint
enumerates_finding: true | false
notes
```

Rules:

- v11 duplicates across `05`, `06`, `07`, `09`, and `10` must be deduplicated;
- duplication does not increase confidence;
- every v12 retrospective surprise must become a source finding.

### Claim record

Required fields:

```text
claim_id
surah
scope_type: ayah | pericope | surah
claim_type: ayah_claim | channel_claim | navigation_link | evidence_claim
scope_refs
assertion_tr
function: clarify | reinforce | nuance | reframe | connect
confidence: strong | medium | exploratory
disposition: accepted | merged | split | evidence_only | deferred | rejected | conflict
render_targets: spoken_core | expanded_ayah | channel | evidence_only
source_finding_ids
source_run_ids
linked_evidence_ids
linked_channel_ids
surprise: true | false
evidence_mode: direct | structural | bridge | analogical | channel_prior
decision_reason
notes
```

### Evidence record

Required fields:

```text
evidence_id
surah
anchor_ref
anchor_phrase_ar
support_refs
roots
branches
branch_images
source_artifact
source_quote_or_pointer
support_explanation_tr
counterevidence_or_limit
```

### Channel record

Required fields:

```text
channel_id
surah
title_tr
scope_refs
thesis_tr
source_channel_ids
ordered_ayah_contributions
linked_claim_ids
linked_navigation_claim_ids
linked_evidence_ids
disposition: accepted | merged | split | evidence_only | deferred | rejected | conflict
decision_reason
notes
```

### Navigation-link record

Navigation links may be stored as `claim_type=navigation_link` in
`claims.jsonl` or as a dedicated downstream view.

Required fields:

```text
navigation_id
surah
canonical_ref
display_ref
channel_id
linked_ayah_claim_ids
link_text_tr
source_channel_id
strength: primary | secondary | conditional
notes
```

Rules:

- navigation is not another interpretive essay;
- from ayah, it points to relevant channels after the ayah reading;
- from channel, it points back to the specific ayah contribution/claim;
- avoid undifferentiated lists of every channel attached to an ayah.

## Planned Output Layout

For each surah:

```text
_prose/tr/output/s001/
  model/
    source_manifest.json
    reference_map.jsonl
    source_findings.jsonl
    claims.jsonl
    evidence.jsonl
    channels.jsonl
    coverage.tsv
  ayah/
    001-001-dinle.md
    001-001-derinles.md
    ...
  channels/
    s001-kanallar.md
  evidence/
    s001-izini-sur.md
```

Same layout for `s100` and `s103`.

## Layer Contracts

### `Dinle`

Default first-listening ayah prose.

Contract:

- 30–60 seconds as an upper range, not a forced target for short ayat;
- start with primary reading;
- identify likely confusion if present;
- include at most one or two necessary secondary activations;
- return explicitly to focused ayah;
- no branch IDs;
- no repeated root-inventory prose;
- v12-led by default;
- may include a short channel hint only after the focused ayah is already clear.

### `Derinleş`

Expanded ayah prose.

Contract:

- preserve all important v12 ayah-attached readings;
- include v11 discoveries only when they pass the ayah-level promotion rule;
- mention channel links;
- distinguish strong readings from exploratory echoes;
- still avoid inventory-style prose.

### `Kanallar`

Surah/pericope channel prose.

Contract:

- based on reviewed `slm_local` channel candidates plus v11 discovery and v3
  synthesis;
- channel titles must be plain Turkish movement titles, not domain labels;
- every participating ayah receives a clear contribution statement;
- link back to ayah claim IDs;
- distinguish primary movements, secondary echoes, and conditional fields;
- do not include channels that cannot be narrated as a movement.

### `İzini Sür`

Evidence/provenance layer.

Contract:

- preserve all roots/branches/provenance;
- include `slm_local`, v11, v12, v3 source pointers;
- include rejected/deferred findings with reasons;
- expose concise accepted findings first, then expandable exploratory/rejected
  material;
- not intended as default spoken prose.

## Integration Procedure

### Phase 1 — Inventory

For S001, S100, S103:

1. list available source artifacts;
2. identify canonical v12 reader file through `frozen_run.json`;
3. identify canonical v11 final/mechanism files;
4. identify available v3 mechanism/discovery files and pin exact run maturity
   (`full`, `pilot`, `style_only`);
5. load `slm_local` channel candidates;
6. write `source_manifest.json`;
7. write `reference_map.jsonl`.

### Phase 2 — v12 Baseline Extraction

For each ayah:

1. extract v12 primary claim;
2. extract activated readings;
3. extract retrospective surprises;
4. write source findings;
5. create initial claim/evidence records;
6. mark at least one claim per in-scope ayah as `v12_primary`.

No `slm_local` matching occurs before this phase is complete. The ayah layer is
constructed first.

### Phase 3 — Channel Prior

For each surah:

1. read `slm_local/domain_channel_candidates.tsv`;
2. read `domain_channel_candidates.jsonl` for `per_ayah_build`;
3. inspect `slm_edges.jsonl` when support examples are insufficient;
4. create source findings for every stable/mixed/weak channel candidate;
5. assign initial disposition:
   - `unreviewed` until explicitly accepted/evidence-only/deferred/rejected;
6. map each reviewed candidate to a provisional Turkish movement title;
7. mark broad/noisy channels for review, especially all-surah channels;
8. create provisional `navigation_link` records connecting channel memberships
   to existing v12 ayah claims, without changing the v12 claims.

### Phase 4 — v11 Discovery Merge

For each v11 finding:

1. create or deduplicate source-finding record;
2. treat the finding as channel/surah-level by default;
3. match to existing channel candidate if possible;
4. match to existing v12 ayah claim only if it passes the ayah-level promotion
   rule;
5. add as evidence if already represented;
6. create new channel claim if missing and interpretable;
7. create new ayah claim only when fixed-ayah attachment is explicit;
8. create or refine navigation links when the finding explains an ayah's role
   in a channel;
9. assign disposition and render targets:
   - `expanded_ayah` if fixed-ayah relevant;
   - `channel` if passage-level;
   - `evidence_only` if interesting but not user-facing;
   - `deferred` if useful but not ready;
   - `rejected` if unsupported or too remote.

### Phase 5 — v3 Synthesis Merge

Use v3 to:

1. merge/split rough `slm_local` axes into narratable channels;
2. improve Turkish channel titles;
3. order ayah contributions;
4. shape final prose rhythm.

Do not allow v3 to silently erase claims from v11/v12.

Add pre-v3/post-v3 claim conservation:

- every accepted pre-v3 claim remains linked after synthesis;
- if v3 merges claims, predecessor claim IDs are retained;
- if v3 omits or weakens a claim, record `disposition=conflict`, `deferred`,
  or `rejected` with `decision_reason`.

v3 acts mainly on channel claims and channel prose. It may suggest improved
ayah prose style, but it does not rewrite v12 ayah claims unless that rewrite is
recorded as a render-only transformation linked back to the original claim.

### Phase 6 — Coverage Audit

The audit must be mechanically closed.

Failures:

- source finding with `unreviewed` disposition;
- v12 retrospective surprise not mapped, merged, deferred, or rejected;
- user-facing claim with no resolvable evidence;
- accepted channel contribution without linked ayah claim;
- navigation link without linked channel and linked ayah claim;
- merge/split without predecessor IDs;
- prose claim outside the ledger;
- pre-v3 accepted claim missing after v3 synthesis;
- unresolved source reference;
- S001/S100/S103 basmalah display/canonical reference ambiguity.

Every source finding must have one disposition:

```text
accepted
merged
split
evidence_only
deferred
rejected
conflict
```

Every accepted claim must have one or more render targets:

```text
spoken_core
expanded_ayah
channel
evidence_only
```

Every rendered channel must have:

- at least one accepted channel claim;
- ordered ayah contributions;
- linked navigation records;
- evidence from `slm_local` plus at least one of v11/v3 or explicit v12
  corroboration.

The audit also reports counts by source version, disposition, and render target.

### Phase 7 — Turkish Render

Render in this order:

1. `Dinle`;
2. `Derinleş`;
3. `Kanallar`;
4. `İzini Sür`.

Rationale:

- `Dinle` sets first-hearing clarity;
- `Derinleş` preserves ayah-specific depth;
- `Kanallar` reveals broader axes after local readings;
- `İzini Sür` keeps the whole system auditable.

## Prose Style Constraints

Avoid:

```text
Bu kök şunu gösterir; şu kökte de bu vardır.
```

Prefer:

```text
Ayet bu imgeyle itirazı en uç noktaya taşır.
Bu ifade önceki sorunun sınırını genişletir.
Bu karşılık sûre boyunca süren kanala bağlanır.
```

Rules:

- primary reading first;
- surprise only after orientation;
- scope must be explicit;
- roots/branches hidden from `Dinle`;
- roots/branches available in `İzini Sür`;
- no silent deletion of discoveries;
- no channel forced into prose unless it can explain movement.

## Initial Surah-Specific Expectations

### S001

Likely major channels from `slm_local`:

- judgment/restraint;
- favor/blessing/attachment;
- authority/obedience;
- guidance/road;
- water/growth as secondary or evidence-level unless strongly corroborated.

Expected prose challenge:

- avoid turning Fatiha into a technical map;
- keep worship/request/guidance clear for first listening;
- reserve broader channel architecture for `Kanallar`;
- solve basmalah alignment before any rendering.

### S100

Likely major channels from `slm_local`:

- motion/incursion/center;
- fire/light/sparks;
- water/growth/fertility/barrenness;
- concealment/extraction/testimony;
- conflict/judgment/restraint.

Expected prose challenge:

- preserve the vivid horse/raid surface;
- show how inner human ingratitude and final extraction emerge;
- avoid over-literalizing water/growth or harvest as surface translation;
- remember that `slm_local` stability is not semantic centrality.

### S103

Likely major channels from `slm_local`:

- loss/economy/attachment;
- restraint/obligation/patience.

Expected prose challenge:

- make `asr` and `khusr` intelligible without overloading;
- show how faith/work/truth/patience form a rescue system;
- keep the prose short enough for the surah’s density.

## First Implementation Slice

Start with S103 only.

Required before prose generation:

1. `source_manifest.json`;
2. `reference_map.jsonl`;
3. `source_findings.jsonl`;
4. normalized `claims/evidence/channels`;
5. disposition for both S103 `slm_local` channels;
6. coverage audit with zero unreviewed source findings;
7. S001 reference-map fixture for basmalah alignment.

Only after this slice passes should S001 and S100 generation begin.

## Review Questions

The reviewer should assess:

1. Does this plan correctly separate source roles?
2. Is `slm_local` used strongly enough as cheap channel detector but safely
   enough not to dominate?
3. Is v12 still protected as ayah-attached control?
4. Is v11 used as discovery coverage rather than prose authority?
5. Is v3 used for synthesis without silent trimming?
6. Are the output layers sufficient for user experience?
7. Is the coverage audit strong enough to prevent dropped surprises?
8. What should be changed before generating S001/S100/S103 outputs?
